---
phase: 08-tool-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlueCode.Core/Domain.fs
  - src/BlueCode.Core/AgentLoop.fs
  - src/BlueCode.Cli/Adapters/Json.fs
  - src/BlueCode.Cli/CompositionRoot.fs
  - src/BlueCode.Cli/Adapters/FsToolExecutor.fs
  - tests/BlueCode.Tests/LlmPipelineTests.fs
  - tests/BlueCode.Tests/CompositionRootTests.fs
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "Domain.fs Tool DU has 3 new cases: EditFile, GlobSearch, GrepSearch"
    - "llmStepSchema action enum accepts 8 values (including edit_file, glob_search, grep_search)"
    - "System prompt describes all 8 tool actions with input schemas"
    - "Project compiles end-to-end with new tool dispatch paths (FsToolExecutor.create has arms for all 7 Tool DU cases via stubs for the 3 new ones)"
    - "Existing v1.1 test suite (218 tests) still passes after enum/prompt updates"
  artifacts:
    - path: "src/BlueCode.Core/Domain.fs"
      provides: "Three new Tool DU cases: EditFile(FilePath, oldStr, newStr); GlobSearch(pattern, FilePath option); GrepSearch(pattern, FilePath option, string option)"
      contains: "EditFile"
    - path: "src/BlueCode.Core/AgentLoop.fs"
      provides: "Three new dispatchTool match arms ('edit_file', 'glob_search', 'grep_search') parsing JSON input into Tool DU cases"
      contains: "\"edit_file\""
    - path: "src/BlueCode.Cli/Adapters/Json.fs"
      provides: "llmStepSchema enum extended from 5 to 8 values"
      contains: "\"edit_file\", \"glob_search\", \"grep_search\""
    - path: "src/BlueCode.Cli/CompositionRoot.fs"
      provides: "defaultSystemPrompt describes 8 actions and their input schemas"
      contains: "edit_file"
    - path: "src/BlueCode.Cli/Adapters/FsToolExecutor.fs"
      provides: "create() match block has 7 arms — 4 existing impls + 3 stubs (`failwith \"not implemented (filled by plan 08-02)\"` acceptable)"
      contains: "EditFile"
  key_links:
    - from: "Domain.fs Tool DU"
      to: "AgentLoop.fs dispatchTool"
      via: "actionName -> Tool case mapping"
      pattern: "\"edit_file\" ->"
    - from: "AgentLoop.fs dispatchTool"
      to: "FsToolExecutor.create match block"
      via: "Tool DU pattern match (exhaustive — must match all 7 cases)"
      pattern: "EditFile|GlobSearch|GrepSearch"
    - from: "Json.fs llmStepSchema"
      to: "CompositionRoot.fs defaultSystemPrompt"
      via: "both must advertise the same 8 action names"
      pattern: "edit_file.*glob_search.*grep_search"
---

<objective>
Wire the shared-seam foundation for three new tools (edit_file, glob_search, grep_search) through all five cross-cutting files without yet implementing the actual filesystem operations. After this plan, the project compiles and schema-validates the 8-action enum, but the three new tool dispatches hit `failwith "not implemented"` stubs in FsToolExecutor. Plan 08-02 fills the stubs.

Purpose: Avoid partial-seam commits where Domain.fs has 3 new DU cases but Json.fs schema still rejects them (or vice versa). By batching all shared-seam edits in one atomic plan, every downstream executor task in plan 08-02 starts from a compiling, schema-consistent baseline and only needs to edit one impl function + one match arm per tool.

Output:
- 3 new Tool DU cases in Domain.fs
- 3 new dispatchTool arms in AgentLoop.fs
- llmStepSchema enum: 5 → 8 values
- defaultSystemPrompt: describes 8 actions (was 5)
- FsToolExecutor.create match block: 4 real impls + 3 `failwith` stubs
- 2 existing tests updated (LlmPipelineTests "all 5 valid action enum values" → 8; CompositionRootTests "SystemPrompt mentions all 5 actions" → 8)
- All 218 existing tests still pass; 3 new stubs are reachable only if the agent actually tries to use the new tools (tests don't exercise them yet — that's 08-02's job)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-tool-expansion/08-RESEARCH.md
@CLAUDE.md

# Existing codebase reference (read these before editing — they define the patterns to match)
@src/BlueCode.Core/Domain.fs
@src/BlueCode.Core/AgentLoop.fs
@src/BlueCode.Cli/Adapters/Json.fs
@src/BlueCode.Cli/CompositionRoot.fs
@src/BlueCode.Cli/Adapters/FsToolExecutor.fs
@tests/BlueCode.Tests/LlmPipelineTests.fs
@tests/BlueCode.Tests/CompositionRootTests.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Tool DU + dispatchTool + schema enum (Core + Json.fs changes)</name>
  <files>
    src/BlueCode.Core/Domain.fs
    src/BlueCode.Core/AgentLoop.fs
    src/BlueCode.Cli/Adapters/Json.fs
  </files>
  <action>
    This task edits the Core domain and the JSON schema. Do NOT edit FsToolExecutor or CompositionRoot in this task — those are Task 2.

    **Subtask 1a: Domain.fs (Core, no Serilog/Spectre/Argu refs allowed)**
    In `src/BlueCode.Core/Domain.fs`, locate the `type Tool` DU (currently has 4 cases: ReadFile, WriteFile, ListDir, RunShell). Add three new cases AFTER `RunShell`:

    ```fsharp
    | EditFile of FilePath * oldString: string * newString: string
    | GlobSearch of pattern: string * searchPath: FilePath option
    | GrepSearch of pattern: string * searchPath: FilePath option * fileGlob: string option
    ```

    Rationale per 08-RESEARCH.md Pattern 1: `oldString`/`newString`/`pattern` are plain strings (not single-case DUs) because they have no project-root validation semantics. `FilePath` wraps the optional search root for consistency with existing tools.

    Constraint: Core purity. No `open Serilog`/`open Spectre.Console`/`open Argu` — if you were about to add one, you're in the wrong layer.

    **Subtask 1b: AgentLoop.fs dispatchTool arms**
    In `src/BlueCode.Core/AgentLoop.fs`, locate the `dispatchTool` function's `match actionName with` block. After the `"run_shell" ->` arm and BEFORE `| other -> Error(UnknownTool(ToolName other))`, add three new arms using the existing `result {}` CE and `requireStr`/`tryStr` helpers:

    ```fsharp
    | "edit_file" ->
        result {
            let! path   = requireStr "path"
            let! oldStr = requireStr "old_string"
            let! newStr = requireStr "new_string"
            return EditFile(FilePath path, oldStr, newStr)
        }
    | "glob_search" ->
        result {
            let! pattern = requireStr "pattern"
            let searchPath = tryStr "path" |> Option.map FilePath
            return GlobSearch(pattern, searchPath)
        }
    | "grep_search" ->
        result {
            let! pattern = requireStr "pattern"
            let searchPath = tryStr "path" |> Option.map FilePath
            let fileGlob   = tryStr "file_glob"
            return GrepSearch(pattern, searchPath, fileGlob)
        }
    ```

    **Subtask 1c: Json.fs llmStepSchema enum extension**
    In `src/BlueCode.Cli/Adapters/Json.fs`, locate the `llmStepSchema` constant and its `"enum"` array (currently 5 values). Replace the enum line with the 8-value version:

    ```json
    "enum": ["read_file", "write_file", "list_dir", "run_shell", "edit_file", "glob_search", "grep_search", "final"]
    ```

    No other changes to the schema (no oneOf/discriminator — per-action input shape is still validated in dispatchTool, consistent with existing design). Do NOT change `additionalProperties: false` or any other schema rule.

    **Verify compile:**
    Before committing, run `dotnet build src/BlueCode.Core/BlueCode.Core.fsproj` and `dotnet build src/BlueCode.Cli/BlueCode.Cli.fsproj`. Expected result: BlueCode.Cli FAILS to build with an exhaustive-match warning or error on `FsToolExecutor.create` because the match block doesn't cover `EditFile | GlobSearch | GrepSearch`. This is expected and will be fixed in Task 2 — DO NOT attempt to fix it here.

    If Core itself fails to build, there's a mistake in Subtask 1a or 1b — fix before proceeding.

    **Commit:** `feat(08-01): extend Tool DU + dispatchTool + schema enum for edit_file, glob_search, grep_search`
    Stage only the three files you edited in this task.
  </action>
  <verify>
    - `grep -c "EditFile\|GlobSearch\|GrepSearch" src/BlueCode.Core/Domain.fs` returns 3 (one per DU case)
    - `grep -E "\"edit_file\"|\"glob_search\"|\"grep_search\"" src/BlueCode.Core/AgentLoop.fs | wc -l` returns 3
    - `grep -E "\"edit_file\", \"glob_search\", \"grep_search\"" src/BlueCode.Cli/Adapters/Json.fs` finds at least one match (in the enum line)
    - `dotnet build src/BlueCode.Core/BlueCode.Core.fsproj` succeeds
    - `grep -n "open Serilog\|open Spectre\|open Argu" src/BlueCode.Core/Domain.fs src/BlueCode.Core/AgentLoop.fs` returns nothing (Core purity)
    - `grep -n "async {" src/BlueCode.Core/Domain.fs src/BlueCode.Core/AgentLoop.fs` returns only references that existed before (or none) — don't introduce async {} in Core (CLAUDE.md invariant)
  </verify>
  <done>
    Three tool DU cases added to Domain.fs. Three dispatchTool arms parse JSON input into those DU cases. Json.fs llmStepSchema enum accepts the 8-value set. BlueCode.Core builds clean. Cli build fails on exhaustive-match (expected — Task 2 fixes it). Commit `feat(08-01): extend Tool DU + dispatchTool + schema enum for edit_file, glob_search, grep_search` exists with only the three files staged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stub FsToolExecutor match arms + update system prompt + update existing 5-value tests</name>
  <files>
    src/BlueCode.Cli/Adapters/FsToolExecutor.fs
    src/BlueCode.Cli/CompositionRoot.fs
    tests/BlueCode.Tests/LlmPipelineTests.fs
    tests/BlueCode.Tests/CompositionRootTests.fs
  </files>
  <action>
    This task closes the shared seam: makes Cli compile by adding stub match arms, extends the system prompt to describe the 3 new tools, and updates the two tests that hardcode the 5-value enum so the existing 218-test suite continues to pass.

    **Subtask 2a: FsToolExecutor.create — add 3 stub match arms**
    In `src/BlueCode.Cli/Adapters/FsToolExecutor.fs`, locate the `create` function's match block (currently has 4 arms for ReadFile/WriteFile/ListDir/RunShell). Add three new arms AFTER the RunShell arm, each a `failwith` stub with a clear message that plan 08-02 fills:

    ```fsharp
    | EditFile(FilePath _, _, _) ->
        task { return failwith "EditFile impl not yet wired (plan 08-02)" }
    | GlobSearch(_, _) ->
        task { return failwith "GlobSearch impl not yet wired (plan 08-02)" }
    | GrepSearch(_, _, _) ->
        task { return failwith "GrepSearch impl not yet wired (plan 08-02)" }
    ```

    Return type must match the other arms: `Task<Result<ToolResult, AgentError>>`. The `failwith` inside `task { ... }` satisfies the type checker because `failwith` returns `'a`.

    Why stubs (not real impls): plan 08-02 implements all three. Stubs prevent build failures here and give 08-02 a clean insertion point.

    **Subtask 2b: CompositionRoot.fs — extend defaultSystemPrompt**
    In `src/BlueCode.Cli/CompositionRoot.fs`, locate `defaultSystemPrompt`. The current action list mentions 5 actions:
    `"<one of: read_file | write_file | list_dir | run_shell | final>"`

    Change it to 8:
    `"<one of: read_file | write_file | list_dir | run_shell | edit_file | glob_search | grep_search | final>"`

    Then add three input-schema lines inside the "Action input schemas:" block, placed after the `run_shell` line and before `final`:

    ```
    - edit_file:   {"path": "<rel-path>", "old_string": "<exact-unique-match>", "new_string": "<replacement>"}
    - glob_search: {"pattern": "<glob like src/**/*.fs>", "path": "<rel-path?>"}
    - grep_search: {"pattern": "<regex or fixed-string>", "path": "<rel-path?>", "file_glob": "<*.ext?>"}
    ```

    Keep existing "Rules:" section unchanged (one tool per response, no chaining, final to stop, no markdown/prose).

    **Subtask 2c: Update LlmPipelineTests.fs — 5 → 8 action enum test**
    In `tests/BlueCode.Tests/LlmPipelineTests.fs` near line 125, find the `testCase "all 5 valid action enum values accepted"` case. Update it to:
    - Rename to `testCase "all 8 valid action enum values accepted"`
    - Replace the `[ "read_file"; "write_file"; "list_dir"; "run_shell"; "final" ]` list with `[ "read_file"; "write_file"; "list_dir"; "run_shell"; "edit_file"; "glob_search"; "grep_search"; "final" ]`

    Also check lines ~75-77 where a comment says "5 valid action values" — update the comment to "8 valid action values" so future readers aren't misled. Do NOT change any other test logic.

    **Subtask 2d: Update CompositionRootTests.fs — 5 → 8 actions in SystemPrompt check**
    In `tests/BlueCode.Tests/CompositionRootTests.fs` at line 33, find `testCase "bootstrap SystemPrompt mentions all 5 actions"`. Update to:
    - Rename to `testCase "bootstrap SystemPrompt mentions all 8 actions"`
    - Replace `[ "read_file"; "write_file"; "list_dir"; "run_shell"; "final" ]` with `[ "read_file"; "write_file"; "list_dir"; "run_shell"; "edit_file"; "glob_search"; "grep_search"; "final" ]`

    **Verify full build + test run:**
    Run from project root:
    ```
    dotnet build
    dotnet test tests/BlueCode.Tests/BlueCode.Tests.fsproj
    ```
    Expected: builds succeed, all 218 existing tests pass (now covering 8-action schema/prompt). Zero new tests added yet (plan 08-02 adds those).

    If any test fails that you didn't modify, investigate before committing — you may have accidentally broken something in Task 1's dispatchTool arms.

    **Commit:** `feat(08-01): close shared seam with FsToolExecutor stubs, 8-action system prompt, 5→8 enum test updates`
    Stage only the four files edited in this task.

    **Also commit plan metadata separately** (per CLAUDE.md commit protocol):
    After committing code, if PLAN.md or SUMMARY.md was edited, stage and commit them with `docs(08-01): complete shared-seam plan` — stages only `.planning/phases/08-tool-expansion/08-01-PLAN.md` and the new `08-01-SUMMARY.md`.
  </action>
  <verify>
    - `dotnet build` succeeds (all projects: Core + Cli + Tests)
    - `dotnet test tests/BlueCode.Tests/BlueCode.Tests.fsproj` reports 218 passed, 0 failed, 1 ignored (v1.1 baseline)
    - `grep -n "all 8 valid action" tests/BlueCode.Tests/LlmPipelineTests.fs` finds the updated test case
    - `grep -n "all 8 actions" tests/BlueCode.Tests/CompositionRootTests.fs` finds the updated test case
    - `grep -c "edit_file\|glob_search\|grep_search" src/BlueCode.Cli/CompositionRoot.fs` returns at least 4 (one in the action-enum line + one per input-schema line)
    - `grep -c "EditFile\|GlobSearch\|GrepSearch" src/BlueCode.Cli/Adapters/FsToolExecutor.fs` returns 3 (the stub match arms)
    - `grep -n "not yet wired (plan 08-02)" src/BlueCode.Cli/Adapters/FsToolExecutor.fs` finds the stubs — they remain visible as reminders for plan 08-02
  </verify>
  <done>
    Cli project builds. All 218 v1.1 tests still pass. FsToolExecutor.create has 7 match arms (4 real impls + 3 stubs). System prompt advertises 8 actions. Two existing tests updated from 5 to 8 enum values. Commits exist: `feat(08-01): close shared seam with FsToolExecutor stubs, 8-action system prompt, 5→8 enum test updates` (code) and `docs(08-01): complete shared-seam plan` (plan metadata).
  </done>
</task>

</tasks>

<verification>
Phase 8 Success Criteria addressed by this plan (see ROADMAP.md Phase 8):

- **SC-1 "grep -c EditFile|GlobSearch|GrepSearch Domain.fs = 3"** — satisfied by Task 1a
- **SC-3 "llmStepSchema accepts edit_file, glob_search, grep_search as valid action values"** — satisfied by Task 1c (schema enum) + Task 2c (test proves it)
- **SC-5 "all 218 v1.1 tests still pass"** — satisfied by Task 2's full test run after stubs + prompt + test updates
- **SC-2 "agent emits action: glob_search for find-like prompts"** — PARTIALLY enabled (schema accepts it; system prompt describes it). Live agent verification is a plan 08-02 + Phase 8 overall success-criteria check, not this plan's job.
- **SC-4 "edit_file 1-match replace / 0-match / N-match"** — NOT in this plan; implemented by plan 08-02.

Core purity invariant (CLAUDE.md):
- `grep -rn "llm-system" src/BlueCode.Core/` returns 0 (unchanged)
- `grep -rn "open Serilog\|open Spectre\|open Argu" src/BlueCode.Core/` returns 0 (unchanged)
- `scripts/check-no-async.sh` passes (no new `async {` literals in Core)

Test registration invariant:
- No new test modules added in this plan, so `rootTests` list and `.fsproj` Compile order don't need changes. Plan 08-02 is the one that adds new test lists — it must handle registration.
</verification>

<success_criteria>
- [ ] Domain.fs has 3 new Tool DU cases (EditFile, GlobSearch, GrepSearch) — verified by grep
- [ ] AgentLoop.fs dispatchTool has 3 new match arms — verified by grep
- [ ] Json.fs llmStepSchema enum: 8 values — verified by grep
- [ ] CompositionRoot.fs defaultSystemPrompt describes 8 actions and 3 new input schemas — verified by grep
- [ ] FsToolExecutor.create has 7 match arms (4 real + 3 `failwith` stubs) — verified by grep
- [ ] `dotnet build` succeeds (no exhaustive-match warnings about Tool DU)
- [ ] `dotnet test` reports all 218 v1.1 tests pass
- [ ] Two existing tests updated from 5 → 8 action enum coverage
- [ ] Core purity preserved: no Serilog/Spectre/Argu imports, no `async {}` literal in src/BlueCode.Core/
- [ ] Commits atomic per task (Task 1 = one commit, Task 2 = one commit + separate plan-metadata commit)
</success_criteria>

<output>
After completion, create `.planning/phases/08-tool-expansion/08-01-SUMMARY.md` per `./.claude/get-shit-done/templates/summary.md`. Include frontmatter with:
- `phase: 08-tool-expansion`
- `plan: 01`
- `subsystem: tools`
- `affects: [08-02]` (plan 08-02 fills the stubs introduced here)
- `requires: []` (no prior plan dependencies)
- `tech-stack.added: []` (no new NuGet packages)

Document the stub strategy explicitly in the SUMMARY so the 08-02 executor understands they're filling known placeholders.
</output>
