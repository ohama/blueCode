---
phase: 09.1-bench-follow-up-fixes
plan: 02
subsystem: core
tags: [fsharp, dispatcher, pattern-match, read_file, expecto, TOOL-08]

# Dependency graph
requires:
  - phase: 09.1-01
    provides: system-prompt clarifications (truncated hint + edit_file terminal hint)
  - phase: 09
    provides: read_file with metadata header + out-of-range branch in FsToolExecutor
provides:
  - Dispatcher (AgentLoop.dispatchTool) with default-window arms for partial line bounds
  - Production-trace TOOL-08-bench testCase validating T6 failure scenario end-to-end
affects:
  - 09.1-03 (bench validation — Fix 1 is the primary behavioral change under test)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern-match guard ordering: new arms inserted BETWEEN existing guarded arm and wildcard"
    - "Test body assertions anchored with \\n to avoid collision with header words"

key-files:
  created: []
  modified:
    - src/BlueCode.Core/AgentLoop.fs
    - tests/BlueCode.Tests/FileToolsTests.fs

key-decisions:
  - "Default window for start_line-only is 100 lines (Some(s, s+99)); chosen per bench-followup doc §2 rationale — covers typical context-inspection window without truncation for most files"
  - "end_line-only arm emits Some(1, e) — read from file start to the given end; symmetric with start_line-only behavior"
  - "Int32 overflow for pathological s+99 values falls through readFileImpl's invalid-range guard — acceptable per RESEARCH.md F#-note 3; no explicit cap added"
  - "No new test module created — new testCase lives inside existing readFileTests testList in FileToolsTests.fs (avoids dual-registration pitfall)"

patterns-established:
  - "Partial-bounds arms pattern: | Some s, None when s > 0 -> Some(s, s + 99) // comment documenting rationale"
  - "Body-content assertions in read_file tests use \\na\\n style (not bare letters) to avoid collision with header words like 'out-of-range', 'truncated', 'lines'"

# Metrics
duration: 5min
completed: 2026-04-25
---

# Phase 9.1 Plan 02: Dispatcher Default-Window Fix + TOOL-08-bench Fixture Summary

**Extended AgentLoop.dispatchTool lineRange match with two new arms — `Some(s, s+99)` for start_line-only and `Some(1, e)` for end_line-only — closing the TOOL-08 dispatcher bridge gap; production-trace TOOL-08-bench testCase validates the T6 failure scenario end-to-end (241 passed + 1 ignored).**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T16:04:00Z
- **Completed:** 2026-04-25T16:06:30Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments

- Fix 1 from `documentation/v1.2-bench-followup.md` §2 landed: `AgentLoop.dispatchTool` now emits `Some(2001, 2100)` for a `start_line=2001`-only input, causing `readFileImpl` out-of-range branch to fire and return the `[file: ..., lines 2001-2100 of 3, out-of-range]` header.
- Production-trace test `TOOL-08-bench` added inside existing `readFileTests` testList, asserting the full header contract and empty body for a 3-line file with `Some(2001, 2100)` request.
- Core purity invariants intact: no async{}, no Spectre/Serilog/Argu/HttpClient references, `grep -rn "llm-system" src/` = 0, `check-no-async.sh` exit 0.

## Task Commits

1. **Task 1: Extend dispatcher lineRange match with default-window arms** - (fix)
2. **Task 2: Add production-trace TOOL-08-bench testCase to readFileTests** - (test)

**Plan metadata:** (docs: complete dispatcher fix plan — in-progress)

## Final Shape of lineRange Match (AgentLoop.fs:69-74)

```fsharp
                let lineRange =
                    match startL, endL with
                    | Some s, Some e when s > 0 && e >= s -> Some(s, e)
                    | Some s, None when s > 0 -> Some(s, s + 99)   // 100-line default window; Int32 overflow falls through readFileImpl invalid-range guard (RESEARCH.md F#-note 3)
                    | None, Some e when e > 0 -> Some(1, e)        // bounded read from start
                    | _ -> None
```

Inline comments preserved verbatim per plan specification.

## Test Count

- **Baseline:** 240 passed, 1 ignored, 0 failed
- **After Task 2:** 241 passed, 1 ignored, 0 failed

**Expecto summary line (verbatim):**
```
EXPECTO! 241 tests run in 00:00:30.8495659 for all – 241 passed, 1 ignored, 0 failed, 0 errored. Success!
```

## Files Created/Modified

- `src/BlueCode.Core/AgentLoop.fs` — Added 2 new match arms (+2 insertions); no deletions (the existing `| _ -> None` wildcard remains)
- `tests/BlueCode.Tests/FileToolsTests.fs` — Added new `testCase "TOOL-08-bench: ..."` inside `readFileTests` testList (+24 insertions, 0 deletions)

## Registration Confirmation

- No new test module created.
- No `.fsproj` edit (`BlueCode.Tests.fsproj` unchanged).
- No `RouterTests.rootTests` edit (`RouterTests.fs` line 97 unchanged).
- `FileToolsTests.fs` was already registered at `BlueCode.Tests.fsproj:12` and `RouterTests.fs:97` — the new `testCase` rides on existing registration.

## Decisions Made

- Default 100-line window for `start_line`-only (matches `documentation/v1.2-bench-followup.md` §2 "Why 100" rationale — covers typical context-inspection window without truncation for most files, and the arithmetic `s + 99` maps naturally to the 1-indexed range convention).
- No explicit cap on `s + 99` for pathological values — Int32 overflow falls through `readFileImpl`'s invalid-range guard, which is safe by design (RESEARCH.md F#-note 3).
- `end_line`-only arm produces `Some(1, e)` — symmetric read from file start.

## Deviations from Plan

None — plan executed exactly as written. Both tasks completed with the verbatim code blocks from the plan. No unexpected build warnings, no API drift, no new dependencies.

## Issues Encountered

None. The existing `readFileImpl` out-of-range branch (Phase 9, `FsToolExecutor.fs`) was already correct; only the dispatcher's `lineRange` match needed extending.

## Next Phase Readiness

- Fix 1 (dispatcher bridge) is committed and tested. Plan 09.1-03 (bench validation) can proceed.
- Phase 9.1 behavioral correctness now depends on the 72B T6 re-bench passing; that is the subject of 09.1-03.
- No blockers.

---
*Phase: 09.1-bench-follow-up-fixes*
*Completed: 2026-04-25*
