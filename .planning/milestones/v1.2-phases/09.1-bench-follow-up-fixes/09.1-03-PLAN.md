---
phase: 09.1-bench-follow-up-fixes
plan: 03
type: execute
wave: 3
depends_on: ["09.1-01", "09.1-02"]
files_modified:
  - /tmp/bench-v1.2/run.sh
  - .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md
  - documentation/benchmark-32b-vs-72b.md
autonomous: true

must_haves:
  truths:
    - "/tmp/bench-v1.2/run.sh exposes a `v9_1` case that runs 10 targeted invocations: T6×32B×3, T6×72B×3, W1×32B×1, W2×32B×1, T1×32B×1, T5×72B×1."
    - "Live bench artifacts exist for all 10 invocations under /tmp/bench-v1.2-fixed/ (one .log + .meta per run)."
    - "T6 × 32B × 3 PASSES 3/3 (gate 3/3 — RESEARCH.md Open Q2 says 'no model-variance excuse' for 32B)."
    - "T6 × 72B × 3 PASSES at least 2/3 (gate ≥2/3; <2/3 escalates to a follow-up Fix 2 wording iteration in a future plan, not this one)."
    - "W1 × 32B step count drops from 4 → 3 (Fix 3 effect)."
    - "W2 × 32B step count drops from 4 → 3 (Fix 3 effect)."
    - "T1 × 32B and T5 × 72B canaries unchanged from v1.2 baseline (regression guard)."
    - "VALIDATION.md summarizes all 10 results against the predicted deltas in `documentation/v1.2-bench-followup.md` §4 and explicitly records halt-and-investigate decisions if any threshold is missed."
    - "If any 32B run exceeded ~90s without progress, `launchctl kickstart -k com.ohama.qwen32b.plist` was issued and the retry is documented (RESEARCH.md Open Q4)."
  artifacts:
    - path: /tmp/bench-v1.2/run.sh
      provides: "v9_1 case clause that runs the 10 targeted invocations"
      contains: "v9_1)"
    - path: .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md
      provides: "Per-run table mapping bench logs to predicted vs actual deltas plus halt/no-halt decision"
      min_lines: 30
    - path: /tmp/bench-v1.2-fixed/timeline.txt
      provides: "Concatenated runner timeline from the v9_1 invocation"
  key_links:
    - from: /tmp/bench-v1.2/run.sh
      to: /tmp/bench-v1.2-fixed/*.log
      via: "the run() helper writes a .log + .meta per labeled invocation"
      pattern: "/tmp/bench-v1\\.2-fixed/v9_1_"
    - from: .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md
      to: documentation/v1.2-bench-followup.md
      via: "VALIDATION.md cites §4 prediction table for each row"
      pattern: "v1\\.2-bench-followup\\.md.*§4"
---

<objective>
Validate Plans 09.1-01 + 09.1-02 with a live bench against `mlx_lm.server`'s 32B and 72B endpoints. This plan does NOT change any source code or system prompts — it (a) extends `/tmp/bench-v1.2/run.sh` with a precise 10-run selector, (b) executes the runs, (c) writes a structured VALIDATION.md mapping outcomes to the predicted deltas in `documentation/v1.2-bench-followup.md` §4.

Purpose: Bench evidence is the gate. Without it, the audit cannot return to `passed`, and v1.2 cannot close. The followup doc explicitly says "Don't re-run all 36" — this plan runs exactly the 10 invocations §4 specifies, plus regression canaries.

Output:
- 10 bench logs under `/tmp/bench-v1.2-fixed/` (one .log + .meta per labeled run, plus a `timeline.txt`).
- Updated `/tmp/bench-v1.2/run.sh` with a `v9_1` case (5-10 lines of bash; RESEARCH.md Pitfall 4 + Open Q1).
- A new `09.1-VALIDATION.md` summarizing per-run pass/fail and step-count deltas against predictions.
- Optional Part 4 append to `documentation/benchmark-32b-vs-72b.md` (only if all gates pass — recording the post-fix numbers as the new v1.2-fixed baseline).

Source-of-truth design: `documentation/v1.2-bench-followup.md` §4 (validation method, prediction table, halt-and-investigate triggers). Harness rationale: `09.1-RESEARCH.md` Pitfall 4 + Open Q1 + Open Q4 + "Bench validation invocation" (lines 318-330).

Gates (codified — RESEARCH.md Open Q2 makes the 32B vs 72B asymmetry explicit):
- 32B T6: 3/3 PASS required (no variance excuse).
- 72B T6: ≥2/3 PASS required; <2/3 triggers a follow-up Fix 2 wording iteration in a future plan (NOT this one — do not iterate prompt wording inside this plan; surface the gap and stop).
- W1 32B + W2 32B: each must drop one step (4→3) — Fix 3 working.
- T1 32B + T5 72B: must remain at the v1.2 baseline (T1 ~3s pass, T5 ~20s pass) — regression canaries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-bench-follow-up-fixes/09.1-RESEARCH.md
@documentation/v1.2-bench-followup.md
@documentation/benchmark-32b-vs-72b.md
@.planning/v1.2-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend run.sh with a v9_1 case for the 10 targeted invocations</name>
  <files>/tmp/bench-v1.2/run.sh</files>
  <action>
The existing `/tmp/bench-v1.2/run.sh` (96 lines, verified shape in RESEARCH.md "Bench validation invocation" lines 320-330) only takes `phase1|phaseA|phaseB|phaseC|all` arguments — it cannot directly run "T6 × 32B × 3" (RESEARCH.md Pitfall 4). Add a new case branch.

Insert a new function `v9_1()` BEFORE the final `case "${1:-all}"` block (around current line 87). The function must:

1. Echo a banner `######## PHASE 9.1: TOOL-08 fix validation (10 runs) ########` to `$LOG_DIR/timeline.txt` (use the existing `tee -a` pattern visible in `phaseA`).
2. Override `LOG_DIR=/tmp/bench-v1.2-fixed` for this function ONLY — so artifacts land separate from the prior baseline. Use `local LOG_DIR=/tmp/bench-v1.2-fixed` and `mkdir -p "$LOG_DIR"` at the top.
3. Run T6 × 32B × 3 with labels `v9_1_T6_32b_run1`, `_run2`, `_run3` using the T6 prompt (verified verbatim from `phase1()` line in HEAD: `"What are the field names in the Step record in src/BlueCode.Core/Domain.fs?"`).
4. Run T6 × 72B × 3 with labels `v9_1_T6_72b_run1`, `_run2`, `_run3` (same prompt, model `72b`).
5. Restore the W1 fixture content `cat > bench-fixtures/bug_lastchar.fs <<'EOF' … EOF` (copy from existing `phaseC()` body), then run W1 × 32B with label `v9_1_W1_32b` and prompt `"Read bench-fixtures/bug_lastchar.fs and fix the bug. Save the corrected version using write_file."`. Save the result fixture too: `cp bench-fixtures/bug_lastchar.fs "$LOG_DIR/v9_1_W1_32b_result.fs" 2>/dev/null || true`.
6. Restore the W2 fixture content `cat > bench-fixtures/bug_average.fs <<'EOF' … EOF` (copy from `phaseC()`), then run W2 × 32B with label `v9_1_W2_32b` and prompt `"Read bench-fixtures/bug_average.fs and add a new function averageSafe that returns int option (None for empty list). Save the updated file."`.
7. Run T1 × 32B canary with label `v9_1_T1_32b` and prompt `"What is 2 to the power of 10? Answer with just the number."`.
8. Run T5 × 72B canary with label `v9_1_T5_72b` and prompt `"Find BlueCode.slnx and tell me its size in bytes using wc."`.
9. Add `v9_1) v9_1 ;;` as a new arm of the trailing `case "${1:-all}"` block, BEFORE the `*)` usage line. Update the usage string to `"usage: $0 [phase1|phaseA|phaseB|phaseC|v9_1|all]"`.

Total: 10 labeled invocations, all matching the followup doc §4 prediction table row labels.

DO NOT touch the existing `run()`, `phase1()`, `phaseA()`, `phaseB()`, `phaseC()` functions — extending them would muddy the validation signal.

This file lives at `/tmp/bench-v1.2/run.sh` — outside the git tree by design (per CLAUDE.md, `.claude/` and `localLLM/` and the `/tmp` artifact area are not committed). NO commit needed for this task. Verification is by `bash -n` (syntax-check) plus a dry-run `case unknown_arg) printout`.
  </action>
  <verify>
1. `bash -n /tmp/bench-v1.2/run.sh` — exits 0 (syntax valid).
2. `grep -c "v9_1_T6_32b_run" /tmp/bench-v1.2/run.sh` — outputs `3` (three label substitutions in the bash for-loop).
3. `bash /tmp/bench-v1.2/run.sh nonsense_arg 2>&1 | head -1` — prints `usage: ... [phase1|phaseA|phaseB|phaseC|v9_1|all]` and exits 1 (the new selector is in the usage line).
4. `grep -c "^v9_1)" /tmp/bench-v1.2/run.sh` — outputs `1` (case arm added).
  </verify>
  <done>
- run.sh is syntactically valid bash.
- New `v9_1()` function present, with all 10 labeled invocations.
- New case arm dispatches to it.
- Usage line updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute v9_1 bench runs and capture artifacts</name>
  <files>/tmp/bench-v1.2-fixed/*.log, /tmp/bench-v1.2-fixed/*.meta, /tmp/bench-v1.2-fixed/timeline.txt</files>
  <action>
Pre-flight checks BEFORE invoking the runner:

1. Verify both servers are responsive:
   - `curl -s http://localhost:8000/v1/models | jq -r '.data[].id' | head -2` — should list local 32B path or HF id.
   - `curl -s http://localhost:8001/v1/models | jq -r '.data[].id' | head -2` — should list local 72B path or HF id.
   - If either fails: `tail -50 ~/llm-system/services/logs/{32b,72b}.err` to triage. Common failure: `[METAL] Insufficient Memory` after long sessions — `launchctl unload && launchctl load -w` per CLAUDE.md "Common Gotchas" "Port 48 Address already in use" guidance.
2. Verify Plans 09.1-01 + 09.1-02 are committed (depends_on guarantees this, but double-check):
   - `git log --oneline -5` — top commits should reference `09.1-01` (system-prompt fixes) and `09.1-02` (dispatcher fix).
3. Build once cleanly so the bench is testing the new bits:
   - `dotnet build BlueCode.slnx -c Release` (or `Debug`; the existing bench uses `dotnet run` which is Debug — match that). Confirm zero new warnings.

Execute:

```bash
mkdir -p /tmp/bench-v1.2-fixed
bash /tmp/bench-v1.2/run.sh v9_1
```

Wall-clock budget: ~3-4 minutes per the followup doc §4. Watch for stalls.

Hang contingency (RESEARCH.md Open Q4 + CLAUDE.md "Common Gotchas"):
- If any 32B run exceeds ~90s without producing log lines, kill that invocation (Ctrl-C the running `dotnet run` process), then `launchctl kickstart -k com.ohama.qwen32b.plist`, wait ~30s for the server to reload weights (`tail -f ~/llm-system/services/logs/32b.out` showing `Application startup complete`), then re-invoke ONLY the failed label using:
  ```bash
  cd /Users/ohama/projs/blueCode
  dotnet run --project src/BlueCode.Cli -- --verbose --model 32b "<the prompt>" 2>&1 | tee /tmp/bench-v1.2-fixed/v9_1_<label>.log
  ```
- Document any retry in the VALIDATION.md (Task 3) under a "Retries" subsection.

Post-run sanity check:
- `ls /tmp/bench-v1.2-fixed/v9_1_*.log | wc -l` — must equal 10 (3 + 3 + 1 + 1 + 1 + 1).
- `tail -3 /tmp/bench-v1.2-fixed/timeline.txt` — should show `===== RUN COMPLETE =====`.

DO NOT iterate on Fix 2 wording inside this task. If 72B T6 < 2/3, surface the failure in VALIDATION.md (Task 3) and stop — followup-iteration is a separate plan, not in scope here.

NO commit on `/tmp/bench-v1.2-fixed/` artifacts — they are intentionally outside the git tree per CLAUDE.md (`.claude/` and `localLLM/` untracked; `/tmp/` is not in the repo at all).
  </action>
  <verify>
1. `ls /tmp/bench-v1.2-fixed/v9_1_*.log | wc -l` — outputs `10` (or higher if retries; document in VALIDATION.md).
2. Each `.log` contains a `final` action JSON or a clear failure indicator (`MaxLoopsExceeded`, `LoopGuard`, `InvalidJsonOutput`, etc.). Spot-check via `grep -l "MaxLoopsExceeded\|LoopGuard\|InvalidJsonOutput" /tmp/bench-v1.2-fixed/v9_1_*.log` — log paths printed are FAIL candidates; absence of T6 logs in this list is positive.
3. `cat /tmp/bench-v1.2-fixed/v9_1_T6_32b_run1.log | grep -c "out-of-range"` — non-zero (the new dispatcher path means T6 32B should now hit the out-of-range header at least once during the loop).
  </verify>
  <done>
- 10 logs + 10 metas + timeline.txt under `/tmp/bench-v1.2-fixed/`.
- No raw stalls remain (any 90s+ hang was kickstarted and retried).
- Logs are inspectable and contain final-or-failure markers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author 09.1-VALIDATION.md summarizing results vs predictions</name>
  <files>.planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md, documentation/benchmark-32b-vs-72b.md</files>
  <action>
Create `.planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` with the following structure (Markdown, no frontmatter required):

1. **Header section**: title, date, runner version (`run.sh v9_1` case), source-of-truth links (`documentation/v1.2-bench-followup.md` §4, `09.1-RESEARCH.md` Open Q2/Q4).
2. **Pre-fix vs predicted vs actual table** — one row per invocation. Columns: `Test`, `Model`, `Run #`, `Pre-fix result`, `Predicted post-fix`, `Actual post-fix`, `Status (PASS/FAIL/CANARY-OK)`. Source the pre-fix and predicted columns directly from `documentation/v1.2-bench-followup.md` §4 lines 122-129. Source the actual column from the `.log` files.
3. **Step-count deltas for W1/W2** — explicit count of dispatched tools per run (grep for `action` lines in the log, or count `Step ` markers; whichever the existing log format provides). Predicted: 4 → 3 each. Required: PASS only if step count strictly decreased.
4. **Gate evaluation block**:
   - 32B T6: gate 3/3. Pass/Fail decision.
   - 72B T6: gate ≥2/3. Pass/Fail decision. If <2/3, surface the wording-iteration referral.
   - W1/W2 32B: gate "step count decreased by exactly 1". Pass/Fail decision.
   - T1 32B + T5 72B canaries: gate "no regression vs v1.2 baseline" (compare elapsed-time and exit code to `phase1()` baseline in `/tmp/bench-v1.2/p1_T1_32b.meta` and `p1_T5_72b.meta`).
5. **Halt-and-investigate triggers fired** — explicit subsection per `documentation/v1.2-bench-followup.md` §4 lines 131-134:
   - "T6 deltas don't materialize → dispatcher math wrong; re-derive (s, s+99) boundaries" — triggered? Y/N.
   - "T1 or T5 changes → unintended interaction; revert and reconsider" — triggered? Y/N.
   - "T6 32B passes but 72B doesn't → Fix 2 prompt wording insufficient; iterate" — triggered? Y/N.
6. **Retries section** (only if Task 2 needed kickstarts): label, retry count, root cause if known.
7. **Conclusion**: "All gates green — Phase 9.1 ready for `/gsd:verify-work`" OR "Gates X/Y not met — escalate Z".

The decision-rule on the 72B T6 gate is REQUIRED to be explicit in this document (RESEARCH.md Open Q2; user request to codify): 72B T6 PASS = ≥2/3; FAIL = <2/3 → triggers a follow-up Plan 09.1-04 to iterate Fix 2 wording (do NOT create that plan as part of this task; just reference it).

Optionally (only if all gates green): append a "Part 4 — v1.2-fixed validation re-bench" subsection to `documentation/benchmark-32b-vs-72b.md` summarizing the post-fix numbers as the new v1.2-fixed baseline. Cite the VALIDATION.md and `/tmp/bench-v1.2-fixed/` log paths. Keep it short (~30 lines).

Commit messages:
- Task 3a (VALIDATION.md): `docs(09.1-03): add 09.1-VALIDATION.md bench results` per CLAUDE.md plan-metadata commit pattern.
- Task 3b (benchmark doc append, if applicable): `docs(09.1-03): append Part 4 v1.2-fixed bench summary` (separate atomic commit).
  </action>
  <verify>
1. `test -f .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — exists.
2. `wc -l .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — outputs at least 30 lines (must_haves min).
3. `grep -c "32B T6" .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — at least 1.
4. `grep -c "72B T6" .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — at least 1.
5. `grep -c "Halt-and-investigate" .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — at least 1.
6. `grep -c "v9_1_T6_32b_run" .planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` — at least 3 (one citation per 32B T6 run).
  </verify>
  <done>
- VALIDATION.md exists with all required sections.
- Each gate has an explicit Pass/Fail verdict citing log evidence.
- 72B T6 gate ≥2/3 codified explicitly (per user instruction in planner brief).
- All halt-triggers from `documentation/v1.2-bench-followup.md` §4 evaluated, even if "not triggered".
- Retries documented if any (RESEARCH.md Open Q4).
- Atomic commit lands.
  </done>
</task>

</tasks>

<verification>
Phase-level checks:
- [ ] 10 logs under `/tmp/bench-v1.2-fixed/v9_1_*.log` exist and are non-empty.
- [ ] 32B T6 gate: 3/3 PASS (per the explicit gate codified in must_haves and Task 3 acceptance).
- [ ] 72B T6 gate: ≥2/3 PASS (gate <2/3 surfaces follow-up referral; do NOT close phase under that condition without verifier sign-off).
- [ ] W1 32B step count strictly less than v1.2 baseline (4 → expected 3).
- [ ] W2 32B step count strictly less than v1.2 baseline (4 → expected 3).
- [ ] T1 32B + T5 72B canaries unchanged vs `/tmp/bench-v1.2/p1_T1_32b.meta` and `p1_T5_72b.meta` (exit 0, elapsed within ±50% of baseline).
- [ ] VALIDATION.md present and committed.
- [ ] No source-code change committed in this plan (validation only — code changes belong to Plans 09.1-01 and 09.1-02).
- [ ] If kickstart was required, retry is logged.
</verification>

<success_criteria>
- All 10 invocations completed (with documented retries if needed).
- All gates evaluated and recorded in VALIDATION.md with explicit Pass/Fail verdicts.
- `/tmp/bench-v1.2/run.sh` is reusable for any future targeted re-bench (`bash /tmp/bench-v1.2/run.sh v9_1`).
- Atomic commit(s) for VALIDATION.md (and optional benchmark doc Part 4 append) per CLAUDE.md.
- Phase 9.1 ready for `/gsd:verify-work 9.1` if all gates green.
</success_criteria>

<rollback>
This plan does NOT modify source code or system prompts — there is nothing to roll back at the code level. The bash function in `/tmp/bench-v1.2/run.sh` is outside the git tree; if it has a bug, edit-in-place is fine.

If gate failures surface a real regression in Plans 09.1-01 or 09.1-02:
- DO NOT auto-revert those plans from inside this plan. The verifier (downstream `/gsd:verify-work 9.1`) decides escalation.
- Surface the failure in VALIDATION.md with concrete log evidence and `git log --oneline -10` showing the offending commit hashes. Stop. The orchestrator will route to a 09.1-04 follow-up plan.

If 32B server hangs persisted after one kickstart retry (RESEARCH.md Open Q4):
- Document the hang in VALIDATION.md and stop. Two consecutive hangs on the same label suggests a stress-state (memory pressure, prompt cache bloat) that warrants a fresh `launchctl unload && launchctl load -w` of the launchd plist, then a single re-attempt — but only after coordinating with the orchestrator.
</rollback>

<output>
After completion, create `.planning/phases/09.1-bench-follow-up-fixes/09.1-03-SUMMARY.md` documenting:
- Test invocations and their labels.
- Per-gate verdict (32B T6, 72B T6, W1, W2, canaries).
- Overall phase 9.1 verdict (READY for verify-work / ESCALATE to 09.1-04).
- Path references to all 10 .log + .meta files and the VALIDATION.md.
- Confirmation that no source code or test files were modified by this plan.
</output>
