---
phase: 09.1-bench-follow-up-fixes
verified: 2026-04-26T02:40:00Z
status: passed
score: 5/5 must-haves verified
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "SC4 W1 × 32B step count 4→3 PASS (closed by 09.1-05 code-level loop-injection)"
  gaps_remaining: []
  regressions: []
must_haves:
  - sc: 1
    status: pass
    evidence: "AgentLoop.fs:72 `| Some s, None when s > 0 -> Some(s, s + 99)` and :73 `| None, Some e when e > 0 -> Some(1, e)`, both between the full-range arm (:71) and the wildcard (:74). Test at FileToolsTests.fs:237 passes in 242-test run."
  - sc: 2
    status: pass
    evidence: "CompositionRoot.fs:60 — verbatim string present: `If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.`"
  - sc: 3
    status: pass
    evidence: "CompositionRoot.fs:66-67 — strengthened directive present: `edit_file is the ONLY action needed to save changes. NEVER call write_file after edit_file.` and `edit_file's success result already confirms the change; do NOT verify with a follow-up read_file.` Old soft sentence (`edit_file persists the change...`) is gone (grep-count = 0). Strengthened wording exceeds original SC3 text and better delivers SC3 intent."
  - sc: 4
    status: pass
    evidence: "09.1-VALIDATION.md re-revalidation gate table (§Plan 09.1-05 section): W1 32B 4→3 PASS, W2 32B 3→3 PASS, T6 32B 3/3 PASS (from 09.1-03, unchanged), T6 72B 3/3 PASS (from 09.1-03, unchanged), T1 32B CANARY-OK (1 step, answer=1024), T5 72B CANARY-OK (unchanged). 16 log files across three runs: 10 at /tmp/bench-v1.2-fixed/, 3 at /tmp/bench-v1.2-fixed-rev/, 3 at /tmp/bench-v1.2-fixed-rev2/. W1 log at /tmp/bench-v1.2-fixed-rev2/v9_1_rev2_W1_32b.log confirms 3-step sequence: read_file → edit_file → final; thought at step 3: `The bug has been fixed and the change is confirmed. No further action is needed on this file.` (contrast with prior `Now, I will save the corrected version`)."
  - sc: 5
    status: pass
    evidence: "Test run: 242 passed, 1 ignored, 0 failed (live run 2026-04-26, 00:00:30.8s). New test at AgentLoopTests.fs:165 (`post-edit_file injection: second LLM call sees [POST-EDIT CONSTRAINT] with edited path`) added by commit 065be91. git diff 5fbb940..HEAD -- src/BlueCode.Core/ shows only AgentLoop.fs changed (no Domain.fs, no other Core file). check-no-async.sh exits 0. grep -rn llm-system src/ = 0 lines. grep -c POST-EDIT CONSTRAINT AgentLoop.fs = 1."
gaps: []
human_verification: []
---

# Phase 9.1: Bench Follow-up Fixes Verification Report

**Phase Goal:** Close the three tech-debt items from the v1.2 milestone audit (revised verdict `tech_debt` after 36-run live re-bench) so TOOL-08 actually delivers its motivating value and v1.1 baselines are recovered before milestone archive.
**Verified:** 2026-04-26T02:40:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (09.1-05 code-level loop-injection closed SC4 W1 gate)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC1 | Dispatcher produces non-None lineRange for partial bounds | VERIFIED | AgentLoop.fs:72-73; test at FileToolsTests.fs:237; 242/1/0 test run |
| SC2 | System prompt `read_file` block adds `truncated` strategy line | VERIFIED | CompositionRoot.fs:60 — verbatim string confirmed |
| SC3 | System prompt `edit_file` block declares action terminal (strengthened) | VERIFIED | CompositionRoot.fs:66-67 — strengthened two-sentence directive present; old soft sentence absent |
| SC4 | Live bench: T6 gates pass, W1/W2 drop one step, canaries stable | VERIFIED | All 6 gates PASS/CANARY-OK per 09.1-VALIDATION.md re-revalidation section; W1 log directly confirms 3-step trace |
| SC5 | 242 tests pass, Core diff confined to AgentLoop.fs, no async, no llm-system | VERIFIED | 242/1/0 live; only AgentLoop.fs in Core diff; async check exits 0; 0 llm-system refs; POST-EDIT CONSTRAINT count = 1 |

**Score:** 5/5 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/BlueCode.Core/AgentLoop.fs` | Four-arm dispatcher + loop-injection via lastEditPath | VERIFIED | Lines 69-74: full-range, start-only, end-only, wildcard. Lines 195-247: buildMessages with lastEditPath param. Lines 340-347: lastEditPath' tracking in runLoop. |
| `src/BlueCode.Cli/CompositionRoot.fs` | `truncated` strategy hint + strengthened `edit_file` directive | VERIFIED | Line 60: truncated hint verbatim. Lines 66-67: strengthened directive (ONLY/NEVER/do NOT verify). |
| `tests/BlueCode.Tests/FileToolsTests.fs` | TOOL-08-bench unit test | VERIFIED | Line 237: `testCase "TOOL-08-bench: dispatcher Some(s, s+99) output triggers out-of-range header (T6 production trace)"` |
| `tests/BlueCode.Tests/AgentLoopTests.fs` | POST-EDIT CONSTRAINT injection test | VERIFIED | Line 165: `testCaseAsync "post-edit_file injection: second LLM call sees [POST-EDIT CONSTRAINT] with edited path"` — asserts constraint text in second LLM call, asserts absent from first call |
| `.planning/phases/09.1-bench-follow-up-fixes/09.1-VALIDATION.md` | Bench results with re-revalidation gate table | VERIFIED | Contains three sections: 09.1-03 run, 09.1-04 re-validation, 09.1-05 re-revalidation; all gates evaluated with verdicts and trigger analysis |
| `/tmp/bench-v1.2-fixed*/` | Bench log files (10 + 3 + 3 = 16 total) | VERIFIED | 10 at `/tmp/bench-v1.2-fixed/v9_1_*.log`, 3 at `/tmp/bench-v1.2-fixed-rev/v9_1_rev_*.log`, 3 at `/tmp/bench-v1.2-fixed-rev2/v9_1_rev2_*.log` |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `runLoop` (AgentLoop.fs) | `buildMessages` | `lastEditPath` parameter | WIRED | `lastEditPath'` computed from `EditFile(FilePath p, _, _), Success _` match; threaded through recursive `runLoop` call and initial `runSession` call (None). |
| `buildMessages` | POST-EDIT CONSTRAINT message | `System`-role message appended after user turn | WIRED | `baseMsgs @ [ constraintMsg ]` when `lastEditPath = Some path`. Message references path three times: do NOT write_file, do NOT read_file to verify, edit is persisted. |
| System prompt `edit_file` block | LLM behavior (W2) | Directive wording | WIRED (behavioral) | W2 bench: model skips verification read_file. W1 additionally requires loop-injection because user prompt names `write_file` explicitly. |
| Loop-injection constraint | LLM behavior (W1) | Post-user-turn System message overrides user instruction | WIRED (behavioral) | W1 bench log confirms thought shift: old `"Now, I will save the corrected version"` → new `"No further action is needed on this file."` |

---

## SC1: Dispatcher Arms — Detail

`src/BlueCode.Core/AgentLoop.fs` lines 69-74:

```fsharp
let lineRange =
    match startL, endL with
    | Some s, Some e when s > 0 && e >= s -> Some(s, e)         // line 71: existing full-range arm
    | Some s, None when s > 0 -> Some(s, s + 99)                // line 72: NEW — 100-line default window
    | None, Some e when e > 0 -> Some(1, e)                     // line 73: NEW — bounded read from start
    | _ -> None                                                  // line 74: wildcard
```

F# match arms are evaluated top-to-bottom. Both new arms sit between the full-range arm and the wildcard. Ordering is correct. The unit test at FileToolsTests.fs:237 passes `Some(2001, 2100)` to `FsToolExecutor` and asserts the header `[file: small.fs, lines 2001-2100 of 3, out-of-range]` with empty body. Passed in the live 242-test run.

---

## SC2: Truncated Strategy Hint — Detail

`src/BlueCode.Cli/CompositionRoot.fs` line 60 (inside the `read_file` schema block of `defaultSystemPrompt`):

```
If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.
```

Verbatim match confirmed. VALIDATION.md confirms behavioral effectiveness: T6 72B switched from full-file reads (truncated) to narrow paired windows and found the Step record on all 3 runs.

---

## SC3: Edit_file Terminal Declaration — Detail (Strengthened)

`src/BlueCode.Cli/CompositionRoot.fs` lines 66-67 (inside the `edit_file` schema block):

```
edit_file is the ONLY action needed to save changes. NEVER call write_file after edit_file.
edit_file's success result already confirms the change; do NOT verify with a follow-up read_file.
```

Both lines present. Old soft sentence (`edit_file persists the change. After a successful edit_file, do NOT call write_file with the same path.`) is absent — `grep -c "edit_file persists the change" CompositionRoot.fs` = 0.

The strengthened wording (from 09.1-05 Task 2, commit `2d409f1`) was directly effective for W2: bench evidence shows no verification read_file in step 3 of the W2 trace. W1 additionally required the loop-injection (SC5 mechanism) because the user prompt named `write_file` explicitly, which the loop-injection overrides by position in the message list (post-user-turn System message).

SC3 is VERIFIED with evidence exceeding the original spec criterion.

---

## SC4: Live Bench Validation — Detail

**Bench infrastructure:** 16 log files across three runs — all confirmed present.

**Gate results per `09.1-VALIDATION.md` (combined across 09.1-03, 09.1-04, 09.1-05 sections):**

| Gate | Requirement | Actual | Source | Verdict |
|------|------------|--------|--------|---------|
| T6 32B × 3 | 3/3 PASS | 3/3 PASS | 09.1-03 logs | PASS |
| T6 72B × 3 | ≥2/3 PASS | 3/3 PASS | 09.1-03 logs | PASS |
| W1 32B step count | 4 → 3 (strict) | 4 → 3 | 09.1-05 log | PASS |
| W2 32B step count | 4 → 3 (strict) | 4 → 3 | 09.1-04 log (preserved in 09.1-05) | PASS |
| T1 32B canary | 1-3 steps, answer=1024 | 1 step, 1024 | 09.1-05 log | CANARY-OK |
| T5 72B canary | unchanged | unchanged | 09.1-03 log | CANARY-OK |

**Direct W1 log evidence** (`/tmp/bench-v1.2-fixed-rev2/v9_1_rev2_W1_32b.log`):

```
step 1: read_file bench-fixtures/bug_lastchar.fs
  thought: First, I need to read the file to understand the bug.
step 2: edit_file {"path": "bench-fixtures/bug_lastchar.fs", "old_string": "s.[s.Length]", "new_string": "s.[s.Length - 1]"}
  thought: The bug is in the line `s.[s.Length]`. It should be `s.[s.Length - 1]` to get the last character.
step 3: final
  thought: The bug has been fixed and the change is confirmed. No further action is needed on this file.
```

No `write_file` in step 3. The `[POST-EDIT CONSTRAINT]` message — injected after the user turn — overrode the user prompt's "using write_file" instruction. Mechanism confirmed effective.

**Halt triggers:**
- Trigger 1 (T6 delta): NOT triggered. T6 passed 6/6.
- Trigger 2 (T1 or T5 canary changes): NOT triggered. T1 CANARY-OK (1 step, 1024). T5 CANARY-OK.
- Trigger 3 (T6 32B vs 72B asymmetry): NOT triggered.

**Audit tech-debt closure:**
- Tech-debt 1 (TOOL-08 dispatcher gap): RESOLVED. T6 32B 3/3, T6 72B 3/3.
- Tech-debt 2 (truncated keyword regression on 72B): RESOLVED. T6 72B fully recovered.
- Tech-debt 3 (32B redundant edit_file + write_file pattern): RESOLVED. W1 closed via loop-injection; W2 closed via directive wording.

All three audit items closed.

---

## SC5: Test Count, Core Purity, and Constraint Injection — Detail

**Test run (live, 2026-04-26):**

```
242 tests run in 00:00:30.8589306 for all — 242 passed, 1 ignored, 0 failed, 0 errored. Success!
```

Count breakdown: 240 v1.2 baseline + 1 from 09.1-02 (TOOL-08-bench in FileToolsTests.fs:237) + 1 from 09.1-05 (POST-EDIT CONSTRAINT injection in AgentLoopTests.fs:165) = 242.

**Core diff (5fbb940..HEAD — `git diff --name-only`):**

```
src/BlueCode.Core/AgentLoop.fs
```

Only `AgentLoop.fs` changed in `src/BlueCode.Core/`. No `Domain.fs` change. The `lastEditPath` parameter is threaded as a function argument; no new DU cases were needed in Domain.

**Constraint injection present in Core code:**

`grep -c "POST-EDIT CONSTRAINT" src/BlueCode.Core/AgentLoop.fs` = 1. Present at line 241 in `buildMessages`, inside the `Some path →` branch.

**CI invariants:**

- `check-no-async.sh`: exits 0 — no `async {}` literals in Core.
- `grep -rn "llm-system" src/`: 0 lines — no absolute path leaks.

---

## Anti-Patterns Found

None. The W1/W2 behavior gap from the prior verification is closed by structural code change (loop-injection), not wording. No stub patterns, empty handlers, or orphaned artifacts.

---

## Re-verification Summary

The single gap from the prior VERIFICATION.md (SC4 W1 × 32B step count 4→4 FAIL) is now closed:

- **Root cause** (identified in 09.1-04): W1's user prompt explicitly instructs "using write_file" — this user-prompt instruction outranked the system-prompt directive. Wording-only intervention class was exhausted.

- **Fix** (09.1-05): Loop-injection injects a System-role `[POST-EDIT CONSTRAINT]` message AFTER the user turn following any successful `edit_file`. Because this message appears later in the conversation history than the user prompt, the model treats it as more recent context and does not proceed to `write_file`. Step count drops from 4 to 3.

- **No regressions**: W2 gate preserved (3 steps). T1 canary unchanged (1 step, answer=1024). Test count increased by exactly 1 (the new constraint-injection test). No Domain.fs change. Core purity intact.

Phase 9.1 goal is achieved: all three v1.2 audit tech-debt items are closed, TOOL-08 delivers its motivating value, and v1.1 baselines are recovered.

---

_Verified: 2026-04-26T02:40:00Z_
_Verifier: Claude (gsd-verifier)_
