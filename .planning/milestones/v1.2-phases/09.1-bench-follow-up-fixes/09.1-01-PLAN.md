---
phase: 09.1-bench-follow-up-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/BlueCode.Cli/CompositionRoot.fs
autonomous: true

must_haves:
  truths:
    - "System prompt's read_file block contains a 'truncated -> pick smaller window' strategy hint (Fix 2)."
    - "System prompt's edit_file block declares the action terminal so the LLM does not chain write_file with the same path (Fix 3)."
    - "defaultSystemPrompt still parses as a valid F# triple-quoted string literal; dotnet build succeeds with zero new warnings."
  artifacts:
    - path: src/BlueCode.Cli/CompositionRoot.fs
      provides: "defaultSystemPrompt with Fix 2 + Fix 3 strategy/terminal hints applied"
      contains: "If truncated: content was clipped"
      contains_alt: "edit_file persists the change"
  key_links:
    - from: src/BlueCode.Cli/CompositionRoot.fs
      to: "LLM behavior on truncated/edit_file"
      via: "system prompt sent in CompleteAsync"
      pattern: "edit_file persists the change\\. After a successful edit_file, do NOT call write_file with the same path\\."
---

<objective>
Apply Fixes 2 and 3 from `documentation/v1.2-bench-followup.md` §2 — pure system-prompt edits inside `defaultSystemPrompt` (a triple-quoted F# string literal in `src/BlueCode.Cli/CompositionRoot.fs`).

Purpose: Fix 2 restores the "narrow the window" remediation strategy that v1.1 72B inferred from response-size differences but v1.2's explicit `truncated` keyword overrode without replacing. Fix 3 closes the 32B mental-model gap where the model uses `edit_file` correctly and then redundantly chains `write_file` containing the full file content.

Output: A modified `defaultSystemPrompt` that contains both new strategy lines verbatim. Zero code changes. Zero test changes. Lowest-risk wave-1 work per `documentation/v1.2-bench-followup.md` §5 (Fix 3 → Fix 2 → Fix 1, lowest risk first).

Source-of-truth design: `documentation/v1.2-bench-followup.md` §2 Fix 2 (lines 80-95) + §2 Fix 3 (lines 97-110). Verified line numbers in `09.1-RESEARCH.md` lines 99-140.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-bench-follow-up-fixes/09.1-RESEARCH.md
@documentation/v1.2-bench-followup.md
@src/BlueCode.Cli/CompositionRoot.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Fix 2 — truncated strategy hint to read_file block</name>
  <files>src/BlueCode.Cli/CompositionRoot.fs</files>
  <action>
Open `src/BlueCode.Cli/CompositionRoot.fs`. Inside the triple-quoted string `let private defaultSystemPrompt: string = """ … """` (lines 51-72), insert ONE new line between the existing line 59 ("Tool output begins: ...") and the existing line 60 ("If out-of-range: ...").

Verified current shape (RESEARCH.md lines 101-108):
```
- read_file:   {"path": "<rel-path>", "start_line": <int?>, "end_line": <int?>}
               Tool output begins: [file: <path>, lines X-Y of Z, not-truncated|truncated|out-of-range]
               If out-of-range: requested start_line > total_lines (Z); choose a start_line <= Z.
```

Target shape (followup doc §2 Fix 2, lines 84-89; RESEARCH.md lines 113-117):
```
- read_file:   {"path": "<rel-path>", "start_line": <int?>, "end_line": <int?>}
               Tool output begins: [file: <path>, lines X-Y of Z, not-truncated|truncated|out-of-range]
               If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.
               If out-of-range: requested start_line > total_lines (Z); choose a start_line <= Z.
```

Indentation MUST match the surrounding lines exactly (15 spaces of leading whitespace inside the literal — visible alignment with `Tool output begins:` and `If out-of-range:`). Use the Edit tool, NOT sed; preserve trailing newline rules of the surrounding string.

Do NOT touch any other line in `defaultSystemPrompt`. Do NOT change the rules block, action enum, or any other action's input schema in this task.

This is Task A of Plan 9.1-01 — commit message MUST be `feat(09.1-01): add truncated strategy hint to system prompt` per CLAUDE.md `{type}({phase}-{plan}): {name}` convention.
  </action>
  <verify>
1. `grep -n "If truncated: content was clipped to 2000 chars" src/BlueCode.Cli/CompositionRoot.fs` — exactly one match, on a line between the existing `Tool output begins:` line and `If out-of-range:` line.
2. `dotnet build BlueCode.slnx` — succeeds with zero new warnings (the project may have pre-existing warnings; new diff must not add any).
3. Visual diff: `git diff src/BlueCode.Cli/CompositionRoot.fs` shows exactly +1 line, 0 lines removed.
  </verify>
  <done>
- `defaultSystemPrompt` contains the verbatim string `If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.`
- The line sits between `Tool output begins:` and `If out-of-range:`.
- Build green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Fix 3 — edit_file terminal-action hint</name>
  <files>src/BlueCode.Cli/CompositionRoot.fs</files>
  <action>
In the same `defaultSystemPrompt` triple-quoted literal, append ONE continuation line directly after the existing `edit_file` row (currently line 64 in HEAD; will be line 65 after Task 1's insertion).

Verified current shape (RESEARCH.md lines 127-130):
```
- edit_file:   {"path": "<rel-path>", "old_string": "<exact-unique-match>", "new_string": "<replacement>"}
```

Target shape (followup doc §2 Fix 3, lines 102-104; RESEARCH.md lines 134-137):
```
- edit_file:   {"path": "<rel-path>", "old_string": "<exact-unique-match>", "new_string": "<replacement>"}
               edit_file persists the change. After a successful edit_file, do NOT call write_file with the same path.
```

Indentation MUST be 15 spaces (visible alignment with the previous line's value column, matching the read_file/list_dir/etc. continuation-line style elsewhere in the prompt).

Do NOT touch the action enum line near the top of the literal. Do NOT alter or remove the existing `edit_file` schema row.

Commit message MUST be `feat(09.1-01): mark edit_file as terminal in system prompt`.
  </action>
  <verify>
1. `grep -n "edit_file persists the change" src/BlueCode.Cli/CompositionRoot.fs` — exactly one match.
2. `grep -n "do NOT call write_file with the same path" src/BlueCode.Cli/CompositionRoot.fs` — exactly one match (same line as above).
3. `dotnet build BlueCode.slnx` — succeeds.
4. `git diff src/BlueCode.Cli/CompositionRoot.fs` for this task: +1 line, 0 removed (cumulative diff after both tasks: +2 lines, 0 removed).
  </verify>
  <done>
- `defaultSystemPrompt` contains the verbatim string `edit_file persists the change. After a successful edit_file, do NOT call write_file with the same path.`
- Line follows the existing `edit_file:` schema row.
- Build green.
  </done>
</task>

</tasks>

<verification>
- [ ] `dotnet build BlueCode.slnx` succeeds.
- [ ] `dotnet run --project tests/BlueCode.Tests/BlueCode.Tests.fsproj` reports the same baseline (240 passed, 1 ignored, 0 failed) — no test changes in this plan, no test count change expected.
- [ ] `grep -c "If truncated: content was clipped" src/BlueCode.Cli/CompositionRoot.fs` outputs `1`.
- [ ] `grep -c "edit_file persists the change" src/BlueCode.Cli/CompositionRoot.fs` outputs `1`.
- [ ] `grep -rn "llm-system" src/` outputs zero lines (Core-purity invariant unchanged — should still be 0).
- [ ] `bash scripts/check-no-async.sh` passes (no `async {}` introduced — this plan touches Cli only, but invariant is global).
- [ ] Diff scope: `git diff --stat src/BlueCode.Cli/CompositionRoot.fs` shows exactly 2 insertions, 0 deletions.
</verification>

<success_criteria>
- Both string-literal lines added verbatim from the followup doc.
- No code logic touched.
- No test count change (240 baseline preserved).
- Build green; check-no-async green; Core-purity grep still 0.
- Two atomic commits per CLAUDE.md `{type}({phase}-{plan}): {name}` convention.
</success_criteria>

<rollback>
If either grep assertion or build fails:
```bash
git checkout -- src/BlueCode.Cli/CompositionRoot.fs
```
Re-attempt with closer attention to indentation (15 spaces of leading whitespace inside the triple-quoted literal). The fix is mechanical; the only failure modes are typos or wrong indentation.
</rollback>

<output>
After completion, create `.planning/phases/09.1-bench-follow-up-fixes/09.1-01-SUMMARY.md` documenting:
- Exact diff applied (line numbers in HEAD before/after).
- Both grep verification commands and their output.
- Build + test count snapshots.
- Confirmation that no Core files were touched.
</output>
