---
phase: 19
plan: 19-02
name: code-bench-docs-alignment
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/BlueCode.Core/Domain.fs
  - src/BlueCode.Core/Router.fs
  - src/BlueCode.Core/AgentLoop.fs
  - src/BlueCode.Cli/CliArgs.fs
  - src/BlueCode.Cli/CompositionRoot.fs
  - src/BlueCode.Cli/Program.fs
  - src/BlueCode.Cli/Adapters/QwenHttpClient.fs
  - tests/BlueCode.Tests/RouterTests.fs
  - tests/BlueCode.Tests/CliArgsTests.fs
  - tests/BlueCode.Tests/RenderingTests.fs
  - tests/BlueCode.Tests/ReplTests.fs
  - tests/BlueCode.Tests/SessionStoreTests.fs
  - tests/BlueCode.Tests/JsonlSinkTests.fs
  - tests/BlueCode.Tests/SmokeTests.fs
  - tests/BlueCode.Tests/AgentLoopTests.fs
  - tests/BlueCode.Tests/ModelsProbeTests.fs
  - bench/run.sh
  - bench/baseline.json
  - scripts/bench-122b-only.sh (DELETE)
  - CLAUDE.md
  - .planning/ROADMAP.md
  - documentation/qwen35-install.md
  - documentation/single-model-eval.md
  - documentation/bench.md
autonomous: true
must_haves:
  truths:
    - "blueCode --model 32b 'x' exits non-zero with a retirement error message referencing Phase 19 and pointing to --model 122b (SC3)"
    - "blueCode --model 72b 'x' exits non-zero with the same retirement error message (SC3)"
    - "blueCode 'x' (no flag) defaults to 122B and runs to completion (SC3 default-works branch)"
    - "blueCode --model 122b 'x' works (canonical alias)"
    - "blueCode --model 35b 'x' (without --with-35b flag) exits non-zero with a clear '35B requires --with-35b' error (SC3)"
    - "blueCode --model 35b --with-35b 'x' when 35B service is absent exits 1 within ~3s with a clear '35B service not loaded — run: launchctl load -w ...' message (SC3 'service absent' branch — B1 fix; eager bootstrap probe in Program.fs gated on withDual)"
    - "tryParseModelId returns AgentError.PathRetired for inputs containing /qwen32b or /qwen72b path segments (SC4)"
    - "bench/run.sh --gate exits 0 against the new single-model baseline (SC2 + SC6)"
    - "bench/baseline.json contains ONLY _122b keys; jq 'keys[]' bench/baseline.json | grep -v _122b returns empty (SC2)"
    - "scripts/bench-122b-only.sh no longer exists (SC2)"
    - "All tests pass in [258, 264]/1/0 range — SC6 amended via W6 from baseline 254/1/0 to reflect +4-7 net new tests in Tasks 2+7"
    - "CLAUDE.md §Runtime Environment describes 122B-only default + dual-mode reactivation procedure; no Qwen 2.5 model references remain (SC5)"
    - "documentation/qwen35-install.md reframes 35B as standby/rollback asset (status badge updated in intro) (SC5)"
    - "documentation/single-model-eval.md §7 cross-references this Phase 19 as the execution of the deferred follow-ups (SC5)"
  artifacts:
    - path: "src/BlueCode.Core/Domain.fs"
      provides: "Model DU with cases Qwen122B and Qwen35B (renamed from Qwen32B/Qwen72B); AgentError.PathRetired variant"
      contains: "| Qwen122B"
      contains_also: "| Qwen35B"
      contains_also_2: "PathRetired"
    - path: "src/BlueCode.Cli/CompositionRoot.fs"
      provides: "parseForcedModel with retirement guards (32b/72b → exit 2 message) + 35b conditional on --with-35b"
    - path: "src/BlueCode.Cli/CliArgs.fs"
      provides: "WithDual CLI arg (--with-35b flag); Model usage string updated for 122b/35b"
    - path: "bench/run.sh"
      provides: "Single-model bench harness — all invocations use --model 122b; absorbs old scripts/bench-122b-only.sh"
    - path: "bench/baseline.json"
      provides: "Single-model 122B baselines only (T6_122b, T5_122b, B2_122b kept; T1_122b/W1_122b/W2_122b added; all _35b/_32b/_72b removed)"
    - path: "CLAUDE.md"
      provides: "Updated Runtime Environment section + dual-mode reactivation procedure"
  key_links:
    - from: "src/BlueCode.Cli/CompositionRoot.fs:parseForcedModel"
      to: "src/BlueCode.Core/Domain.fs:Model"
      via: "Some 122b/35b → Some Qwen122B/Qwen35B; Some 32b/72b → failwithf retirement"
      pattern: "match.*Some \"122b\""
    - from: "src/BlueCode.Cli/Program.fs"
      to: "src/BlueCode.Cli/CliArgs.fs:WithDual"
      via: "args.Contains WithDual passed into parseForcedModel and CliOptions"
      pattern: "Contains.*WithDual|WithDual35b"
    - from: "bench/run.sh:gate"
      to: "bench/baseline.json"
      via: "labels variable matches baseline.json keys (T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b)"
      pattern: "T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b"
---

<objective>
Make the source code, bench harness, and operator-facing docs match the post-retirement
filesystem state established by 19-01. Domain.fs's `Model` DU is renamed
(Qwen32B/Qwen72B → Qwen122B/Qwen35B); a retirement guard rejects `--model 32b/72b` at
the CLI parse layer with an explicit Phase 19 reference; `--with-35b` becomes the explicit
opt-in for dual mode; `tryParseModelId` gains a `PathRetired` AgentError check;
`bench/run.sh` absorbs `scripts/bench-122b-only.sh` in-place and `bench/baseline.json`
halves to the 6-key single-model gate set. CLAUDE.md and three docs are updated.

Purpose: 19-01 retired the physical assets. This plan retires their digital traces and
makes 122B-only the canonical operating shape.

Output: ~9 atomic commits (one per task), 254/1/0 tests preserved, `bench/run.sh --gate`
exit 0 against the new 6-entry baseline.
</objective>

<execution_context>
This plan is fully autonomous — every task is `auto`, no checkpoints. The compile cascade
from Domain.fs's DU rename is mechanical (compiler enforces exhaustiveness across all 9
files, ~26 occurrences). The test impact is enumerated upfront in 19-RESEARCH.md.

Wave 2 dependency on 19-01 means: this plan should not start until 19-01 has confirmed
the post-retirement filesystem state. The bench gate at the end (Task 9) will fail if
the 122B service isn't healthy — which presumes 19-01's checkpoint succeeded.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-qwen25-retirement/19-RESEARCH.md
@.planning/phases/19-qwen25-retirement/19-01-RETIREMENT.md
@CLAUDE.md
@src/BlueCode.Core/Domain.fs
@src/BlueCode.Core/Router.fs
@src/BlueCode.Core/AgentLoop.fs
@src/BlueCode.Cli/CliArgs.fs
@src/BlueCode.Cli/CompositionRoot.fs
@src/BlueCode.Cli/Program.fs
@src/BlueCode.Cli/Adapters/QwenHttpClient.fs
@bench/run.sh
@bench/baseline.json
@scripts/bench-122b-only.sh
@documentation/single-model-eval.md
@documentation/qwen35-install.md
@documentation/bench.md
</context>

<tasks>

<task n="1" type="auto">
  <name>Task 1: Domain.fs Model DU rename + AgentError.PathRetired variant (compile cascade — atomic)</name>
  <goal>Atomic big-bang rename of Model DU cases (Qwen32B → Qwen122B, Qwen72B → Qwen35B) AND addition of AgentError.PathRetired variant, in a SINGLE commit. The compile cascade ripples to Router.fs, QwenHttpClient.fs, CompositionRoot.fs, and 7 test files; all must update in the same commit so the build is green at every git ref.</goal>
  <files>
    - src/BlueCode.Core/Domain.fs
    - src/BlueCode.Core/Router.fs
    - src/BlueCode.Core/AgentLoop.fs (READ for AgentResult construction sites — RESEARCH §Pitfall 6: AgentResult.Model field at Domain.fs line 193-197 means all `AgentResult { ...; Model = Qwen32B/Qwen72B }` construction sites in AgentLoop.fs MUST update to Qwen122B/Qwen35B, otherwise the build breaks here)
    - src/BlueCode.Cli/Adapters/QwenHttpClient.fs
    - src/BlueCode.Cli/CompositionRoot.fs
    - tests/BlueCode.Tests/RouterTests.fs
    - tests/BlueCode.Tests/RenderingTests.fs
    - tests/BlueCode.Tests/ReplTests.fs
    - tests/BlueCode.Tests/SessionStoreTests.fs
    - tests/BlueCode.Tests/JsonlSinkTests.fs
    - tests/BlueCode.Tests/SmokeTests.fs
    - tests/BlueCode.Tests/AgentLoopTests.fs (always — paired with Core/AgentLoop.fs DU cascade)
  </files>
  <steps>
    1. **Domain.fs:**
       - Replace `type Model = | Qwen32B | Qwen72B` (lines 17-19) with `type Model = | Qwen122B | Qwen35B`. Update the docstring above the type to "canonical 122B; opt-in 35B via --with-35b flag (Phase 19)".
       - In the `AgentError` DU (line 139-151), add `| PathRetired of modelPath: string` AFTER `PlanInvalid` (last v2.0 addition). Comment it: `// v2.0 Phase 19: qwen32b/qwen72b path detected post-retirement`.
    2. **Router.fs:** Replace every `Qwen32B` → `Qwen122B` and every `Qwen72B` → `Qwen35B` (Router.fs lines 40,42,48,49,62,63 per RESEARCH grep). Re-read the file after to confirm the routing semantics: with the rename, intent routing now sends Debug/Design/Analysis → Qwen35B and Implementation/General → Qwen122B. This intent table is structurally preserved — the meaning shifts naturally because 35B is the smaller (faster) model in the new pair, paralleling 32B's role in the old pair. (When `--with-35b` is absent, ForcedModel = Some Qwen122B in CompositionRoot bypasses this routing entirely; see Task 3.)
    3. **QwenHttpClient.fs:** Replace `Qwen32B` → `Qwen35B` (35B sits on port 8000 in the new pair) and `Qwen72B` → `Qwen122B` (122B sits on port 8001) at lines 392, 405, 406. NOTE: this is a SEMANTIC SWAP of the model→port mapping — the smaller model (now 35B) takes port 8000, the larger (122B) takes port 8001 — same physical mapping as before, just with renamed DU labels. Verify the spinner label at line 405-406 ends up: `Qwen122B -> "122B"; Qwen35B -> "35B"`.
    4. **CompositionRoot.fs:** Replace `Qwen32B` → `Qwen122B` and `Qwen72B` → `Qwen35B` at lines 45-46. NOTE: this preserves alias semantics for now — `parseForcedModel "32b" → Some Qwen122B` and `parseForcedModel "72b" → Some Qwen35B`. **Task 3 will REPLACE this logic entirely with the retirement-error guard** — Task 1's job is just to keep the build green at this commit boundary.
    5. **AgentLoop.fs (Core):** Per RESEARCH §Pitfall 6, `AgentResult` record has a `Model: Model` field. All `AgentResult { ...; Model = Qwen32B }` and `Model = Qwen72B` construction sites in `src/BlueCode.Core/AgentLoop.fs` MUST update to `Qwen122B`/`Qwen35B` respectively. Grep first: `grep -n "Model = Qwen" src/BlueCode.Core/AgentLoop.fs`. Apply rename to every hit.
    6. **Test files** (RouterTests, RenderingTests, ReplTests, SessionStoreTests, JsonlSinkTests, SmokeTests, AgentLoopTests): Mechanical replace of `Qwen32B` → `Qwen122B` and `Qwen72B` → `Qwen35B`. Reference RESEARCH §Pitfall 1 for line numbers. AgentLoopTests.fs is included unconditionally (paired with the Core/AgentLoop.fs DU cascade in step 5). ModelsProbeTests.fs (string fixtures) is NOT touched in this task — let the compiler tell you if any others surface.
    7. Run `dotnet build` — MUST be green. If any file the planner did not enumerate also references the DU, the compile error tells you exactly where; fix in the same task before committing.
    8. Run the test suite — MUST stay 254/1/0 (the 1 failing is the existing SmokeTests.fs network test, per CLAUDE.md). Test ASSERTIONS may semantically shift (e.g., RouterTests expecting Qwen32B for Implementation now expects Qwen122B; this is correct per the rename).
  </steps>
  <verify>
    - `dotnet build src/BlueCode.Cli 2>&1 | grep -cE "^.*error" ` returns 0
    - `dotnet test tests/BlueCode.Tests --no-build 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed: 254, Failed: 1 (existing SmokeTests network), Errored: 0
    - **(B3 cleanliness)** `grep -rn "Qwen32B\|Qwen72B" src/ tests/ --include="*.fs" | grep -v obj/` returns 0 lines
    - **(B3 Core scoping)** `grep -rn "Qwen32B\|Qwen72B" src/BlueCode.Core/ --include="*.fs"` returns 0 lines (Core fully migrated; AgentLoop.fs's AgentResult.Model construction sites covered)
    - `grep -c "PathRetired" src/BlueCode.Core/Domain.fs` >= 1
    - `grep -c "Qwen122B\|Qwen35B" src/BlueCode.Core/Domain.fs` >= 2
    - **(W5 semantic-correctness — Router rename swap is intentional, not mechanical)**
      - `grep -B1 -A2 "Qwen122B" src/BlueCode.Core/Router.fs | grep -cE "Implementation|General"` >= 1 (Qwen122B handles Implementation + General arms post-rename)
      - `grep -B1 -A2 "Qwen35B" src/BlueCode.Core/Router.fs | grep -cE "Debug|Design|Analysis"` >= 1 (Qwen35B handles Debug/Design/Analysis arms post-rename)
      - `grep -c "Qwen122B" src/BlueCode.Cli/Adapters/QwenHttpClient.fs | head -1` — verify spinner label match arm reads `Qwen122B -> "122B"` (not `Qwen122B -> "35B"`); if a single grep can't prove this, eyeball the spinner label match block
  </verify>
  <commit>
    - Stage individually (NEVER `git add .` per CLAUDE.md):
      - `git add src/BlueCode.Core/Domain.fs src/BlueCode.Core/Router.fs`
      - `git add src/BlueCode.Core/AgentLoop.fs`
      - `git add src/BlueCode.Cli/Adapters/QwenHttpClient.fs src/BlueCode.Cli/CompositionRoot.fs`
      - `git add tests/BlueCode.Tests/RouterTests.fs tests/BlueCode.Tests/RenderingTests.fs`
      - `git add tests/BlueCode.Tests/ReplTests.fs tests/BlueCode.Tests/SessionStoreTests.fs`
      - `git add tests/BlueCode.Tests/JsonlSinkTests.fs tests/BlueCode.Tests/SmokeTests.fs`
      - `git add tests/BlueCode.Tests/AgentLoopTests.fs`
    - Message: `refactor(19-02): rename Model DU cases for single-model default + add PathRetired`
    - Body: "Domain.fs: Qwen32B→Qwen122B, Qwen72B→Qwen35B; AgentError.PathRetired variant. Compile cascade across Router.fs, AgentLoop.fs (AgentResult.Model construction sites), QwenHttpClient.fs, CompositionRoot.fs, 7 test files. Build green; tests 254/1/0."
  </commit>
</task>

<task n="2" type="auto">
  <name>Task 2: Wire AgentError.PathRetired into tryParseModelId</name>
  <goal>tryParseModelId in QwenHttpClient.fs detects retired path segments (qwen32b, qwen72b) and surfaces PathRetired error. This is the DEFENSIVE check at the probe layer (CLI parse layer guard comes in Task 3). Per RESEARCH Open Decision 1: Domain.fs already has the variant (Task 1); now wire it.</goal>
  <files>
    - src/BlueCode.Cli/Adapters/QwenHttpClient.fs
    - tests/BlueCode.Tests/ModelsProbeTests.fs (add 1-2 PathRetired test cases)
    - tests/BlueCode.Tests/RouterTests.fs (rootTests registry update)
  </files>
  <steps>
    1. **QwenHttpClient.fs:** In `tryParseModelId` (signature currently returns `string option`), the prior recommendation was to keep it pure. Per planner's call (Decisions §1): elevate to `Result<string, AgentError>` or add a sibling `tryParseModelIdGuarded` that wraps the existing `tryParseModelId` with a retirement check. **Concrete approach:** keep `tryParseModelId : JsonElement -> string option` UNCHANGED (it stays a pure parser), and add `validateModelPath : string -> Result<string, AgentError>` directly above it that returns `Error (PathRetired path)` if the input string contains "/qwen32b" or "/qwen72b" (substring match — both `~/llm-system/models/qwen32b/...` and HF id `Qwen/Qwen2.5-...-32B` are caught by checking lowercase contains). Otherwise `Ok path`. Wire `validateModelPath` into `probeModelInfoAsync` AFTER `tryParseModelId` returns Some path: `match validateModelPath path with | Error e -> return Error e | Ok p -> ...continue probe...`.
    2. **Render PathRetired in any AgentError display surface.** Grep `match.*AgentError\|with.*AgentError` in src/ — if Rendering.fs or similar has an exhaustive match, add a branch: `| PathRetired path -> sprintf "Path retired in Phase 19: %s. Re-run with --model 122b (or no flag)." path`. If no match exists (CLAUDE.md notes Rendering doesn't currently exhaustively match individual cases), the compiler won't force this. Add it anyway to any natural error-formatting path encountered.
    3. **Tests:** Add to `ModelsProbeTests.fs` two new `testCase`s:
       - `"validateModelPath rejects qwen32b path"` — given input string `"/Users/x/llm-system/models/qwen32b"`, expect `Error (PathRetired _)`
       - `"validateModelPath rejects qwen72b path"` — same shape
       - `"validateModelPath accepts qwen122b path"` — expect `Ok _`
       - `"validateModelPath accepts qwen35b path"` — expect `Ok _`
    4. **Test discovery:** ModelsProbeTests.fs is already in `tests/BlueCode.Tests/BlueCode.Tests.fsproj` and `rootTests` (CLAUDE.md gotcha — verify both). The new testCases are inside the existing `tests` testList, so no new module wiring needed.
    5. Run `dotnet test`. Expect 258/1/0 (4 new test cases added; SmokeTests.fs network test still 1 failing).
  </steps>
  <verify>
    - `dotnet build src/BlueCode.Cli 2>&1 | grep -cE "^.*error"` returns 0
    - `dotnet test tests/BlueCode.Tests --no-build 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed: 258, Failed: 1, Errored: 0
    - `grep -c "PathRetired" src/BlueCode.Cli/Adapters/QwenHttpClient.fs` >= 1
    - `grep -c "validateModelPath" src/BlueCode.Cli/Adapters/QwenHttpClient.fs` >= 2 (definition + call site)
    - `grep -c "validateModelPath" tests/BlueCode.Tests/ModelsProbeTests.fs` >= 2 (test cases)
  </verify>
  <commit>
    - Stage:
      - `git add src/BlueCode.Cli/Adapters/QwenHttpClient.fs`
      - `git add tests/BlueCode.Tests/ModelsProbeTests.fs`
      - (if Rendering.fs touched: `git add src/BlueCode.Cli/Rendering.fs`)
    - Message: `feat(19-02): add PathRetired guard for retired Qwen2.5 paths`
    - Body: "validateModelPath in QwenHttpClient rejects /qwen32b and /qwen72b path segments with AgentError.PathRetired. Probe layer second line of defense behind CLI parse-layer guard (next task). 4 new ModelsProbeTests cases. Tests 258/1/0."
  </commit>
</task>

<task n="3" type="auto">
  <name>Task 3: Argu CLI shape — add WithDual flag, replace --model parser with retirement guard</name>
  <goal>CliArgs.fs gains `WithDual` (--with-35b flag). CompositionRoot.fs's parseForcedModel becomes the canonical retirement gate: 32b/72b → exit 2 with Phase 19 retirement message, 35b → conditional on WithDual flag, 122b/None → Some Qwen122B. Program.fs threads WithDual into parseForcedModel and CliOptions.</goal>
  <files>
    - src/BlueCode.Cli/CliArgs.fs
    - src/BlueCode.Cli/CompositionRoot.fs
    - src/BlueCode.Cli/Program.fs
  </files>
  <steps>
    1. **CliArgs.fs:** Add to the `CliArgs` DU after `NewSession`: `| [<AltCommandLine("--with-35b")>] WithDual` (no parameter; Argu treats parameterless DU cases as boolean presence flags, like `Verbose` and `Trace`). Update `Usage` interface: `| WithDual -> "Enable dual-mode (--model 35b allowed; requires launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist)"`. Update `Model` Usage string: `"Force model: 122b (default), 35b (requires --with-35b). 32b/72b retired in Phase 19."`.
    2. **CompositionRoot.fs:** REPLACE `parseForcedModel` (current lines 43-47) with the retirement-guarded version per RESEARCH §parseForcedModel Phase 19 target:
       ```fsharp
       let parseForcedModel (s: string option) (withDual: bool) : BlueCode.Core.Domain.Model option =
           match s with
           | None -> Some BlueCode.Core.Domain.Qwen122B   // default to 122B
           | Some "122b" -> Some BlueCode.Core.Domain.Qwen122B
           | Some "35b" when withDual -> Some BlueCode.Core.Domain.Qwen35B
           | Some "35b" ->
               failwithf "Model 35b requires --with-35b flag. Run: launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist; then re-invoke with --model 35b --with-35b. See CLAUDE.md §Runtime Environment."
           | Some "32b" ->
               failwithf "Model 32b retired in Phase 19. Use --model 122b (or no flag for default). Migration: see CLAUDE.md §Runtime Environment."
           | Some "72b" ->
               failwithf "Model 72b retired in Phase 19. Use --model 122b (or no flag for default). Migration: see CLAUDE.md §Runtime Environment."
           | Some other -> failwithf "Unknown model: %s (valid values: 122b; 35b requires --with-35b)" other
       ```
       Per planner Decisions §2: `parseForcedModel None = Some Qwen122B` (explicit default), NOT `None`. This eliminates the "no override → use intent routing" indirection — there's only one model when `--with-35b` is absent, so make it explicit.
       
       Also per planner Decisions §2 (intent routing layer): keep `Router.intentToModel` structurally as-is. With `ForcedModel = Some Qwen122B` from default, the router is bypassed (AgentLoop honors ForcedModel before consulting intent). When `--with-35b` is set with no `--model` flag, fall through to intent routing — but per planner Decisions §3, the cleanest UX is also `Some Qwen122B` default; user must pass BOTH `--with-35b` AND `--model 35b` to actually use 35B. (No special "auto-route" behavior with --with-35b alone.) So the function above is complete.
    3. **CompositionRoot.fs (CliOptions):** Add `WithDual35b: bool` field to `CliOptions` record (find the record near `parseForcedModel` and append the field). Wire through `bootstrap` if any signature consumes it (likely not for v1 — the flag is consumed by `parseForcedModel` via Program.fs threading, then forgotten).
    4. **Program.fs:** Where args are parsed (look for `args.Contains NewSession` pattern), add `let withDual = args.Contains WithDual`. Where `parseForcedModel` is called (currently `parseForcedModel modelStr`), update to `parseForcedModel modelStr withDual`. Where `CliOptions` is constructed, populate `WithDual35b = withDual`. Where Argu raises (the existing `eprintfn "ERROR: %s" ex.Message` per RESEARCH §Pitfall 4), this catches the `failwithf` from retirement guards — verify exit code is 2 (or update to 2 if currently 1; ROADMAP §Phase 19 SC3 requires "exit 2"). If the existing error path uses exit 1, add a specific catch for the retirement messages: `with | ex when ex.Message.Contains "retired in Phase 19" -> eprintfn "ERROR: %s" ex.Message; exit 2`.
    
       **(B1 LOAD-BEARING) The `with | ex when ex.Message.Contains "retired"` catch-block in Program.fs is load-bearing for SC3 exit-2 compliance.** Without it, the `failwithf` in `parseForcedModel` falls into the generic Argu error path (exit 1) instead of the SC3-required exit 2. Task 7 adds a CliArgsTests assertion that exercises the `failwithf` directly (proxy verification — full exit-code check requires a live invocation per Task 9 §7).
    5. **(B1) Eager port check for `--with-35b` mode** — add to `CompositionRoot.bootstrap` (or to the section in Program.fs that runs immediately after CLI parse, BEFORE entering the agent loop):
       ```fsharp
       // (B1) When --with-35b is set, fail fast at bootstrap if 35B service isn't responding.
       // Without this guard, the user sees a generic LlmUnreachable on first CompleteAsync after
       // ~15-30s of probe latency. SC3 requires "a clear '35B not loaded' message".
       if withDual then
           use httpClient = new System.Net.Http.HttpClient(Timeout = System.TimeSpan.FromSeconds(2.0))
           try
               let resp = httpClient.GetAsync("http://127.0.0.1:8000/v1/models").GetAwaiter().GetResult()
               if not resp.IsSuccessStatusCode then
                   eprintfn "ERROR: 35B service not loaded — run: launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist"
                   exit 1
           with _ ->
               eprintfn "ERROR: 35B service not loaded — run: launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist"
               exit 1
       ```
       NOTES on placement:
       - Per RESEARCH §parseForcedModel and CLAUDE.md §Bootstrap, "CompositionRoot.bootstrap is synchronous and touches no network". This guard is in PROGRAM.FS (CLI adapter), NOT inside `bootstrap` — preserves the v1.1 lazy-bootstrap invariant. The check runs after `parseForcedModel` and before `bootstrap` is called, only when `withDual=true`.
       - 2-second timeout keeps bootstrap latency negligible when the service IS up (HEAD-equivalent succeeds in <50ms locally).
       - The error message text format matches the migration hint format used by the retirement messages in `parseForcedModel` (consistent UX).
       - Exits with code 1 (not 2) — exit 2 is reserved for the explicit retirement-alias case (32b/72b). "Service unhealthy" is a different failure mode.
       - This supersedes Decisions §3 ("lazy LlmUnreachable on first call") for the `--with-35b` path. The decision rationale (avoid bootstrap latency) is preserved for the default (no `--with-35b`) path: when `withDual=false`, no probe fires.
    6. Build + test. Build must be green; tests still 258/1/0. The eager probe in step 5 is live-only (skipped in dotnet test runs because `withDual=false` is the default in CliOptions construction); no test infra change needed.
  </steps>
  <verify>
    - `dotnet build src/BlueCode.Cli 2>&1 | grep -cE "^.*error"` returns 0
    - `dotnet test tests/BlueCode.Tests --no-build 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed: 258, Failed: 1, Errored: 0
    - `grep -c "WithDual" src/BlueCode.Cli/CliArgs.fs` >= 1
    - `grep -c "with-35b" src/BlueCode.Cli/CliArgs.fs` >= 1 (alias)
    - `grep -c "retired in Phase 19" src/BlueCode.Cli/CompositionRoot.fs` >= 2 (32b + 72b messages)
    - `grep -c "withDual" src/BlueCode.Cli/CompositionRoot.fs` >= 1
    - `grep -c "WithDual" src/BlueCode.Cli/Program.fs` >= 1
    - **(B2 ForcedModel default override wired)** `grep -c "Some BlueCode.Core.Domain.Qwen122B\|Some Qwen122B" src/BlueCode.Cli/CompositionRoot.fs` >= 2 (default `None -> Some Qwen122B` arm + explicit `Some "122b"` arm — confirms ForcedModel override path is wired)
    - **(B1 eager probe wired)** `grep -c "35B service not loaded" src/BlueCode.Cli/Program.fs` >= 1 (or `src/BlueCode.Cli/CompositionRoot.fs` if relocated; one of the two MUST contain this string)
    - **(B1 eager probe gated on withDual)** `grep -B1 "35B service not loaded" src/BlueCode.Cli/Program.fs | grep -c "withDual\|WithDual"` >= 1 (probe ONLY fires when --with-35b is set; no overhead for default invocations)
    - **(W4 retirement-error catch is load-bearing)** `grep -c "Contains.*retired" src/BlueCode.Cli/Program.fs` >= 1 (the `with | ex when ex.Message.Contains "retired"` exit-2 catch is present)
    - Live smoke (skip if 122B service unavailable; not gating): `dotnet run --project src/BlueCode.Cli -- --model 32b "x" 2>&1; echo "Exit: $?"` should print error containing "Phase 19" and exit 2.
    - **(B1 live smoke for `--with-35b` + service absent)** Live-only: with `launchctl unload ~/Library/LaunchAgents/com.ohama.qwen35b.plist` first, then `dotnet run --project src/BlueCode.Cli -- --model 35b --with-35b "x" 2>&1; echo "Exit: $?"` should print "35B service not loaded" and exit 1 within ~3 seconds (proves SC3 "35B not loaded" branch).
  </verify>
  <commit>
    - Stage:
      - `git add src/BlueCode.Cli/CliArgs.fs`
      - `git add src/BlueCode.Cli/CompositionRoot.fs`
      - `git add src/BlueCode.Cli/Program.fs`
    - Message: `feat(19-02): replace --model aliases for retirement; add --with-35b dual-mode flag`
    - Body: "parseForcedModel: Some 32b/72b → exit 2 retirement error referencing Phase 19; Some 35b conditional on --with-35b; None defaults to Some Qwen122B (explicit default). Argu gains WithDual flag. Tests still 258/1/0."
  </commit>
</task>

<task n="4" type="auto">
  <name>Task 4: Router.fs flag-gated dual-port probe alignment</name>
  <goal>Router.fs's intent table stays structurally intact (already DU-renamed in Task 1). Verify QwenHttpClient.fs probe selection: when ForcedModel=Some Qwen122B (default), only probe8001 fires; probe8000 stays unallocated. When ForcedModel=Some Qwen35B (--with-35b path), both probes available. This task is the final cleanup pass on Router.fs + verifying the probe Lazy<> doesn't fire 8000 in single-model default.</goal>
  <files>
    - src/BlueCode.Core/Router.fs
    - src/BlueCode.Cli/Adapters/QwenHttpClient.fs (verify lazy semantics)
  </files>
  <steps>
    1. **Router.fs:** Re-read lines 40-65 post-Task-1 rename. Confirm intent table reads:
       - Debug → Qwen35B
       - Design → Qwen35B
       - Analysis → Qwen35B
       - Implementation → Qwen122B
       - General → Qwen122B
       Note: this matches the OLD pattern (smaller model for Debug/Design/Analysis, larger for Implementation/General) — Task 1's mechanical rename preserves the routing semantic. This is correct for `--with-35b` mode. In single-model mode, `ForcedModel = Some Qwen122B` overrides this routing entirely; the table is dormant unless `--with-35b` is set + user lets routing happen (currently impossible per Task 3's `parseForcedModel None → Some Qwen122B` default; routing only fires if user passes `--model` with an unknown intent path, which ROADMAP doesn't require).
       
       **Cleanup:** Update the docstring/comment block above the intent table to read: "Intent routing: dormant in single-model default mode (ForcedModel = Some Qwen122B). Active when both --with-35b and explicit dual-mode invocation are set. Phase 19: retained for future SHIP-BOTH evolution."
    2. **modelToEndpoint (Router.fs):** Verify `Qwen122B → Port8001` and `Qwen35B → Port8000`. Per RESEARCH, this preserves the physical port mapping (smaller-on-8000, larger-on-8001).
    3. **modelToTemperature (Router.fs):** Verify temperature settings are preserved (no semantic change from rename).
    4. **QwenHttpClient.fs lazy probe:** Read lines 380-395 for the probe initialization. The current pattern allocates BOTH probe8000 and probe8001 as `Lazy<Task<ModelInfo>>` upfront in `create()`. With single-model default, allocating an unfired Lazy<> for port 8000 is harmless (no network call until forced). However, per ROADMAP §Phase 19 architectural goal "Router collapses from 5 to 2 functions", consider whether probe8000 should be `if withDual then Some(lazy ...) else None` — but this changes the probe-selection match arm. For minimal Task-4 footprint, KEEP probe8000 as an unfired Lazy<> (no behavioral difference; lazy initializer never runs without `Qwen35B` path); add a comment: `// probe8000: only fired when model = Qwen35B (--with-35b mode); harmless to allocate as unfired Lazy<>`.
    5. Build + test. Tests still 258/1/0.
  </steps>
  <verify>
    - `dotnet build src/BlueCode.Cli 2>&1 | grep -cE "^.*error"` returns 0
    - `dotnet test tests/BlueCode.Tests --no-build 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed: 258, Failed: 1, Errored: 0
    - `grep -c "single-model default" src/BlueCode.Core/Router.fs` >= 1 (docstring updated)
    - `grep -c "Qwen122B" src/BlueCode.Core/Router.fs` >= 2 (Implementation + General routing arms)
    - `grep -c "probe8000" src/BlueCode.Cli/Adapters/QwenHttpClient.fs` >= 1 (kept, just commented)
    - **(B2 — ForcedModel override path wired end-to-end)** `grep -cE "ForcedModel.*Some.*Qwen122B|Some Qwen122B" src/BlueCode.Cli/CompositionRoot.fs` >= 1 — confirms `parseForcedModel None → Some Qwen122B` flows into AgentConfig.ForcedModel as the default override (NOT `None`). Without this, the default invocation would fall back to the intent table, potentially routing some queries to Qwen35B (port 8000, unloaded). This is the LOAD-BEARING wiring that guarantees single-model mode is structurally enforced, not just a routing-table coincidence.
    - **(B2 — RouterTests bypass coverage)** Optional but cheap: add to `tests/BlueCode.Tests/RouterTests.fs` a test case `"in single-model default mode, ForcedModel Some Qwen122B bypasses intent routing"` — given `ForcedModel = Some Qwen122B` and an Intent that maps to Qwen35B in the table (e.g., Intent.Debug), assert AgentLoop's effective model = Qwen122B (not Qwen35B). If the AgentLoop test surface doesn't permit this directly, a unit test on `Router.intentToModel` plus a comment "ForcedModel bypasses this; see CompositionRoot.bootstrap" satisfies the intent.
  </verify>
  <commit>
    - Stage:
      - `git add src/BlueCode.Core/Router.fs`
      - `git add src/BlueCode.Cli/Adapters/QwenHttpClient.fs`
    - Message: `refactor(19-02): document Router intent table as dormant in single-model default`
    - Body: "Router.fs intent table preserved structurally (active only in --with-35b mode); docstring updated. QwenHttpClient.probe8000 commented as unfired-by-default. Tests 258/1/0."
  </commit>
</task>

<task n="5" type="auto">
  <name>Task 5: bench/run.sh in-place rewrite (absorb scripts/bench-122b-only.sh)</name>
  <goal>Rewrite bench/run.sh so every invocation uses --model 122b. Drop all `for model in 32b 72b` loops. Gate function uses 6 labels (T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b). Delete scripts/bench-122b-only.sh after absorption.</goal>
  <files>
    - bench/run.sh
    - scripts/bench-122b-only.sh (DELETE)
  </files>
  <steps>
    1. **bench/run.sh:** Read current file in full. Identify all functions: `regression()`, `canary()`, `b2_mode()`, `gate()`, `phase_variance()`, `phase_diagnose()`, `phase_write()`. For each:
       - Replace `for model in 32b 72b; do ... done` with the body executed once with `model=122b` (no loop).
       - Replace any hardcoded `--model 32b` or `--model 72b` with `--model 122b`.
       - Update labels embedded in `run "label_${model}"` patterns to use `122b` suffix.
    2. **gate() specifically (per planner Decisions §4 — gate is 6/6, NOT padded to 8):**
       - `local labels="T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b"`
       - `local total=6`
       - Hardcoded `run "gate_T6_122b" "122b" "<T6 prompt>"` etc. — six invocations, all with `"122b"` model arg.
       - Update final summary line: `printf "GATE PASS (%d/%d)\n" $passed $total` (already uses variables; no string change needed beyond label set).
       - Borrow the pre-condition curl check from `scripts/bench-122b-only.sh` (likely a `curl -fsS http://127.0.0.1:8001/v1/models` near top of gate function with early-exit on failure).
    3. **Mode help text:** Update `--help` output to remove dual-model language; describe single-model 122B as canonical.
    4. **Log directory naming:** Verify gate uses `gate-<ts>/` (NOT `122b-only-<ts>/` which was bench-122b-only.sh's pattern). Existing log parsing presumes `gate-*/` shape.
    5. **Delete scripts/bench-122b-only.sh:** `git rm scripts/bench-122b-only.sh`. Per RESEARCH this script's purpose has been absorbed; SC2 requires it no longer exists.
    6. **Smoke run** (does NOT gate; only checks shell parses): `bash -n bench/run.sh` — must exit 0 (syntax clean). Then `bash bench/run.sh --help 2>&1 | head -20` — must show updated help text.
  </steps>
  <verify>
    - `bash -n bench/run.sh; echo "Syntax exit: $?"` prints "Syntax exit: 0"
    - `grep -c "for model in 32b 72b" bench/run.sh` returns 0
    - `grep -c "model 32b\|model 72b" bench/run.sh` returns 0
    - `grep -c "model 122b" bench/run.sh` >= 6 (at least six --model 122b invocations in gate)
    - `grep "labels=" bench/run.sh | grep -c "T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b"` >= 1
    - `[ ! -f scripts/bench-122b-only.sh ] && echo PASS || echo FAIL` prints PASS
    - `git status --short scripts/bench-122b-only.sh` shows the deletion staged
  </verify>
  <commit>
    - Stage:
      - `git add bench/run.sh`
      - `git rm scripts/bench-122b-only.sh` (the rm command auto-stages)
    - Message: `chore(19-02): absorb 122b-only bench harness into bench/run.sh`
    - Body: "bench/run.sh: drop dual-model loops, all invocations --model 122b. gate() label set 8→6 (T6/W1/W2/T1/T5/B2 all _122b). scripts/bench-122b-only.sh deleted (absorbed). Per ROADMAP Decision D."
  </commit>
</task>

<task n="6" type="auto">
  <name>Task 6: bench/baseline.json halve to single-model 122B keys</name>
  <goal>baseline.json becomes 6-entry single-model. Drop _35b/_32b/_72b keys; keep T6_122b/T5_122b/B2_122b; add T1_122b/W1_122b/W2_122b using Phase 18 bench data (no fresh bench needed for population per RESEARCH).</goal>
  <files>
    - bench/baseline.json
  </files>
  <steps>
    1. Read current `bench/baseline.json`. Note actual key set (RESEARCH says: T6_35b, T6_122b, W1_35b, W2_35b, T1_35b, T5_122b, B2_35b, B2_122b — 8 entries).
    2. Construct new JSON with 6 entries:
       - `T6_122b` — preserve from current file (step_count_max=5, etc.)
       - `T5_122b` — preserve from current file (step_count_max=4)
       - `B2_122b` — preserve from current file (step_count_max=3, pass=true)
       - `T1_122b` — NEW. Per RESEARCH §bench/baseline.json: step_count=1, baseline_max=3 (mirror T1_35b shape). Median elapsed 4s per Phase 18 data.
       - `W1_122b` — NEW. Per Phase 18 single-model-eval.md: step_count=3, step_count_max=3 (matches W1_35b max from current baseline). Median elapsed 8s.
       - `W2_122b` — NEW. Per Phase 18 single-model-eval.md: step_count=3, step_count_max=3. Median elapsed 9s.
    3. **Schema preservation:** Each entry's JSON shape must match the existing entries (e.g., `{ "step_count_max": N, "elapsed_p50_seconds": N }` or whatever schema the existing T6_122b uses). Do NOT invent new fields — copy the existing T6_122b entry and adjust numbers.
    4. Format with `jq . bench/baseline.json > /tmp/bl.json && mv /tmp/bl.json bench/baseline.json` to keep canonical formatting.
  </steps>
  <verify>
    - `jq 'keys | length' bench/baseline.json` returns 6
    - `jq 'keys[]' bench/baseline.json | sort` returns exactly: "B2_122b", "T1_122b", "T5_122b", "T6_122b", "W1_122b", "W2_122b" (in alphabetical sort order)
    - `jq 'keys[]' bench/baseline.json | grep -cE "_35b|_32b|_72b"` returns 0
    - `jq -e '.T6_122b' bench/baseline.json` exits 0 (entry is non-null)
    - `jq -e '.W1_122b.step_count_max' bench/baseline.json` exits 0
  </verify>
  <commit>
    - Stage: `git add bench/baseline.json`
    - Message: `chore(19-02): halve baseline.json to single-model 122B (6 entries)`
    - Body: "Drop _35b/_32b/_72b keys (5 removed). Keep T6_122b, T5_122b, B2_122b. Add T1_122b (step=1), W1_122b (step=3), W2_122b (step=3) from Phase 18 single-model-eval bench data. Per ROADMAP §Phase 19 SC2."
  </commit>
</task>

<task n="7" type="auto">
  <name>Task 7: CliArgsTests.fs — convert 32b/72b assertions to retirement-error tests</name>
  <goal>Per RESEARCH §Pitfall 2: CliArgsTests.fs has 4 tests asserting `parseForcedModel (Some "32b") = Some Qwen32B` and similar. These now must assert that 32b/72b RAISE the retirement error, while new tests cover 122b/35b paths.</goal>
  <files>
    - tests/BlueCode.Tests/CliArgsTests.fs
  </files>
  <steps>
    1. Read current CliArgsTests.fs around lines 71-100 (the 4 parseForcedModel test cases).
    2. Replace the 4 old tests with 6 new ones (4 → 6 net +2):
       - `"parseForcedModel None defaults to Qwen122B"` — `parseForcedModel None false |> should equal (Some Qwen122B)`
       - `"parseForcedModel Some 122b returns Qwen122B"` — assert `Some Qwen122B`
       - `"parseForcedModel Some 35b without dual flag throws retirement-style error"` — wrap in try/with; expect message containing "requires --with-35b"
       - `"parseForcedModel Some 35b with dual flag returns Qwen35B"` — `parseForcedModel (Some "35b") true` returns `Some Qwen35B`
       - **(W4 LOAD-BEARING)** `"parseForcedModel Some 32b throws retirement error mentioning Phase 19"` — wrap in try/with; assert exception message contains BOTH "retired in Phase 19" AND "122b" (proxy verification for Program.fs's `with | ex when ex.Message.Contains "retired" -> exit 2` catch-block — the catch matches on this exact substring, so the test guarantees the message format that triggers exit 2).
       - **(W4 LOAD-BEARING)** `"parseForcedModel Some 72b throws retirement error mentioning Phase 19"` — same shape; same assertion on "retired in Phase 19" substring.
    3. **Argu string-parse tests (lines 72-89 per RESEARCH):** These tests just verify `--model 72b` is captured as the string `"72b"` by Argu (NOT that the model resolves). Per RESEARCH these stay valid — Argu's job is string capture, not validation. Leave unchanged. (If any of these tests assertion-deep on the resolved Model value, update to reflect the new retirement error.)
    4. **Test discovery:** CliArgsTests.fs is already in `BlueCode.Tests.fsproj` and `rootTests` (per CLAUDE.md gotcha — verify). The 6 new test cases live in the existing `tests` testList; no module wiring change needed.
    5. **WithDual flag parsing test:** Add one test verifying Argu parses `--with-35b` as `WithDual`: `"args parses --with-35b as WithDual flag"` — `args.Contains WithDual = true` after parsing `[|"--with-35b"; "x"|]`.
    6. Run tests. Expect 261/1/0 (4 old removed, 6+1 new added = 258 → 258 + 3 = 261; SmokeTests still 1 failing).
       Note: net delta depends on what was there. Recompute after edit; range 258-261 acceptable.
  </steps>
  <verify>
    - `dotnet test tests/BlueCode.Tests --no-build 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed: in [258, 264] range, Failed: 1, Errored: 0
    - `grep -c "retired in Phase 19" tests/BlueCode.Tests/CliArgsTests.fs` >= 2 (32b + 72b assertion text)
    - `grep -c "WithDual" tests/BlueCode.Tests/CliArgsTests.fs` >= 1 (--with-35b parsing test)
    - `grep -c "Qwen122B" tests/BlueCode.Tests/CliArgsTests.fs` >= 2 (None default + 122b explicit)
    - `grep -c "Qwen35B" tests/BlueCode.Tests/CliArgsTests.fs` >= 1 (35b with dual)
  </verify>
  <commit>
    - Stage: `git add tests/BlueCode.Tests/CliArgsTests.fs`
    - Message: `test(19-02): cover Qwen2.5 retirement errors and --with-35b flag parsing`
    - Body: "Old 4 tests asserting Some Qwen32B/Qwen72B replaced with retirement-error tests. New tests: parseForcedModel default→Qwen122B, --with-35b enables --model 35b, 32b/72b throw Phase 19 retirement messages. Tests in 258-264 range, 1 failed, 0 errored."
  </commit>
</task>

<task n="8" type="auto">
  <name>Task 8: Documentation updates — CLAUDE.md, qwen35-install.md, single-model-eval.md, bench.md</name>
  <goal>Bring the docs to canonical-single-model-122B state. CLAUDE.md §Runtime Environment fully reframed; "Don't Do" rule about Router.modelToName preserved (it's an unrelated v1 cleanup, NOT a Phase 19 concern — see RESEARCH note); qwen35-install.md status badge → "standby asset"; single-model-eval.md gains §7 cross-reference to Phase 19 as the executor of deferred follow-ups; bench.md "Hang Contingency" updated to reference 122B's plist.</goal>
  <files>
    - CLAUDE.md
    - .planning/ROADMAP.md
    - documentation/qwen35-install.md
    - documentation/single-model-eval.md
    - documentation/bench.md
  </files>
  <steps>
    1. **CLAUDE.md §Runtime Environment** (current state per claudeMd context: describes "Qwen 3.5 35B-A3B-4bit ... + 122B-A10B-4bit"):
       - Reframe to lead with 122B as canonical sole model; 35B as standby cold rollback.
       - Update memory budget paragraph: post-retirement combined RSS = 45.4 GB (122B alone), down from 62.4 GB.
       - Add a "Dual-mode reactivation" subsection with exact procedure: `launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist` + invoke blueCode with `--with-35b` flag (and explicit `--model 35b` to actually route to it).
       - Remove all references to qwen32b / qwen72b (RESEARCH §Pitfall 8 noted these in legacy docs section — keep the legacy doc PATHS as historical references but remove operational mentions).
    2. **CLAUDE.md §Don't Do:**
       - Per task description: "delete `Don't swap Router.modelToName back in` rule? — RESEARCH may have details". Re-read CLAUDE.md §Don't Do; the `modelToName` rule is a v1 cleanup invariant unrelated to Phase 19 — KEEP IT. (No change.) Document in commit body that this rule was reviewed and intentionally preserved.
       - However, the `tryParseModelId`'s `StartsWith("/")` heuristic Don't-Do rule may need updating since `validateModelPath` is now layered on top. Add a note: "Don't reintroduce qwen32b/qwen72b path support — Phase 19 retirement; PathRetired guard in validateModelPath."
    3. **documentation/qwen35-install.md:** Update intro paragraph (likely first 20 lines) to read "**Status (Phase 19, 2026-04-27):** Qwen 3.5 35B is retained on disk as a STANDBY/ROLLBACK asset. The canonical blueCode runtime is 122B alone (single-model default). To reactivate 35B for dual-mode use, see §Dual-mode reactivation below." Add a new §Dual-mode reactivation section (or update existing §6 Path B reference) with the launchctl + --with-35b procedure.
    4. **documentation/single-model-eval.md:** Add §7 (or update existing §7) "Phase 19 Execution" pointing to `.planning/phases/19-qwen25-retirement/19-{01,02}-PLAN.md` as the executor of the deferred follow-ups enumerated in §SC5. Brief paragraph: "The architectural follow-ups deferred in §SC5 (Router collapse, baseline halve, CLAUDE.md update, scripts/bench-122b-only.sh promotion) are executed in Phase 19 (planned 2026-04-27, executed [DATE]). See 19-02-PLAN.md for the autonomous code/bench/docs alignment."
    5. **documentation/bench.md** (per RESEARCH §Pitfall 8):
       - Line 148 area "Hang Contingency": replace `com.ohama.qwen32b` with `com.ohama.qwen122b` in the kickstart command. Replace `unload`/`load` commands referencing qwen32b with qwen122b equivalents.
       - Lines 179-186 sample gate output: update from 8-entry pattern to new 6-entry pattern (T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b). Update GATE PASS line to (6/6).
       - Line 203 "Known Regressions" historical note: annotate `B2_32b and B2_72b` with `(historical — Qwen 2.5 retired in Phase 19; current B2_122b preserves diagnose accuracy)`.
       - Update any §Mode flags table that lists `--model 32b/72b` to remove those rows; add `--model 122b` (default) and `--model 35b` (requires `--with-35b`).
    6. Sanity check: `grep -c "qwen32b\|qwen72b\|32B\|72B" CLAUDE.md` should return 0 (or only historical-doc-path mentions like `qwen32b-base-to-instruct.md` filename references, which are valid as legacy doc names per CLAUDE.md context).
    7. **(W6) ROADMAP.md SC6 baseline count update** — open `.planning/ROADMAP.md`, locate the Phase 19 SC6 line. It currently reads "254/1/0" (the pre-Phase-19 baseline). Tasks 2 + 7 of THIS plan add 4-7 net new test cases (Task 2 adds 4 ModelsProbeTests cases for `validateModelPath`; Task 7 nets +2-3 in CliArgsTests after the retirement-error swap and `--with-35b` parsing test). Replace "254/1/0" with "258-264/1/0" in SC6 to reflect the post-19-02 expected range. Keep the same `Failed: 1` (existing SmokeTests network test) and `Errored: 0` constraints. Stage `.planning/ROADMAP.md` individually per CLAUDE.md commit protocol.
  </steps>
  <verify>
    - `grep -c "Phase 19" CLAUDE.md` >= 1 (retirement reference)
    - `grep -c "with-35b" CLAUDE.md` >= 1 (dual-mode reactivation procedure)
    - `grep -c "Qwen 2.5\|qwen32b/\|qwen72b/" CLAUDE.md` returns 0 (or only legacy filename refs that are intentional)
    - `grep -c "STANDBY\|standby" documentation/qwen35-install.md` >= 1 (status badge updated)
    - `grep -c "Phase 19\|19-02-PLAN" documentation/single-model-eval.md` >= 1 (cross-reference added)
    - `grep -c "qwen122b" documentation/bench.md` >= 1 (Hang Contingency updated)
    - `grep -c "T6_122b W1_122b W2_122b T1_122b T5_122b B2_122b\|6/6" documentation/bench.md` >= 1 (sample gate output updated)
    - **(W6 ROADMAP SC6 amended)** `grep -c "258-264/1/0\|258/1/0" .planning/ROADMAP.md` >= 1 (Phase 19 SC6 reflects new test-count range)
    - **(W6 old count removed in SC6 context)** Read .planning/ROADMAP.md Phase 19 SC6 line directly to confirm the literal "254/1/0" no longer appears as the SC6 acceptance number (it may still appear elsewhere in ROADMAP as historical baseline references for prior phases — only SC6 of Phase 19 changes).
  </verify>
  <commit>
    - Stage docs commit (1 of 2 commits in this task):
      - `git add CLAUDE.md`
      - `git add documentation/qwen35-install.md`
      - `git add documentation/single-model-eval.md`
      - `git add documentation/bench.md`
    - Message: `docs(19-02): document single-model 122B as canonical; reframe 35B as standby`
    - Body: "CLAUDE.md §Runtime Environment: 122B canonical, dual-mode reactivation procedure. qwen35-install.md: STANDBY status badge. single-model-eval.md §7: Phase 19 cross-reference. bench.md: Hang Contingency → qwen122b, sample gate 8→6 entries, --model alias table updated. Router.modelToName Don't-Do rule reviewed and preserved (unrelated to Phase 19)."
    - Stage roadmap commit (2 of 2; per CLAUDE.md plan-metadata commits separate from code/docs commits):
      - `git add .planning/ROADMAP.md`
    - Message: `docs(19): amend Phase 19 SC6 test-count range for new test cases`
    - Body: "SC6 baseline count 254/1/0 → 258-264/1/0 to reflect Tasks 2 + 7 net additions (4 ModelsProbeTests for validateModelPath, 2-3 net CliArgsTests for retirement errors + --with-35b parsing). Failed: 1 (existing SmokeTests network) and Errored: 0 unchanged."
  </commit>
</task>

<task n="9" type="auto">
  <name>Task 9: Final verification — bench gate + test count + grep cleanliness</name>
  <goal>Run the full verification suite. SC6 requires 254/1/0 tests (note: RESEARCH says current is 254/1/0; we added 4-7 new test cases through Tasks 2 and 7, so the new total is 258-261 — recompute SC6 in commit body). bench gate must exit 0 against the new 6-entry baseline. No qwen32b/qwen72b refs in src/.</goal>
  <files>
    - (none — verification only; may touch CLAUDE.md/STATE.md if metric updates are recorded)
  </files>
  <steps>
    1. Run full test suite: `dotnet test tests/BlueCode.Tests 2>&1 | tail -20`. Capture output. Expect Passed: in [258, 264], Failed: 1 (existing SmokeTests network), Errored: 0.
       - If Errored > 0: investigate and fix in this task before committing — this is a regression.
       - If Failed > 1: investigate; new failures must be addressed.
    2. Run `bench/run.sh --gate` and capture output: `bench/run.sh --gate 2>&1 | tail -20`. Expect "GATE PASS (6/6)" and exit code 0.
       - If gate fails: surface the first failing entry. Common cause: baseline.json keys don't match gate labels exactly (RESEARCH §Pitfall 3) — verify Task 5 + Task 6 alignment.
    3. Final grep cleanup verification:
       - `grep -rn "Qwen32B\|Qwen72B" src/ tests/ --include="*.fs" | grep -v obj/` → must return 0
       - `grep -rn "qwen32b\|qwen72b" src/ --include="*.fs" | grep -v obj/` → may return 0 or only docstring references; if any remain in code (not strings), investigate
       - `[ -f scripts/bench-122b-only.sh ]; echo $?` → must print 1 (file does not exist)
       - `jq 'keys | length' bench/baseline.json` → 6
       - `jq 'keys[]' bench/baseline.json | grep -cE "_35b|_32b|_72b"` → 0
    4. Note any remaining cleanup needs in commit body (e.g., if a stale comment references qwen32b in a docstring, note it as deferred non-blocker).
    5. **Commit only if changes were made** (this task may produce no commit if all prior tasks landed cleanly). If no diff, log "Verification clean — no commit needed for Task 9; phase ready for /gsd:verify-work 19".
  </steps>
  <verify>
    - `dotnet test tests/BlueCode.Tests 2>&1 | grep -E "Passed:|Failed:|Errored:"` shows Passed in range [258, 264], Failed: 1, Errored: 0
    - `bench/run.sh --gate; echo "Exit: $?"` prints "Exit: 0" (and shows "GATE PASS (6/6)" earlier in output)
    - `grep -rn "Qwen32B\|Qwen72B" src/ tests/ --include="*.fs" | grep -v obj/ | wc -l` returns 0
    - `[ ! -f scripts/bench-122b-only.sh ] && echo "PASS: deleted" || echo "FAIL"` prints "PASS: deleted"
    - `jq 'keys | length' bench/baseline.json` returns 6
  </verify>
  <commit>
    - If any final fixup files were touched: stage individually and commit with `chore(19-02): final verification fixups`.
    - If verification is clean with no diff: NO COMMIT. Per CLAUDE.md: "If there are no changes to commit ... do not create an empty commit."
  </commit>
</task>

</tasks>

<decisions>

Planner-level calls (resolves all RESEARCH Open Questions; executor does NOT need to revisit):

1. **PathRetired location → AgentError DU variant in Core.** Per CLAUDE.md "AgentError is the typed error contract" pattern (v1.1 invariant), retirement is a first-class error case. Domain.fs gains `| PathRetired of modelPath: string`. Wired into QwenHttpClient via `validateModelPath` helper (Task 2). This is the more conservative choice — RESEARCH's "Path 1 (passive)" would skip the variant, but per task brief Decision 4-A this is required for SC4 conformance.

2. **parseForcedModel default → `Some Qwen122B` (explicit default).** Removes the "no override → intent routing" indirection that's meaningless in single-model mode. Cleaner semantics: `parseForcedModel None _ = Some Qwen122B`. Intent router (Router.intentToModel) becomes dormant in default mode — kept structurally for future SHIP-BOTH evolution but not exercised. Per task brief Decision 4-B.

3. **--model 35b with absent service → eager 2s HEAD probe in Program.fs (NOT inside CompositionRoot.bootstrap).** REVISED post-checker B1: the original Decision 3 ("lazy LlmUnreachable on first call") was insufficient for SC3's requirement of a "clear '35B not loaded' message" — a generic port-unreachable surfaced at first CompleteAsync after ~15-30s of probe latency does not qualify as "clear". Task 3 step 5 adds a 2-second-timeout probe in Program.fs (BEFORE bootstrap is invoked, ONLY when `withDual=true`) that exits 1 with the explicit migration hint `"35B service not loaded — run: launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist"`. This preserves the lazy-bootstrap invariant (the probe is in CLI adapter, not in `bootstrap`); imposes no overhead on the default path (no `--with-35b` ⇒ no probe); and stays under 50ms latency when the service IS healthy. Exit code is 1 (not 2) — exit 2 is reserved for the explicit retirement-alias case (32b/72b).

4. **Gate set size → 6/6 (no padding).** Six natural entries: T6, W1, W2, T1, T5, B2 — all _122b. Single-model gate is fundamentally smaller; padding to 8 would dilute signal without adding test value. ROADMAP doesn't specify a minimum count. Per task brief Decision 4-D.

5. **Router intent table → preserved structurally, dormant by default.** `intentToModel` keeps 5 Intent cases mapped to 2 Models (Debug/Design/Analysis → Qwen35B; Implementation/General → Qwen122B). With ForcedModel = Some Qwen122B as default, the table is bypassed. Active only in --with-35b explicit dual mode. Per RESEARCH §Pitfall 10 recommendation.

6. **CLAUDE.md `Don't swap Router.modelToName back in` rule → preserved.** This rule is a v1 cleanup invariant unrelated to Phase 19. Reviewed and intentionally not modified. Documented in Task 8 commit body.

7. **Compile cascade strategy → big-bang single commit (Task 1).** Per v1.1 pattern referenced in CLAUDE.md §Key Seams ("the v1.1 'big-bang compile cascade' pattern"). All ~26 occurrences across 9 files in one commit so the build is green at every git ref. Subsequent commits (Tasks 2-9) then layer on top of a clean baseline.

8. **Test discovery registration:** Tasks 2 + 7 add new test cases inside EXISTING test modules (ModelsProbeTests.fs, CliArgsTests.fs) — both already in `BlueCode.Tests.fsproj` and `rootTests` per RESEARCH. No new module wiring needed. (CLAUDE.md test-discovery gotcha doesn't apply because we're not adding new test files.)

</decisions>

<verification>

Cross-cutting checks (executed by Task 9; gating for `/gsd:verify-work 19`):

1. **Build green:** `dotnet build src/BlueCode.Cli` exits 0 with no errors.
2. **Tests green:** `dotnet test tests/BlueCode.Tests` shows Passed in [258, 264], Failed: 1 (existing SmokeTests network test only), Errored: 0.
3. **Bench gate green:** `bench/run.sh --gate` shows "GATE PASS (6/6)" and exits 0.
4. **No legacy DU references:** `grep -rn "Qwen32B\|Qwen72B" src/ tests/ --include="*.fs" | grep -v obj/` returns 0 lines.
5. **bench/baseline.json shape:** `jq 'keys | length' bench/baseline.json` returns 6; `jq 'keys[]' bench/baseline.json | grep -cE "_35b|_32b|_72b"` returns 0.
6. **scripts/bench-122b-only.sh deleted:** `[ ! -f scripts/bench-122b-only.sh ]` exits 0.
7. **Live retirement smoke (manual, post-merge):**
   - `dotnet run --project src/BlueCode.Cli -- --model 32b "x" 2>&1; echo "Exit: $?"` prints retirement error mentioning Phase 19, exits 2.
   - `dotnet run --project src/BlueCode.Cli -- --model 72b "x" 2>&1; echo "Exit: $?"` same.
   - `dotnet run --project src/BlueCode.Cli -- --model 122b "What is 2+2?"` runs to FinalAnswer.
   - `dotnet run --project src/BlueCode.Cli -- --model 35b "x" 2>&1; echo "Exit: $?"` prints "requires --with-35b" error, exits non-zero.
8. **Docs cleanliness:** `grep -c "Qwen 2.5\|qwen32b/\|qwen72b/" CLAUDE.md` returns 0; qwen35-install.md has STANDBY badge; single-model-eval.md cross-references Phase 19; bench.md sample gate uses 6/6 format.

</verification>

<success_criteria>

19-02 is complete when:
- [x] All 5 ROADMAP §Phase 19 SCs hold (SC1 inherited from 19-01; SC2 = baseline + bench/run.sh; SC3 = retirement aliases + default; SC4 = PathRetired + tryParseModelId; SC5 = docs)
- [x] SC6: tests in [258, 264] range Passed, 1 Failed (existing SmokeTests network), 0 Errored
- [x] bench/run.sh --gate exit 0 with GATE PASS (6/6)
- [x] 8-9 atomic commits land (one per Task, possibly skipping Task 9 if no fixup needed)
- [x] No git add . / git add -A used (CLAUDE.md commit protocol)
- [x] Each commit message follows `{type}(19-02): {summary}` shape per CLAUDE.md commit protocol

</success_criteria>

<output>
After completion, create `.planning/phases/19-qwen25-retirement/19-02-SUMMARY.md` with:
- One-paragraph summary of code/bench/docs changes
- Commit SHAs of each Task's commit
- Test count delta (e.g., 254/1/0 → 261/1/0)
- bench gate post-change result (6/6 PASS)
- Pointer to 19-01-RETIREMENT.md as the physical-state evidence and to this PLAN as the source-state evidence
- Status: "Phase 19 complete; ready for `/gsd:verify-work 19`."
</output>
