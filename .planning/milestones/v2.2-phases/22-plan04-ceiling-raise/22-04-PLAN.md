---
phase: 22-plan04-ceiling-raise
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - documentation/qwen35-122b-coding-eval.md
  - .planning/STATE.md
autonomous: true

must_haves:
  truths:
    - "bash bench/eval-qwen35-122b.sh --refactor produces refactor_orphan_count.txt containing '0'"
    - "refactor_multifile_diff.txt contains 'CORR-EVAL-02 PASS: 0 orphan' line"
    - "documentation/qwen35-122b-coding-eval.md §2.4 verdict line reads PASS with orphan_count=0"
    - "documentation/qwen35-122b-coding-eval.md §7 scorecard: Multi-file refactor row shows 5/5"
    - "documentation/qwen35-122b-coding-eval.md §7 Correctness subtotal shows 36/40"
    - "documentation/qwen35-122b-coding-eval.md §7 Total shows 87/100"
    - "documentation/qwen35-122b-coding-eval.md final line: **Total: 87/100, Recommendation: KEEP**"
    - "documentation/qwen35-122b-coding-eval.md §8 multi-file caveat removed or marked resolved"
    - "documentation/qwen35-122b-coding-eval.md §9 '5-step cap raised' item marked resolved"
    - "bash bench/run.sh --gate exits 0 with GATE PASS (7/7) after fixtures restored"
    - ".planning/STATE.md updated with CORR-EVAL-02 PASS observation note"
  artifacts:
    - path: "documentation/qwen35-122b-coding-eval.md"
      provides: "Updated eval doc with CORR-EVAL-02 PASS, 87/100 final verdict"
      contains: "Total: 87/100, Recommendation: KEEP"
    - path: ".planning/STATE.md"
      provides: "Observation note recording CORR-EVAL-02 PASS on 2026-04-28"
      contains: "CORR-EVAL-02 PASS\\|orphan_count=0"
  key_links:
    - from: "bash bench/eval-qwen35-122b.sh --refactor"
      to: "bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt"
      via: "run_refactor() writes orphan count before EXIT trap can restore fixtures; value must be 0"
      pattern: "refactor_orphan_count.txt"
    - from: "documentation/qwen35-122b-coding-eval.md §7 scorecard"
      to: "Correctness subtotal 36/40 and Total 87/100"
      via: "inline edit updating 0→5 for Multi-file refactor row; subtotal 31→36; total 82→87"
      pattern: "87/100"
    - from: "bash bench/run.sh --gate (mandatory final)"
      to: "GATE PASS (7/7)"
      via: "EXIT trap restores refactor_multifile fixtures on gate run; gate verifies all 7 fixtures pass"
      pattern: "GATE PASS.*7/7"
---

<objective>
Re-run CORR-EVAL-02 with the raised 10-step ceiling to verify the agent successfully completes the multi-file rename refactor (orphan_count=0). Update `documentation/qwen35-122b-coding-eval.md` to reflect the PASS: flip §2.4 verdict, re-aggregate §7 scorecard (36/40 Correctness; 87/100 Total), remove §8 multi-file caveat, mark §9 re-evaluation trigger resolved, update final verdict line. Run the mandatory final bench gate to confirm fixtures are restored and all 7 gate entries still pass.

Purpose: This plan closes Phase 22 and delivers the milestone goal. CORR-EVAL-02 FAIL with orphan_count=1 was the v2.1 data signal that motivated v2.2. A confirmed PASS here proves the ceiling raise was the correct and sufficient fix. The 87/100 eval doc verdict creates the empirical record for future milestones.

Output:
- Live CORR-EVAL-02 run artifact: `bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt` = "0"
- Updated `documentation/qwen35-122b-coding-eval.md` with all scorecard changes
- Updated `.planning/STATE.md` observation note
- Final `bash bench/run.sh --gate` confirming 7/7 PASS post-fixture-restore
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/22-plan04-ceiling-raise/22-RESEARCH.md
@.planning/phases/22-plan04-ceiling-raise/22-03-SUMMARY.md
@bench/baseline.json
</context>

<architectural_invariants>
1. **Fixture pre-flight (mandatory):** Before running `--refactor`, reset the refactor_multifile fixtures to their canonical (pre-refactor) state. The fixtures may be in a modified state from any prior run (v2.1's CORR-EVAL-02 FAIL run left them partially modified). Use `git checkout -- bench/fixtures/refactor_multifile/` before invoking `--refactor`. This is documented in RESEARCH.md §7: the `--refactor` handler does NOT reset fixtures itself; only `bench/run.sh`'s EXIT trap resets them, and that trap does not fire during a standalone `--refactor` invocation.

2. **orphan_count.txt capture timing:** `run_refactor()` writes `refactor_orphan_count.txt` BEFORE the handler returns, and BEFORE `bench/run.sh`'s EXIT trap fires (the trap fires on the gate run later). The count in that file reflects the actual post-refactor state of the fixtures. Do NOT re-run `--refactor` after a subsequent gate run (the gate's EXIT trap will restore `add` references, making a re-run a false start).

3. **CORR-EVAL-02 is all-or-nothing:** PASS = orphan_count=0 (5/5 pts). FAIL = orphan_count > 0 (0/5 pts). No partial credit. If orphan_count > 0, the scorecard update must NOT proceed; instead investigate why the agent still failed with 10-step budget and consider whether another ceiling or prompt iteration is needed.

4. **Eval doc edits are inline (no rewrite):** Only the specific lines identified in RESEARCH.md §8 are changed. The overall structure, formatting, and v2.1 data are preserved. Do not reformat sections or add new sections.

5. **bench/baseline.json byte-for-byte preserved:** The final gate run verifies fixtures and gate stability; it does NOT update the baseline.

6. **No src/ changes:** This plan makes zero source edits. The only file modifications are to `documentation/qwen35-122b-coding-eval.md` and `.planning/STATE.md`.

7. **Atomic commits per CLAUDE.md:** Stage files individually. NEVER `git add .` or `git add -A`. `.claude/` and `localLLM/` are intentionally untracked.
</architectural_invariants>

<out_of_scope>
- DO NOT rewrite or restructure the eval doc — inline edits only at the identified lines
- DO NOT update bench/baseline.json
- DO NOT modify src/ files
- DO NOT run --coldstart (Phase 23; separate optional phase)
- DO NOT run --full or --throughput or other eval handlers (only --refactor is needed here)
- DO NOT add new eval fixtures
- DO NOT add slash commands, compaction, sub-agents, streaming, thinking-mode-on
- DO NOT update CLAUDE.md §Bench section (it refers to the gate; bench gate is unchanged)
</out_of_scope>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight fixture reset; run CORR-EVAL-02 (--refactor); verify orphan_count=0</name>
  <files>(no persistent file modifications — bench/runs/ artifacts are gitignored)</files>
  <action>
**Step 1 — Confirm 122B service is responsive:**
```bash
curl -fsS http://127.0.0.1:8001/v1/models | jq '.data[].id'
```
If unreachable: `launchctl kickstart -k gui/$(id -u)/com.ohama.qwen122b` then poll until responsive.

**Step 2 — Pre-flight fixture reset (MANDATORY):**
```bash
git checkout -- bench/fixtures/refactor_multifile/
```
Verify the fixtures are in their canonical `add` state:
```bash
grep "let add\|Calculator\.add\|open Calculator" bench/fixtures/refactor_multifile/*.fs
```
Expected: several matches (Calculator.fs defines `let add`; Main.fs and Tests.fs call `Calculator.add`). If no matches, the `git checkout --` failed or the files are already in a post-refactor state — investigate.

**Step 3 — Run CORR-EVAL-02:**
```bash
bash bench/eval-qwen35-122b.sh --refactor
```
This runs the full agent loop against the fixture with the 10-step ceiling in place. Expected wall-clock: 25-40 seconds (7-8 steps × ~4-5s/step at 34.6 tok/s). If it takes > 120 seconds, the agent may be stuck — check `bench/runs/qwen35-eval-*/refactor_multifile_diff.txt` for the transcript.

**Step 4 — Locate artifacts:**
```bash
EVAL_DIR=$(ls -dt bench/runs/qwen35-eval-*/refactor_orphan_count.txt 2>/dev/null | head -1 | xargs dirname)
echo "EVAL_DIR=$EVAL_DIR"
```

**Step 5 — Verify PASS:**
```bash
cat "$EVAL_DIR/refactor_orphan_count.txt"
```
Must be `0`. If it is > 0, CORR-EVAL-02 has FAILED again. Do NOT proceed to Task 2 (eval doc update). Instead:
- Read `$EVAL_DIR/refactor_multifile_diff.txt` to understand where the agent stopped
- Check how many steps were used: count `[INF] Step` lines in the transcript
- If the agent hit MaxLoopsExceeded (10 steps) without completing: the task may need further ceiling increase or prompt iteration; escalate to user
- If the agent completed but missed some files: the task description may need adjustment (out of scope for v2.2)

```bash
grep "CORR-EVAL-02" "$EVAL_DIR/refactor_multifile_diff.txt"
```
Must contain `CORR-EVAL-02 PASS: 0 orphan 'add' references remain`.

**Step 6 — Inspect post-refactor file states (qualitative):**
```bash
cat bench/fixtures/refactor_multifile/Calculator.fs
cat bench/fixtures/refactor_multifile/Main.fs
cat bench/fixtures/refactor_multifile/Tests.fs
```
Expected: `add` replaced by `sum` (and `add3` by `sum3`) in all three files. No orphan `let add` or `Calculator.add` references. README.md should be unchanged.

Document the step count used and qualitative transcript summary for the SUMMARY.md.
  </action>
  <verify>
- `cat "$EVAL_DIR/refactor_orphan_count.txt"` = `0` (single zero character)
- `grep "CORR-EVAL-02 PASS" "$EVAL_DIR/refactor_multifile_diff.txt"` matches
- `grep "let add\|Calculator\.add" bench/fixtures/refactor_multifile/Calculator.fs bench/fixtures/refactor_multifile/Main.fs bench/fixtures/refactor_multifile/Tests.fs` returns nothing (agent removed all orphans)
- `test -f "$EVAL_DIR/refactor_multifile.meta"` (meta file exists with exit code + elapsed)
- `cat bench/fixtures/refactor_multifile/README.md | grep -c "rename\|sum\|add"` > 0 (README preserved)
  </verify>
  <done>
CORR-EVAL-02 PASS confirmed. orphan_count=0. Transcript captured in bench/runs/. Fixtures show successful rename in all 3 F# files. Phase 22 SC5 met.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update documentation/qwen35-122b-coding-eval.md — §2.4, §7, §8, §9, final line, header</name>
  <files>documentation/qwen35-122b-coding-eval.md</files>
  <action>
Read `documentation/qwen35-122b-coding-eval.md` in full before editing. Then make the following inline targeted edits. Use RESEARCH.md §8 as the precise guide for each edit site.

**EDIT 1 — §2.4 verdict line:**

Find the line that reads (approximately):
```
§2.4 verdict: FAIL — orphan_count=1 (5-step budget exhausted). Score: **0/5**.
```
Change to:
```
§2.4 verdict: PASS — orphan_count=0 (10-step budget sufficient). Score: **5/5**.
```

Also update the §2.4 body text (lines describing the v2.1 run) to reference the v2.2 re-run. Add a note that v2.1 produced orphan_count=1 (5-step budget exhausted); v2.2 re-run 2026-04-28 produced orphan_count=0 with 10-step ceiling. Reference the `bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt` artifact from Task 1 (substitute the actual timestamp from `$EVAL_DIR`).

**EDIT 2 — §7 Verdict scorecard — Multi-file refactor row:**

Find the table row:
```
| Correctness | Multi-file refactor (all-or-nothing) | 0 | 5 |
```
Change to:
```
| Correctness | Multi-file refactor (all-or-nothing) | 5 | 5 |
```

**EDIT 3 — §7 Verdict scorecard — Correctness subtotal row:**

Find the row:
```
| **Correctness subtotal** | | **31** | **40** |
```
Change to:
```
| **Correctness subtotal** | | **36** | **40** |
```

**EDIT 4 — §7 Verdict scorecard — Correctness dimension coverage row:**

Find the row:
```
| Correctness | 31/40 | 77.5% | YES |
```
Change to:
```
| Correctness | 36/40 | 90.0% | YES |
```

**EDIT 5 — §7 aggregate verdict / grand total:**

Find the line:
```
- Grand total: 31 + 20 + 25 + 6 = **82/100** → ≥80 band
```
Change to:
```
- Grand total: 36 + 20 + 25 + 6 = **87/100** → ≥80 band
```

(Note: 20 = Reliability, 25 = Performance, 6 = Usability — these are unchanged.)

**EDIT 6 — §8 Caveats — remove or replace multi-file caveat:**

Find caveat 6 (approximately):
```
6. **Multi-file refactor is a structural blueCode limit, not a model deficiency.** The 5-step PLAN-04
   hard cap makes any task requiring >5 steps impossible to complete in a single blueCode invocation.
```
Replace with:
```
6. **Multi-file refactor capability restored in v2.2.** The 5-step PLAN-04 hard cap was raised to 10 in v2.2 (2026-04-28). CORR-EVAL-02 re-run confirms PASS: orphan_count=0.
```

**EDIT 7 — §9 Re-evaluation triggers — mark item 6 resolved:**

Find item 6 (approximately):
```
6. **blueCode step limit change (PLAN-04)** — if the 5-step cap is raised, the multi-file refactor
   should be re-run and CORR-EVAL-02 re-scored.
```
Change to (using strikethrough Markdown):
```
6. ~~**blueCode step limit change (PLAN-04)**~~ — RESOLVED in v2.2 (ceiling raised to 10; CORR-EVAL-02 re-run 2026-04-28 → PASS orphan_count=0).
```

**EDIT 8 — Final verdict line:**

Find the final line (approximately):
```
**Total: 82/100, Recommendation: KEEP**
```
Change to:
```
**Total: 87/100, Recommendation: KEEP**
```

**EDIT 9 — Header fields:**

Near the top of the document, find the `**blueCode commit:**` field. Update it to the current HEAD commit hash:
```bash
git rev-parse HEAD
```
Use the full or shortened hash. Also update the `**Date:**` field if it says anything other than `2026-04-28`.

**After all edits, verify internal consistency:**
```bash
# Final verdict line strict format check (REQUIREMENTS.md PLAN-CAP-05 validation)
grep -E "^\*\*Total: 87/100, Recommendation: KEEP\*\*$" documentation/qwen35-122b-coding-eval.md
```
Must return exactly one match.

```bash
# Correctness subtotal
grep "36" documentation/qwen35-122b-coding-eval.md | grep -i "subtotal\|correctness"
```
Must show the updated subtotal.

```bash
# No remaining 82/100 in scorecard (final section)
grep "82/100" documentation/qwen35-122b-coding-eval.md | grep -v "v2\.1\|was\|previously\|prior\|old\|before"
```
Must return nothing (82/100 may appear in historical context within the document body, but the final scorecard line must say 87/100).

Atomic commit: `docs(22-04): update eval doc — CORR-EVAL-02 PASS; scorecard 82→87`
Stage exactly: `documentation/qwen35-122b-coding-eval.md`
  </action>
  <verify>
- `grep -E "^\*\*Total: 87/100, Recommendation: KEEP\*\*$" documentation/qwen35-122b-coding-eval.md` matches exactly once
- `grep "36/40" documentation/qwen35-122b-coding-eval.md` matches (Correctness subtotal)
- `grep "90.0%" documentation/qwen35-122b-coding-eval.md` matches (Correctness dimension coverage)
- `grep "Multi-file refactor.*5 | 5" documentation/qwen35-122b-coding-eval.md` matches (updated scorecard row)
- `grep "orphan_count=0.*10-step" documentation/qwen35-122b-coding-eval.md` matches (§2.4 updated)
- `grep "RESOLVED in v2.2" documentation/qwen35-122b-coding-eval.md` matches (§9 trigger marked)
- `grep "CORR-EVAL-02 PASS" documentation/qwen35-122b-coding-eval.md` matches (§8 caveat updated)
- `git log -1 --oneline` shows `docs(22-04): update eval doc — CORR-EVAL-02 PASS; scorecard 82→87`
  </verify>
  <done>
Eval doc updated across all 9 edit sites. Final scorecard line 87/100 confirmed. All in-scope section updates applied. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md; run final mandatory bench gate</name>
  <files>.planning/STATE.md</files>
  <action>
**Step 1 — Update STATE.md:**

Read `.planning/STATE.md` before editing.

In the "Empirical Baselines" section (or "Accumulated Context"), add or update the entry for CORR-EVAL-02:

Find the line:
```
- **5-step PLAN-04 ceiling structurally blocks multi-file refactor** — CORR-EVAL-02 FAIL with orphan_count=1; first v2.2 candidate, data-driven; needs Core change (Domain.fs Plan validator)
```

Replace with:
```
- **Multi-file refactor capability confirmed (v2.2)** — CORR-EVAL-02 PASS: orphan_count=0 (2026-04-28). Ceiling raised 5→10 in Phase 22. Eval doc verdict 82→87/100. Bench gate 7/7 PASS preserved.
```

Also update the "Current Position" section to reflect Phase 22 completion:
- Update `Phase: 22` status from "not started" / "in progress" to "complete"
- Update `Plan: 4 of 4 complete`
- Update `Status: Ready for Phase 23 (optional cold-start) or v2.2 milestone closeout`

Atomic commit: `chore(22-04): update STATE.md with CORR-EVAL-02 PASS and Phase 22 completion`
Stage exactly: `.planning/STATE.md`

**Step 2 — Run final mandatory bench gate:**

This is the conclusive gate run for Phase 22. The `--refactor` run in Task 1 modified the fixtures (the agent renamed `add` to `sum`). `bench/run.sh`'s EXIT trap must restore them. Running the gate triggers that restoration.

```bash
bash bench/run.sh --gate
echo "gate_exit=$?"
```
Expected: exit 0, `GATE PASS (7/7)`.

**Verify fixtures restored after gate:**
```bash
git diff bench/fixtures/refactor_multifile/Calculator.fs
git diff bench/fixtures/refactor_multifile/Main.fs
git diff bench/fixtures/refactor_multifile/Tests.fs
```
All must be empty (EXIT trap restored canonical `add` state).

```bash
grep "let add\|Calculator\.add" bench/fixtures/refactor_multifile/Calculator.fs bench/fixtures/refactor_multifile/Main.fs bench/fixtures/refactor_multifile/Tests.fs
```
Must show matches (canonical fixtures have `add`; confirmed restored).

**Baseline sanity:**
```bash
git diff bench/baseline.json
```
Must be empty.

**Final source sanity:**
```bash
git diff src/
```
Must be empty (no src/ changes in this plan).

**Record final gate results for SUMMARY:** Note the 7/7 PASS, per-fixture step counts, and that fixtures were confirmed restored by the EXIT trap.
  </action>
  <verify>
- `grep "CORR-EVAL-02 PASS" .planning/STATE.md` matches
- `grep "orphan_count=0" .planning/STATE.md` matches
- `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)`
- `git diff bench/fixtures/refactor_multifile/` produces empty output (fixtures restored)
- `grep "let add" bench/fixtures/refactor_multifile/Calculator.fs` matches (canonical state restored)
- `git diff bench/baseline.json` produces empty output
- `git diff src/` produces empty output
- `git log -1 --oneline` shows `chore(22-04): update STATE.md with CORR-EVAL-02 PASS and Phase 22 completion`
  </verify>
  <done>
STATE.md updated. Final bench gate 7/7 PASS. Fixtures restored by EXIT trap. Baseline preserved. Phase 22 all success criteria satisfied.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (run after all tasks complete):**

1. `cat bench/runs/qwen35-eval-*/refactor_orphan_count.txt` (latest run) = `0`.
2. `grep "CORR-EVAL-02 PASS: 0 orphan" bench/runs/qwen35-eval-*/refactor_multifile_diff.txt` (latest run) matches.
3. `grep -E "^\*\*Total: 87/100, Recommendation: KEEP\*\*$" documentation/qwen35-122b-coding-eval.md` matches exactly once.
4. `grep "36/40" documentation/qwen35-122b-coding-eval.md` matches (Correctness subtotal).
5. `grep "Multi-file refactor.*5 | 5" documentation/qwen35-122b-coding-eval.md` matches.
6. `grep "RESOLVED in v2.2" documentation/qwen35-122b-coding-eval.md` matches (§9 trigger).
7. `grep "CORR-EVAL-02 PASS" .planning/STATE.md` matches.
8. `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)`.
9. `git diff bench/fixtures/refactor_multifile/` empty (EXIT trap restored fixtures).
10. `git diff bench/baseline.json` empty.
11. `git diff src/` empty.

Maps to Phase 22 SC5 (CORR-EVAL-02 PASS; orphan_count=0; PASS line in transcript).
Maps to Phase 22 SC6 (eval doc updated; §2.4 PASS; §7 re-aggregated 36/40 Correctness, 87/100 Total; §8 caveat removed; §9 trigger resolved; final line exact-format match).
</verification>

<success_criteria>
- [ ] `bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt` = `0`
- [ ] `bench/runs/qwen35-eval-<ts>/refactor_multifile_diff.txt` contains `CORR-EVAL-02 PASS: 0 orphan 'add' references remain`
- [ ] `documentation/qwen35-122b-coding-eval.md` final line: `**Total: 87/100, Recommendation: KEEP**` (strict format match)
- [ ] `documentation/qwen35-122b-coding-eval.md` §2.4 verdict: PASS with orphan_count=0
- [ ] `documentation/qwen35-122b-coding-eval.md` §7 Correctness subtotal: 36/40
- [ ] `documentation/qwen35-122b-coding-eval.md` §7 Multi-file refactor row: 5/5
- [ ] `documentation/qwen35-122b-coding-eval.md` §8 multi-file caveat: replaced with resolved note
- [ ] `documentation/qwen35-122b-coding-eval.md` §9 "5-step cap raised" trigger: marked resolved
- [ ] `.planning/STATE.md` updated with CORR-EVAL-02 PASS observation
- [ ] `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)` (final post-fixture-restore verification)
- [ ] `bench/fixtures/refactor_multifile/` restored to canonical `add` state (EXIT trap confirmed)
- [ ] `bench/baseline.json` byte-for-byte unchanged
- [ ] No `src/` modifications by this plan
- [ ] Three atomic commits: `docs(22-04): ...`, `chore(22-04): ...`, plus plan-meta
</success_criteria>

<output>
After completion, create `.planning/phases/22-plan04-ceiling-raise/22-04-SUMMARY.md` documenting:
- CORR-EVAL-02 result: orphan_count, step count used by agent, wall-clock elapsed
- Qualitative agent behavior: did it read all 4 files? edit all 3 correctly? how many steps total?
- Eval doc sections updated (§2.4 verdict, §7 scorecard rows, §8 caveat, §9 trigger, final line)
- Final gate result (expected: 7/7 PASS)
- Fixture restore confirmation (EXIT trap verified)
- Phase 22 completion statement: "All 6 success criteria satisfied (SC1-SC6)"
- Phase 23 note: optional cold-start — separate phase, user opt-in required
- Plan-meta commit: `docs(22-04): complete re-evaluation plan` staging only PLAN.md + SUMMARY.md
- Phase completion commit: `docs(22): complete Phase 22 PLAN-04 Ceiling Raise` staging ROADMAP.md + STATE.md + REQUIREMENTS.md (update phase statuses to complete)
</output>
