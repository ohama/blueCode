# Milestone v2.2: Multi-file Capability

**Status:** ✅ SHIPPED 2026-04-28
**Phases:** 22 + 23
**Total Plans:** 5 (4 in Phase 22; 1 in Phase 23)

## Overview

v2.2 was scoped from v2.1 audit's CORR-EVAL-02 FAIL constraint discovery — the first **data-driven** v2.2 candidate (vs speculative deferred-list draining). Goal: raise the PLAN-04 5-step ceiling that v2.1 hypothesized as the structural blocker for genuine multi-file refactor; verify by re-running CORR-EVAL-02 to PASS; optionally close cold-start measurement deferred from v2.1.

Outcome: Architectural ceiling raised cleanly (5→10 across 4 source files; tests +2; bench gate 7/7 PASS held throughout). CORR-EVAL-02 PASS gate **NOT met** — re-ran twice with different READMEs, both produced textually identical step-5 thoughts (`add3→sum3` only, ignoring `add→sum`). User-accepted Option C: persistent extraction bias on shared-prefix function names surfaced as v2.3 first candidate. Cold-start measurement (Phase 23) added +5 to Performance dimension via different path. Final verdict **82 → 87/100, KEEP**.

The CORR-EVAL-02 finding is a **constructive empirical result**: it disproved the v2.1 hypothesis that ceiling alone was the constraint, and surfaced a deeper model behavior pattern (persistent extraction bias) as the data-driven first v2.3 candidate.

## Phases

### Phase 22: PLAN-04 Ceiling Raise

**Goal:** Raise step ceiling from 5 to 10 via config-driven seam (single source of truth across `PlanValidator.validatePlan` + `AgentLoop.runLoop`); update system prompt; add boundary tests; verify 7/7 bench gate held; re-run CORR-EVAL-02 producing orphan_count=0 PASS; update eval doc verdict 82 → 87.

**Depends on:** v2.1 milestone (eval doc + bench gate 7/7 baseline + `bench/eval-qwen35-122b.sh --refactor` harness)

**Plans:** 4 (sequential — each depends on prior)

Plans:
- [x] 22-01: Core change — `PlanValidator.MaxPlanSteps` 5→10 + `CompositionRoot bootstrap MaxLoops = 10`; Domain.fs comment + PlanValidator.fs docstring updated; PlanValidatorTests boundary tests (10 PASS, existing 6-step test re-pointed to 11); CompositionRootTests assertion updated; AgentLoopTests new MaxLoopsExceeded-at-10 test using `{ testConfig with MaxLoops = 10 }`. Tests 282 → 284. (PLAN-CAP-01)
- [x] 22-02: Adapter strings — `CompositionRoot.fs:98` system prompt "1-5 steps" → "1-10 steps" + 1-sentence usage guidance; `AgentLoop.fs:502` `[PLAN INVALID]` "max 5 steps" → "max 10 steps"; `Rendering.fs:114` MaxLoopsExceeded "5 steps" → "10 steps"; `RenderingTests.fs:73` assertion updated. T6 baseline held first try (no iteration needed). System prompt 695 chars (≤900 budget). (PLAN-CAP-02)
- [x] 22-03: Bench gate regression hold — `bash bench/run.sh --gate` exit 0 + GATE PASS (7/7); per-fixture step counts unchanged (T6=5/5, W1=3/3, W2=3/3, T1=1/3, T5=3/4, B2=2/3, MT=2/4). No code changes; verification-only plan. (PLAN-CAP-03)
- [x] 22-04: Re-run CORR-EVAL-02 + update eval doc — TWO attempts produced FAIL with orphan_count=1; agent used 8/10 steps in both attempts (no MaxLoopsExceeded); textually identical step-5 thoughts across both attempts. Stage-2 attempt rewrote README to enumerate both rename targets explicitly (902 → 2128 chars); model behavior unchanged. Eval doc §2.4/§8/§9 updated with two-stage finding documentation; verdict line stays 82/100 KEEP per Option C. (PLAN-CAP-04 partial; PLAN-CAP-05 partial)

**Phase 22 outcome:** SC1-SC4 fully satisfied (architectural deliverables shipped clean). SC5/SC6 partial-by-design per user-accepted Option C (persistent extraction bias finding documented as v2.3 candidate). Eval doc verdict at end of Phase 22 stays at 82/100.

### Phase 23: Cold-start Empirical (deferred from v2.1)

**Goal:** Execute `bash bench/eval-qwen35-122b.sh --coldstart` once during user-accepted disruption window; flip eval doc §3.3 from "deferred per scope" to actual measurement; update §7 Performance subtotal.

**Depends on:** Phase 22 (eval doc base state); user opt-in for ~3 min disruption

**Plans:** 1

Plans:
- [x] 23-01: Cold-start single execution — pre-flight harness fix (`mkdir` before `tee` under `set -euo pipefail`; commit `4bcd8a4`; **third macOS bash-strict-mode pattern this milestone series**); --coldstart cycle: 37s to model-ready (PID 44880 → 10536 confirms genuine process replacement; warm OS file cache case; 1s first-generation post-recovery confirms full load). Eval doc §3.3 flipped from "deferred per scope" to actual measurement; §7 Performance row 0/5 → 5/5; subtotal 20/25 → 25/25; coverage 80% → 100%; total 82 → 87; final scorecard line `**Total: 87/100, Recommendation: KEEP**`; §8 Caveat #1 updated. Bench gate 7/7 PASS post-recovery. (COLD-EVAL-01)

**Phase 23 outcome:** Full closure. Surprising finding: 37s vs v2.0's "up to 240s" estimate (~6× faster); attributable to warm OS file cache (kernel preserves file pages even when kickstart kills the process). v2.0 estimate stays valid for first-boot (truly cold disk cache) scenarios.

**Plans NOT created formally:**
- Phase 23 has no `23-01-PLAN.md` (orchestrator created SUMMARY.md inline; same pattern as v2.0 Phase 20's missing VERIFICATION.md)
- Phase 23 has no `23-VERIFICATION.md` (SUMMARY.md SC table substitutes)

## Architectural Invariants Preserved

1. **Core purity** — `src/BlueCode.Core/{Domain,PlanValidator,AgentLoop}.fs` no Serilog/Spectre/Argu/HttpClient creep
2. **`task {}` only** in Core (no `async {}` literal; CI grep `scripts/check-no-async.sh` clean)
3. **Bench gate stability** — 7/7 PASS throughout Phase 22 + Phase 23
4. **`bench/baseline.json` byte-for-byte preserved**
5. **No `bench/run.sh` body changes** (only line-18 EXIT trap from v2.1-03 which is preserved)
6. **Role=User invariant** unchanged
7. **Test count** 282 → 284 (+2 boundary tests; 1 in-place updated)
8. **`git diff src/`** post-v2.2: only Phase 22 changes (5 source files)
9. **Atomic commits** — 19 commits across milestone with `{type}({phase}-{plan}): {name}` format

## Final Verdict Scorecard

| Dimension | Score | Max | Pct | Δ from v2.1 |
|-----------|-------|-----|-----|-------------|
| Correctness | 31 | 40 | 77.5% | unchanged |
| Performance | 25 | 25 | 100.0% | **+5** (cold-start) |
| Reliability | 25 | 25 | 100.0% | unchanged |
| Coding quality | 6 | 10 | 60.0% | unchanged |
| **Total** | **87** | **100** | **87%** | **+5** |

**Aggregate verdict: KEEP** — empirically useful for daily F# coding via blueCode (≥80 threshold; all dimensions ≥60%; multi-turn stable through N=7; HumanEval+ chat 93.9%).

---

## Milestone Summary

**Decimal Phases:** None.

**Key Decisions:**

- **v2.2 first candidate is data-driven** (vs deferred-list draining) — CORR-EVAL-02 FAIL surfaced PLAN-04 ceiling as the structural blocker. Outcome: ✓ Good — milestone scoped from real measurement, not speculation. Discipline preserved.
- **Option 1 design (independent constants)** for ceiling raise — `PlanValidator.MaxPlanSteps` and `AgentConfig.MaxLoops` stay separate per existing PlanValidator.fs:36-40 docstring rationale (validator runs at JSON parse time without AgentConfig in scope). Outcome: ✓ Good — no new shared module; surgical changes only.
- **Option C (accept partial closure + document finding)** for CORR-EVAL-02 FAIL x2 — user explicitly chose to close v2.2 with the discovered model behavior pattern recorded as v2.3 candidate rather than fighting the symptom (Option A README rewrite, Option B system prompt guidance, Option D fixture redesign). Outcome: ✓ Good — intellectually honest; verdict went UP via Phase 23 (different path); v2.3 has cleanly-scoped first candidate.
- **Phase 23 inline (no formal PLAN/VERIFICATION)** — 1-plan operational phase fully self-verified via SUMMARY.md SC table + inline gate run. Outcome: ⚠ Process drift — same pattern as v2.0 Phase 20; non-blocking; flagged in audit.
- **Two-stage finding preserved in eval doc** (§2.4 keeps FAIL with two-stage description; §8 Caveat #6 documents the empirical chain) — design choice, not drift. Outcome: ✓ Good — historical record preserved for v2.3 scoping.

**Issues Resolved:**

- v2.0 cold-start "up to 240s" estimate empirically refuted for warm-cache common case (37s actual). v2.0 estimate stays valid for first-boot pristine case.
- v2.1 hypothesis "5-step ceiling = sole constraint for multi-file refactor" empirically disproved. Constraint discovery #2: comprehension layer is the new bottleneck.
- Architectural ceiling unlock: blueCode can now plan and execute up to 10-step coherent sequences (where the model's planning is correct).

**Issues Deferred:**

- **CORR-EVAL-02 PASS** gate (PLAN-CAP-04 SC) — ceiling necessary but insufficient; awaits v2.3 comprehension-layer intervention.
- **Pristine cold-start measurement** (post-reboot, truly cold disk cache) — warm-cache 37s documented; pristine case requires separate scheduled disruption window.
- **Idiomatic F# generation improvement** — Coding-quality 1/5 sub-score may be artifact of Python-task transcript selection; needs F# transcripts before drawing conclusions. v2.3 candidate.

**Technical Debt Incurred:**

- **Phase 23 missing formal VERIFICATION.md** (process drift; SUMMARY.md SC table substitutes)
- **Phase 23 missing formal PLAN.md** (orchestrator created SUMMARY.md inline for 1-plan operational phase)

**Auto-fixed deviations during execution:**

- `4bcd8a4` `fix(23-01): move mkdir before tee in run_coldstart to satisfy set -euo pipefail` — third macOS bash-strict-mode pattern this milestone series (after 21-04's set-e/dotnet-exit and grep-c-pipefail bugs). Pattern: under `set -euo pipefail`, any I/O command must verify its target dir exists first.

**v2.3 candidates surfaced (from v2.2 findings):**

1. **COMP-BIAS-01 (highest priority, data-driven)** — Persistent extraction bias on shared-prefix function names. Two CORR-EVAL-02 runs with completely different READMEs produced textually identical step-5 thoughts. Needs system prompt enumeration guidance + few-shot multi-file examples + plan-mode pre-flight rename-target enumeration.
2. **IDIOMATIC-FS-01 (medium)** — Coding-quality 1/5 sub-score may be transcript-selection artifact; needs F# task fixtures.
3. **COLDSTART-PRISTINE-01 (low)** — post-reboot pristine cold-start untested.
4. **STM-01 (low; deferred 8x)** — defer pattern is itself the signal.

**Audit:** `/gsd:audit-milestone` ran post-Phase-23 (status: passed). 4/6 requirements fully + 2 partial-by-design (CAP-04/05 user-accepted Option C). 9/9 plan-integration edges PASS. 4/4 E2E flows. 12/12 documentation cross-references. Bench gate 7/7 PASS. Tech debt aggregated into `.planning/milestones/v2.2-MILESTONE-AUDIT.md`.

---

*For current project status, see .planning/PROJECT.md (post-v2.2).*
*Archived: 2026-04-28 as part of v2.2 milestone completion*
