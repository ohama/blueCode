# Requirements Archive: v2.3 Comprehension Layer

**Archived:** 2026-04-29
**Status:** ✅ SHIPPED

This is the archived requirements specification for v2.3.
For current requirements, see `.planning/REQUIREMENTS.md` (created for next milestone via `/gsd:new-milestone`).

---

# Requirements: blueCode v2.3 Comprehension Layer

**Defined:** 2026-04-28
**Core Value:** Mac 로컬 Qwen 3.5 122B를 strong-typed F# agent loop로 **empirically** 안정적으로 돌린다 (post-v2.2 verdict 87/100 KEEP; v2.3 unlocks multi-file refactor end-to-end via comprehension-layer intervention)
**Milestone goal:** Resolve the persistent extraction bias on shared-prefix function names that v2.2 audit surfaced as the new bottleneck (CORR-EVAL-02 FAIL x2 with textually identical step-5 thoughts across two completely different READMEs). Multi-prong intervention (P1 + P2 + P3) attacking comprehension layer at three angles. Verify by re-running CORR-EVAL-02 to PASS (orphan_count=0); flips Correctness 31/40 → 36/40 → Total 87 → 92.

## v2.3 Requirements (1 candidate, 6 requirements, 2 categories)

### Comprehension intervention (4 reqs)

- [x] **COMP-01**: System prompt instructs agent to enumerate ALL rename targets from spec before editing (P1 prong) — ✓ delivered in Plan 24-01 (`planSystemPromptSuffix` 695→879 chars; bench gate 7/7 PASS preserved)
  - **Outcome:** Directive added at end of `planSystemPromptSuffix` in `src/BlueCode.Cli/CompositionRoot.fs`. Originally plan-mode-only; **migrated to `defaultSystemPrompt` in Phase 27-01** to reach agent-loop path after Phase 26 BLOCKED exposed architectural scope mismatch. Final placement: `defaultSystemPrompt` (967 chars post-migration; 783 → 967). Conditional clause "When the task requires renaming or restructuring multiple symbols..." dormant for all 7 bench gate fixtures (no `rename`/`restructur` keywords). `grep -F "list ALL targets explicitly" src/BlueCode.Cli/CompositionRoot.fs` matches.

- [x] **COMP-02**: Plan-mode prompt includes 1-2 inline few-shot examples of correct multi-file refactor plans (P2 prong) — ✓ delivered in Plan 24-02 (`planSystemPromptSuffix` 879→1183 chars; shared-prefix `add`/`add3` example)
  - **Outcome:** 3-line block (Example/Targets/Steps format) added to `planSystemPromptSuffix`. P2 stays plan-mode-only post-Phase-27 migration (its `Targets:`/`Steps:` notation is plan-mode-specific; out-of-scope per ROADMAP §Phase 27 guardrails). Final state: `planSystemPromptSuffix` 999 chars (post-Phase-27 P1 removal); P2 example intact. `grep -F "Example:" src/BlueCode.Cli/CompositionRoot.fs` matches.

- [x] **COMP-03**: Plan validator pre-flight pass checks user-prompt rename targets are enumerated as plan steps (P3 prong) — ✓ delivered in Plan 25-01 (`checkRenameTargetsEnumerated` heuristic + `validatePlan` signature change)
  - **Outcome:** New function `checkRenameTargetsEnumerated: userPrompt: string -> Plan -> Result<Plan, AgentError>` in `src/BlueCode.Core/PlanValidator.fs:108`; bound into `validatePlan` chain at line 143; called from `AgentLoop.fs:484` inside `runPlanTurn`. Heuristic regex `\brename\s+\`?([A-Za-z_]\w+)\`?\s+to\s+\`?([A-Za-z_]\w+)\`?` (case-insensitive). Coverage check via `_raw` JSON `old_string` field on `edit_file` steps; only `edit_file` counts (write_file/grep_search/read_file return false). Malformed JSON returns false (defensive).
  - **Decision Note (2026-04-28): Interpretation B chosen.** The "new PlanInvalid reason" is encoded as a structured substring of the existing `PlanInvalid of detail: string` case in `Domain.fs` (e.g., `"rename targets not enumerated: add, add3"`) rather than as a new `AgentError` DU variant. Avoids compile cascade across `Rendering.fs:120` and `AgentLoop.buildCorrection`. Same observable LLM-correction behavior; smaller diff. `Domain.fs` / `Rendering.fs` / `buildCorrection` byte-for-byte unchanged across Phase 25 (verified empirically by 25-VERIFICATION.md).
  - **Note (post-Phase-27):** P3 PlanValidator only runs in `runPlanTurn` (plan-mode); does NOT reach the CORR-EVAL-02 eval harness (which uses agent-loop). P3's contribution to v2.3 was structural (validator now exists for plan-mode invocations) rather than directly responsible for the CORR-EVAL-02 PASS — that came from Phase 27's P1 migration to `defaultSystemPrompt`.

- [x] **COMP-04**: Tests + bench gate regression hold across all three prongs — ✓ delivered across Phase 24 (bench gate 7/7 PASS post-P1+P2), Phase 25 (bench gate 7/7 PASS post-P3; test count 284→287 via 3 new boundary tests), Phase 27 (bench gate 7/7 PASS post-P1-migration + final gate post-eval)
  - **Outcome:** All 7 bench fixtures (W1/W2/B2/T1/T5/T6/MT) within v2.2 baseline_max throughout the milestone. `bench/baseline.json` byte-for-byte preserved (`git diff milestone-v2.2 HEAD -- bench/baseline.json` empty). Test count 284 → 287 (+3 PlanValidator boundary tests in Phase 25-02; Phases 24/26/27 added zero tests).

### Re-evaluation (2 reqs)

- [x] **COMP-05**: CORR-EVAL-02 re-run produces orphan_count=0 (PASS) — ✓ delivered in Plan 27-02 (was scoped to Phase 26; Phase 26 BLOCKED → reassigned to Phase 27)
  - **Outcome:** `bench/runs/qwen35-eval-20260429-105907/refactor_orphan_count.txt` contains `0`; PASS verdict line `CORR-EVAL-02 PASS: 0 orphan 'add' references remain` in `refactor_multifile_diff.txt`. PASS achieved on attempt 1 of 3 with kickstart pre-flight (`launchctl kickstart -k gui/501/com.ohama.qwen122b`) clearing KV cache contamination. P1 effect directly observable in step-2 thought: "I need to rename `add` to `sum` and `add3` to `sum3` across three files" — both targets enumerated explicitly before edits.
  - **Note:** Phase 26 attempted this requirement first and BLOCKED (3 FAIL attempts; new hallucination failure mode + extraction bias when prongs didn't reach agent-loop). Phase 26's BLOCKED record (commit `7837ad5`, 26-VERIFICATION.md status:blocked) preserved as historical diagnostic. Phase 27 closed the architectural gap and delivered COMP-05.

- [x] **COMP-06**: Eval doc updated; scorecard re-aggregated (Total 87 → 92) — ✓ delivered in Plan 27-03 (was scoped to Phase 26; reassigned to Phase 27)
  - **Outcome:** `documentation/qwen35-122b-coding-eval.md` 11 edit sites applied (line numbers from 26-RESEARCH.md Q1 reused verbatim; substituted RUN_TS=20260429-105907 in §2.4 artifact block; §8 Caveat 6 reframed to "four-stage progression v2.1→v2.2→v2.3→v2.3+27, RESOLVED in v2.3 Phase 27" preserving historical evidence; §9 Item 8 marked **RESOLVED in v2.3 (Phase 27)**). Final scorecard line `**Total: 92/100, Recommendation: KEEP**` strict-format regex match confirmed (`grep -E "^\*\*Total: 92/100, Recommendation: KEEP\*\*$" documentation/qwen35-122b-coding-eval.md` exits 0). Zero orphan `87/100` references remain. §3.3 cold-start row UNCHANGED at 5/5 (Performance subtotal 25/25 preserved). §6.3 cloud non-goal preserved.

## Out of Scope (preserved)

v1 + v2.0 + v2.1 + v2.2 boundaries unchanged. v2.3 explicitly excluded:

- IDIOMATIC-FS-01 (F# style hints + few-shot for idiomatic F# generation) — v2.4+ candidate
- COLDSTART-PRISTINE-01 (post-reboot pristine cold-start) — v2.4+ candidate (low urgency)
- Slash commands (`/sessions`, `/plan`, `/clear`) — v2.4+ candidate (observation-driven)
- Compaction (auto-snip at 80% max_model_len) — v2.4+ candidate
- Sub-agent delegation (Agent tool) — v2.4+ candidate
- Plan-mode bench fixture (mocked-IKeyReader) — v2.4+ candidate
- Thinking-mode-on (`<think>` consumption) — defer indefinitely
- Native OpenAI `tool_calls` — v3.0 territory
- Streaming inference (STM-01) — deferred 9th cycle
- Cloud comparison (Claude/GPT-4) — deliberate non-goal preserved from v2.1 §6.3
- Re-eval beyond CORR-EVAL-02 — other dimensions stay at v2.2 baselines
- `bench/baseline.json` modifications — eval observational; gate baseline preserved byte-for-byte
- Multi-platform (Windows/Linux) — Mac-only ethos preserved

## Future Requirements (v2.4+ candidates)

Tracked for awareness; not pulled into v2.3.

- **IDIOMATIC-FS-01** (medium) — F# style guide hints in system prompt; few-shot examples; re-run §5 of eval to verify Coding-quality 1/5 → 3-5/5
- **COLDSTART-PRISTINE-01** (low) — Post-reboot pristine cold-start measurement
- **SLASH-01** (v2.4+ candidate) — `/sessions`, `/plan`, `/clear` slash commands inside REPL
- **COMPACT-01** (v2.4+ candidate) — Auto-compaction when session approaches 80% of `max_model_len`
- **SUBAG-01** (v2.4+ candidate) — Sub-agent delegation via Agent tool
- **PLAN-MODE-BENCH-01** (v2.4+ candidate) — Plan-mode bench fixture via mocked-IKeyReader
- **STM-01** (deferred 9x) — SSE token streaming in blueCode runtime

Plus newly-surfaced from v2.3:

- **AGENT-LOOP-FEW-SHOT-01** (medium) — Migrate or adapt P2 (currently plan-mode-only) for agent-loop mode if Coding-quality or future eval surfaces a need. Phase 27 deliberately left P2 in plan-mode (its `Targets:`/`Steps:` format is plan-mode-specific). If observation surfaces value, design an agent-loop-friendly variant.
- **HARNESS-AUDIT-01** (low) — `bench/eval-qwen35-122b.sh` accumulated three macOS-bash-strict-mode patches across v2.1+v2.2+v2.3 (set-e/dotnet-exit; grep-c-pipefail double-output; mkdir-before-tee; orphan-grep-zero-match-exit-1). Worth a one-pass audit + howto entry codifying the pattern.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| COMP-01 | Phase 24-01 → migrated to `defaultSystemPrompt` in Phase 27-01 | ✓ Complete (2026-04-28; migrated 2026-04-29) |
| COMP-02 | Phase 24-02 (stays plan-mode-only) | ✓ Complete (2026-04-28) |
| COMP-03 | Phase 25-01 (Interpretation B) | ✓ Complete (2026-04-29) |
| COMP-04 | Phase 24 + 25 + 27 (regression hold per phase) | ✓ Complete (2026-04-29) |
| COMP-05 | Phase 27-02 (was Phase 26-01 — superseded) | ✓ Complete (2026-04-29) |
| COMP-06 | Phase 27-03 (was Phase 26-01 — superseded) | ✓ Complete (2026-04-29) |

**Coverage:** 6/6 v2.3 requirements ✓

---

## Milestone Summary

**Shipped:** 6 of 6 v2.3 requirements
**Adjusted:** COMP-01 placement migrated mid-milestone — originally landed in `planSystemPromptSuffix` (Phase 24-01); migrated to `defaultSystemPrompt` (Phase 27-01) after Phase 26 BLOCKED exposed architectural scope mismatch. Substantively the same directive; different home for different reach.
**Reassigned:** COMP-05 + COMP-06 originally mapped to Phase 26 (Re-Evaluation); Phase 26 BLOCKED → Phase 27 took over delivery. Phase 26 stays as historical record.
**Dropped:** None.

---

*Archived: 2026-04-29 as part of v2.3 milestone completion*
