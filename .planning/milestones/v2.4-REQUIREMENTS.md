# Requirements Archive: v2.4 Coding Quality

**Archived:** 2026-04-29
**Status:** ✅ SHIPPED

This is the archived requirements specification for v2.4.
For current requirements, see `.planning/REQUIREMENTS.md` (created for next milestone via `/gsd:new-milestone`).

---

# Requirements: blueCode v2.4 Coding Quality

**Defined:** 2026-04-29
**Core Value:** Mac 로컬 Qwen 3.5 122B를 strong-typed F# agent loop로 **empirically** 안정적으로 돌린다 (post-v2.3 verdict 92/100 KEEP; v2.4 closes the only ≤60%-threshold dimension via measurement-first F# fixture eval)
**Milestone goal:** Resolve the Coding-quality 6/10 (Idiomatic F# 1/5) sub-score on the eval scorecard. v2.2/v2.3 audit hedged that the 1/5 may be a Python-transcript artifact. Measurement-first: build F# task fixtures, measure idiomatic-F# generation in agent-loop mode, then intervene only if data justifies.

## v2.4 Requirements (3 categories, 7 requirements; 5 unconditional + 2 conditional)

### F# Coding-quality Measurement (3 reqs)

- [x] **FS-EVAL-01**: 3-5 F# task fixtures created under `bench/fixtures/fs_idiomatic/` — ✓ delivered in Plan 28-02 (commit `25eb35d`). 3 fixtures: `pipeline.{task.md,fs}`, `dupatternmatch.{task.md,fs}`, `optionhandling.{task.md,fs}`. Each `.task.md` ≤500 bytes (472, 496, 495); each `.fs` skeleton compiles standalone via `failwith "TODO"` body.
- [x] **FS-EVAL-02**: New `--fs-idiomatic` mode in `bench/eval-qwen35-122b.sh` runs fixtures through agent-loop mode + captures transcripts — ✓ delivered in Plan 28-03 (commit `b7611a8`) + Plan 28-04 (commit `1413111`). `run_fs_idiomatic()` at lines 328-391 mirrors `run_refactor()` template; per-fixture loop with `git checkout` between fixtures + reminder-only kickstart pre-flight + transcript/diff/meta capture; HTTP-only invariant preserved.
- [x] **FS-EVAL-03**: Eval doc §5 + §7 re-scored with new F#-fixture evidence — ✓ delivered in Plan 28-04 (scoring; commit `1413111`) + Plan 28-05 (eval doc edits; commit `a36cc35`). 7 edit sites applied. Score: §5 Idiomatic F# 1/5 → 5/5; §7 Coding-quality subtotal 6/10 → 10/10; Grand total 92/100 → 96/100 KEEP. Strict-format final scorecard line `^\*\*Total: 96/100, Recommendation: KEEP\*\*$` regex match confirmed.

### F# Style Intervention (2 reqs, conditional — SKIPPED)

- [ ] **FS-INTERVENE-01** (conditional): F# style hint added to `defaultSystemPrompt` — **SKIPPED** (Phase 28 `passed_disprove_1of5` classification; `mapped_score 5/5 ≥ 3` triggered Path A disprove; conditional path not triggered; never executed)
- [ ] **FS-INTERVENE-02** (conditional): F# fixtures re-run post-intervention — **SKIPPED** (same reason)

### Harness Audit + Regression Hold (2 reqs)

- [x] **HARNESS-01**: `bench/eval-qwen35-122b.sh` audited; macOS bash-strict-mode patterns codified into `documentation/howto/macos-bash-strict-mode-patterns.md` — ✓ delivered in Plan 28-01 (commits `94d905c` + `280677a`). Howto with 5 patterns (4 originally planned + Pattern 5 found during audit pass at line 378 of `bench/eval-qwen35-122b.sh` — real bug, auto-fixed). Commit refs: `a6159c4` (mkdir-before-tee; corrected from earlier draft `4bcd8a4` which does not exist), `9f8e06e` (grep-cE zero-match), `94d905c` (Pattern 5 grep-oE session-id capture), v2.1 21-04 commits (set-e/dotnet-exit + grep-c/pipefail).
- [x] **REGRESSION-01**: Bench gate 7/7 PASS preserved through entire milestone; `bench/baseline.json` byte-equal — ✓ delivered across Phase 28 (per-phase 7/7 PASS verified after each plan; 28-06 explicit gate run) + Phase 30 (final milestone-wide gate; 30-VERIFICATION.md must-have #1). `git diff milestone-v2.3 HEAD -- bench/baseline.json` empty.

## Out of Scope (preserved)

v1 + v2.0 + v2.1 + v2.2 + v2.3 boundaries unchanged. v2.4 explicitly excluded:

- AGENT-LOOP-FEW-SHOT-01 (P2 migration) — v2.5+ candidate
- COLDSTART-PRISTINE-01 — v2.5+ candidate (low urgency)
- Slash commands, Compaction, Sub-agent delegation, Plan-mode bench fixture — v2.5+ candidates (observation-driven)
- Thinking-mode-on (`<think>` consumption) — defer indefinitely
- Native OpenAI `tool_calls` — v3.0 territory
- Streaming inference (STM-01) — deferred 9th cycle
- Cloud comparison — deliberate non-goal
- Re-eval beyond F# fixtures — other dimensions stay at v2.2/v2.3 baselines
- `bench/baseline.json` modifications — eval observational; gate baseline preserved
- Multi-platform (Windows/Linux) — Mac-only ethos preserved
- F# fixture set above 5 — scope discipline

## Future Requirements (v2.5+ candidates)

Tracked for awareness; not pulled into v2.4. Observation-driven scoping after v2.4 ships.

- **AGENT-LOOP-FEW-SHOT-01** — P2 few-shot migration to `defaultSystemPrompt` if observation surfaces value
- **COLDSTART-PRISTINE-01** (low) — Post-reboot pristine cold-start measurement
- **SLASH-01** — `/sessions`, `/plan`, `/clear` slash commands inside REPL
- **COMPACT-01** — Auto-compaction when session approaches 80% of `max_model_len`
- **SUBAG-01** — Sub-agent delegation via Agent tool
- **PLAN-MODE-BENCH-01** — Plan-mode bench fixture via mocked-IKeyReader
- **STM-01** — SSE token streaming (deferred 10x as of v2.4 close)
- **F# fixture expansion** — if v2.5+ surfaces signal that warrants more fixtures or tighter rubric

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| FS-EVAL-01 | Phase 28-02 | ✓ Complete (2026-04-29) |
| FS-EVAL-02 | Phase 28-03 + 28-04 | ✓ Complete (2026-04-29) |
| FS-EVAL-03 | Phase 28-04 + 28-05 | ✓ Complete (2026-04-29) |
| HARNESS-01 | Phase 28-01 | ✓ Complete (2026-04-29) |
| REGRESSION-01 | Phase 28-06 + Phase 30-01 | ✓ Complete (2026-04-29; per-phase + milestone-wide) |
| FS-INTERVENE-01 | Phase 29 (conditional) | SKIPPED (passed_disprove_1of5; never executed) |
| FS-INTERVENE-02 | Phase 29 (conditional) | SKIPPED (passed_disprove_1of5; never executed) |

**Coverage:** 5/5 unconditional ✓ + 2/2 conditional SKIPPED-by-design

---

## Milestone Summary

**Shipped:** 5 of 5 unconditional v2.4 requirements
**SKIPPED-by-design:** 2 of 2 conditional v2.4 requirements (Path A clean disprove; never executed)
**Adjusted:** None — original requirement IDs and behaviors held throughout. One audit-time correction (REQUIREMENTS.md HARNESS-01 validation clause: `4bcd8a4` → `a6159c4` per actual commit hash; commit `3ab6102`).
**Dropped:** None.

---

*Archived: 2026-04-29 as part of v2.4 milestone completion*
