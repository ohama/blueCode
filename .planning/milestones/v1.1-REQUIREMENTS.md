# Requirements Archive: v1.1 blueCode Refinement

**Archived:** 2026-04-24
**Status:** ✅ SHIPPED

This is the archived requirements specification for v1.1.
For v1.2+ requirements, see `.planning/REQUIREMENTS.md` (created fresh by `/gsd:new-milestone`).

---

## v1.1 Requirements (all Complete)

v1.0 UAT 중 노출된 3개 기술 빚 청소. 새 기능 없음.

### Refactor / Portability

- [x] **REF-01**: `Router.modelToName` 하드코딩 제거 — Phase 6에서 adapter-layer `tryParseModelId` + `probeModelInfoAsync` + per-port `Lazy<Task<ModelInfo>>` 로 구현 완료. 06-03 gap closure 로 local-path preference 추가 (HF fallback 차단). 2026-04-24 live 검증 통과.
- [x] **REF-02**: `/v1/models` probe 를 bootstrap 에서 lazy 화 — Phase 6에서 `bootstrapAsync` 삭제 + sync `bootstrap` + lazy per-port probe 로 구현 완료. 2026-04-24 live 검증 통과 (startup 에 HTTP 0건).

### Observability

- [x] **OBS-05**: 실제 LLM thought 를 `Step.Thought` 에 저장 — Phase 7에서 `LlmResponse = { Thought; Output }` Core 레코드 추가 + `ILlmClient.CompleteAsync` 시그니처 확장 + `AgentLoop` Step 생성 + `toLlmOutput` 경로 연결로 구현 완료. 2026-04-24 live 검증 통과 (예: `thought: "The user wants a simple phrase."`).

---

## Traceability (Final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| REF-01 | Phase 6 | ✓ Complete |
| REF-02 | Phase 6 | ✓ Complete |
| OBS-05 | Phase 7 | ✓ Complete |

---

## Milestone Summary

**Shipped:** 3 of 3 v1.1 requirements ✓

**Adjusted during implementation:**
- **REF-01** — initial implementation in 06-01 sent `data[0].id` which triggered mlx_lm.server HF fallback. Scope expanded mid-milestone to include 06-03 gap closure (local-path preference heuristic). Core behavior unchanged; selection heuristic refined.

**Dropped:** None. All 3 requirements delivered.

**v1.2 carry-forward items** (recorded in STATE.md as post-milestone notes, not requirement scope changes):
- Per-port `MaxModelLen` visibility in `AppComponents` (currently hardcoded 8192 floor)
- Consolidate `makeMockResponse` helper to shared test module (currently duplicated in two test files)
- `tryParseModelId` multi-platform path detection (current `StartsWith("/")` is macOS/Linux only)
- Long-running service memory management (prompt cache accumulation ~50MB/run; 10+ run sessions may approach OOM threshold without restart)
- v1.0 research-flagged candidates still applicable: streaming (STM-01), session persistence (SES-01), tool extension (TLX-01..03)

---

*Archived: 2026-04-24 as part of v1.1 milestone completion*
*Original requirements defined: 2026-04-23*
