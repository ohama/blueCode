diff --git a/bench/fixtures/fs_idiomatic/optionhandling.fs b/bench/fixtures/fs_idiomatic/optionhandling.fs
index 5af558e..c90a069 100644
--- a/bench/fixtures/fs_idiomatic/optionhandling.fs
+++ b/bench/fixtures/fs_idiomatic/optionhandling.fs
@@ -3,7 +3,8 @@ module OptionHandling
 /// Parses input; returns 2*value if successful, 0 if not.
 /// Idiomatic implementation should use Option.map + Option.defaultValue.
 let safeDouble (input: string) : int =
-    failwith "TODO: implement safeDouble using Option.map and Option.defaultValue"
+    let parsed = System.Int32.TryParse input
+    parsed |> Option.ofObj |> Option.map (fun x -> 2 * x) |> Option.defaultValue 0
 
 for s in [ "42"; "abc"; "0"; "-7"; "" ] do
     let r = try safeDouble s with _ -> -1
