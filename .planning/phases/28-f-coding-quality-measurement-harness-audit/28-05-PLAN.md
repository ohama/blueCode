---
phase: 28-f-coding-quality-measurement-harness-audit
plan: 05
type: execute
wave: 4
depends_on: ["28-04"]
files_modified:
  - documentation/qwen35-122b-coding-eval.md
autonomous: true

must_haves:
  truths:
    - "§5 Coding-quality 'Idiomatic F#' subsection records the F# fixture evidence from 28-04 honestly"
    - "§7 scorecard Idiomatic F# row updated with the new mapped score (or annotated with disambiguation note if unchanged)"
    - "§7 Coding-quality subtotal re-aggregated"
    - "§7 Grand total re-aggregated"
    - "Final scorecard line strict-format matches `^\\*\\*Total: \\d+/100, Recommendation: KEEP\\*\\*$`"
    - "Score change is referenced to archived transcripts (`.planning/phases/28-.../transcripts/`)"
  artifacts:
    - path: "documentation/qwen35-122b-coding-eval.md"
      provides: "Updated §5 (Idiomatic F# rescored with F# fixture evidence) + §7 (subtotal + grand total) + final scorecard line"
      contains: "F# fixture evidence (v2.4 Phase 28)"
  key_links:
    - from: "§5 new subsection"
      to: ".planning/phases/28-.../transcripts/"
      via: "explicit path reference in citation"
      pattern: "transcripts/fs_idiomatic"
    - from: "§7 Idiomatic F# row"
      to: "§7 Coding-quality subtotal"
      via: "subtotal = sum of category rows; arithmetic must match"
      pattern: "Coding quality"
    - from: "§7 Coding-quality subtotal"
      to: "§7 Dimension coverage check table row (line 873) — `| Coding quality | <subtotal>/10 | <pct>% | YES |`"
      via: "percentage recalculation when subtotal changes (drop 'exactly at threshold' qualifier when pct > 60%)"
      pattern: "Coding quality \\| [0-9]+/10 \\| [0-9.]+%"
    - from: "§7 Coding-quality subtotal"
      to: "Final scorecard line `**Total: <N>/100, Recommendation: KEEP**`"
      via: "Total = sum of all category subtotals"
      pattern: "Total: \\d+/100"
---

<objective>
Update `documentation/qwen35-122b-coding-eval.md` §5 + §7 + final scorecard line based on 28-04's classification verdict and aggregate F# fixture score. Single edit-site task; arithmetic must reconcile across §7 subtotals and grand total.

Purpose: Honest score update reflecting the F#-fixture-based measurement (replacing the Python-transcript-based 1/5 that hedged across v2.2/v2.3). Final scorecard line must remain strict-format-regex matchable.

Output: Updated `documentation/qwen35-122b-coding-eval.md` with §5 expanded (new "F# fixture evidence (v2.4 Phase 28)" subsection citing archived transcripts), §7 row + subtotal + grand total updated (or annotated if unchanged), final scorecard line updated.

Requirement: FS-EVAL-03.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-f-coding-quality-measurement-harness-audit/28-RESEARCH.md
@.planning/phases/28-f-coding-quality-measurement-harness-audit/28-04-SUMMARY.md

# The doc being edited (full file context for arithmetic reconciliation)
@documentation/qwen35-122b-coding-eval.md

# Archived transcripts cited from §5
@.planning/phases/28-f-coding-quality-measurement-harness-audit/transcripts/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update §5 with F# fixture evidence subsection + §5.1 score line</name>
  <files>documentation/qwen35-122b-coding-eval.md</files>
  <action>
Read 28-04-SUMMARY.md to extract:
- Mapped score `M/5` (integer)
- Classification verdict (`passed_disprove_1of5` | `passed_confirm_1of5` | `passed_inconclusive`)
- Per-fixture totals (3 numbers, e.g., `pipeline=4/5, dupatternmatch=4/5, optionhandling=3/5`)

Read `documentation/qwen35-122b-coding-eval.md` lines 642-768 (the §5 section).

**Edit 1: §5.1 Idiomatic F# score verdict line (currently around line 686).**

Locate by content (line numbers may have drifted ±2):
```bash
grep -n 'Idiomatic F#' documentation/qwen35-122b-coding-eval.md
```

The v2.3 verdict was 1/5 based on 1-of-3 Python-answer transcripts. Replace with the new evidence-based verdict. Form depends on classification:

**If `passed_disprove_1of5` (M ≥ 3):**

Replace the existing §5.1 subsection score line with the new score AND add a "F# fixture evidence (v2.4 Phase 28)" paragraph immediately after the existing §5.1 narrative. Suggested replacement:

```markdown
**Score: <M>/5** (revised from 1/5 in v2.3 — see "F# fixture evidence (v2.4 Phase 28)" below).

The original 1/5 score was based on HumanEval+ Python-answer transcripts under chat mode (i.e., the model's Python output reviewed for "F# idioms" — a category error noted in v2.2 audit Caveat 6). v2.4 Phase 28 ran F# task fixtures through the daily-driver agent-loop path (no `--plan`) and rescored against a 5-point binary rubric.

**F# fixture evidence (v2.4 Phase 28):**

3 fixtures under `bench/fixtures/fs_idiomatic/` (pipeline `|>`, DU pattern matching, `Option.map`/`Option.defaultValue`) were filled by Qwen 3.5 122B with kickstart pre-flight. Per-fixture rubric (5 binary criteria each: idiomatic pattern used, no anti-pattern fallback, naming + organization, type signatures preserved, code compiles):

- `pipeline.fs`: <N>/5
- `dupatternmatch.fs`: <N>/5
- `optionhandling.fs`: <N>/5

Aggregate: <X.XX>/5 → mapped to **<M>/5** on this scorecard.

Transcripts archived at `.planning/phases/28-f-coding-quality-measurement-harness-audit/transcripts/`. Scoring detail: `.planning/phases/.../28-04-SUMMARY.md`.

The Python-transcript-based 1/5 was a category artifact; on actual F# generation in agent-loop mode, the model is in solid mid-to-upper tier.
```

**If `passed_confirm_1of5` (M = 1):**

Keep §5.1 score at 1/5 but add the same "F# fixture evidence (v2.4 Phase 28)" paragraph confirming on F# fixtures the score holds. Recommend Phase 29 directive intervention. Replacement keeps the score line and adds:

```markdown
**Score: 1/5** (confirmed by v2.4 Phase 28 F# fixture evidence — see below).

v2.2 audit Caveat 6 hedged that the 1/5 may be a Python-transcript artifact. v2.4 Phase 28 ran F# task fixtures through the daily-driver agent-loop path and confirms the score is real on F# generation as well.

**F# fixture evidence (v2.4 Phase 28):**

(same per-fixture breakdown as Path A)

Aggregate: <X.XX>/5 → mapped to **1/5** on this scorecard.

Phase 29 (F# style intervention via `defaultSystemPrompt` directive) is recommended to address this; see ROADMAP §Phase 29.
```

**If `passed_inconclusive` (M = 2):**

Keep §5.1 score noted as ambiguous; document the 2/5 measurement and explain uncertainty:

```markdown
**Score: 2/5** (revised from 1/5; v2.4 Phase 28 measurement landed in the ambiguous zone).

(F# fixture evidence paragraph as above with M=2)

The score is mid-low; whether to trigger Phase 29 intervention is a judgment call recorded in 28-06-SUMMARY.md.
```

The edit MUST be made by `Read`-then-`Edit` (or Write tool with full content) ensuring the surrounding §5.1 narrative remains intact and the existing 1-of-3 transcript citation is preserved as historical evidence (per research Q12).
  </action>
  <verify>
```bash
# §5.1 score line updated
grep -n 'Idiomatic F#' documentation/qwen35-122b-coding-eval.md

# F# fixture evidence subsection added
grep -F 'F# fixture evidence (v2.4 Phase 28)' documentation/qwen35-122b-coding-eval.md
grep -F 'bench/fixtures/fs_idiomatic' documentation/qwen35-122b-coding-eval.md

# Per-fixture lines present (3 fixture name mentions)
grep -E 'pipeline\.fs|dupatternmatch\.fs|optionhandling\.fs' documentation/qwen35-122b-coding-eval.md

# Transcript archive cited
grep -F 'transcripts/' documentation/qwen35-122b-coding-eval.md

# Original 1-of-3 transcripts citation preserved as historical context
grep -F '1 of 3 transcripts' documentation/qwen35-122b-coding-eval.md
```
  </verify>
  <done>
§5 contains the new "F# fixture evidence (v2.4 Phase 28)" subsection with per-fixture scores, aggregate, mapped score, and citation to `.planning/phases/28-.../transcripts/`. The historical 1-of-3 Python-transcript citation remains intact. The §5.1 score verdict line reflects the new mapped score (or remains 1/5 with confirmed-on-F# annotation depending on classification).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update §7 scorecard table + §7 subtotal + grand total + final scorecard line</name>
  <files>documentation/qwen35-122b-coding-eval.md</files>
  <action>
Reconcile §7 arithmetic with the §5 score change.

**Edit 2: §7 Idiomatic F# row (currently line 861).**

Locate by content:
```bash
grep -n 'Idiomatic F#' documentation/qwen35-122b-coding-eval.md
```

Current row:
```
| Coding quality | Idiomatic F# (1 of 3 transcripts) | 1 | 5 |
```

Replace with (depending on classification):

**Path A (disprove, M ≥ 3):**
```
| Coding quality | Idiomatic F# (v2.4 Phase 28 F# fixtures) | <M> | 5 |
```

**Path B (confirm, M = 1):**
```
| Coding quality | Idiomatic F# (v2.4 Phase 28 F# fixtures, confirmed) | 1 | 5 |
```

**Path C (inconclusive, M = 2):**
```
| Coding quality | Idiomatic F# (v2.4 Phase 28 F# fixtures, ambiguous) | 2 | 5 |
```

**Edit 3: §7 Coding-quality subtotal line (currently line 864).**

Read the §7 table around line 858-870. Compute the new subtotal as the sum of all "Coding quality" rows (only the Idiomatic F# row changed; sum delta is `M - 1`). Update the subtotal line accordingly.

For Path A: old subtotal 6/10 → new 6 + (M-1) = `(5+M)/10` (e.g., M=3 → 8/10; M=4 → 9/10; M=5 → 10/10).
For Path B: subtotal unchanged (6/10).
For Path C: old 6/10 → 7/10.

**Edit 3b: §7 Dimension coverage check table row (currently line 873).**

The §7 "Dimension coverage check" table contains a percentage row for Coding quality:

```
| Coding quality | 6/10 | 60.0% | YES (exactly at threshold) |
```

When §7 Coding-quality subtotal changes (e.g., 6/10 → 8/10), this percentage row also changes:
- Path A (M=3): `| Coding quality | 8/10 | 80.0% | YES |` (drop "exactly at threshold" qualifier — percentage now exceeds 60%)
- Path A (M=4): `| Coding quality | 9/10 | 90.0% | YES |` (drop qualifier)
- Path A (M=5): `| Coding quality | 10/10 | 100.0% | YES |` (drop qualifier)
- Path B (M=1): row unchanged (subtotal stays 6/10)
- Path C (M=2): `| Coding quality | 7/10 | 70.0% | YES |` (drop "exactly at threshold" qualifier — percentage now exceeds 60%)

Locate by content:
```bash
grep -n 'Coding quality | 6/10 | 60.0%' documentation/qwen35-122b-coding-eval.md
```

Edit the row to reflect new subtotal and percentage. Remove the "(exactly at threshold)" qualifier whenever percentage > 60%.

**Edit 4: §7 Grand total line (currently line 881).**

Compute new grand total: old 92 + (subtotal delta). Update the row accordingly.

For Path A: 92 + (M-1) → 93/94/95/96.
For Path B: 92 unchanged.
For Path C: 93.

**Edit 5: Final scorecard line (currently line 1051).**

Current:
```
**Total: 92/100, Recommendation: KEEP**
```

Replace with new total. MUST match strict-format regex `^\*\*Total: \d+/100, Recommendation: KEEP\*\*$` exactly.

For Path A: `**Total: <92+M-1>/100, Recommendation: KEEP**`
For Path B: `**Total: 92/100, Recommendation: KEEP**` (unchanged)
For Path C: `**Total: 93/100, Recommendation: KEEP**`

**Arithmetic reconciliation invariant:** §7 grand total MUST equal sum of §7 subtotal rows AND MUST equal the final scorecard line's total. Any inconsistency = blocker.

Verify the math by reading §7 in full after edits and adding the row scores by hand. Document in 28-05-SUMMARY.md.
  </action>
  <verify>
```bash
# §7 Idiomatic F# row updated (mentions v2.4 Phase 28 fixtures)
grep -E '^\| Coding quality \| Idiomatic F# \(v2\.4 Phase 28' documentation/qwen35-122b-coding-eval.md

# §7 Dimension coverage check row recomputed (Path A or C: percentage > 60%, qualifier dropped; Path B: unchanged at 60.0%)
grep -E '^\| Coding quality \| (8|9|10|7|6)/10 \| (60\.0|70\.0|80\.0|90\.0|100\.0)% \| YES' documentation/qwen35-122b-coding-eval.md
# For Path A or C: assert "exactly at threshold" qualifier is removed when percentage > 60%
# (manual reconcile: Path B leaves the row unchanged including the qualifier)

# Final scorecard line strict-format match
grep -E '^\*\*Total: [0-9]+/100, Recommendation: KEEP\*\*$' documentation/qwen35-122b-coding-eval.md

# Manual arithmetic check: extract the §7 Coding-quality subtotal and the grand total; they must reconcile
# (Verifier reads the edited section + 28-05-SUMMARY.md to confirm the addition is correct)

# Out-of-scope guardrail: only documentation/qwen35-122b-coding-eval.md changed in this commit
# (After Task 3 commits, HEAD~1 is the pre-eval-edit state. We assert no other files were committed.)
# Until Task 3 commits, this check applies to the working tree against HEAD; after commit, against HEAD~1:
git diff HEAD~1 --name-only 2>/dev/null | grep -vF 'documentation/qwen35-122b-coding-eval.md' | wc -l   # = 0 (no other files in commit)
```
  </verify>
  <done>
§7 Idiomatic F# row updated (cites v2.4 Phase 28 F# fixtures); §7 Coding-quality subtotal recomputed and updated (or annotated unchanged); §7 grand total recomputed and updated (or annotated unchanged); final scorecard line strict-format-regex matches; arithmetic reconciles between §7 row → subtotal → grand total → final line.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bench gate hold + atomic commit</name>
  <files>(verification + commit only)</files>
  <action>
1. **Bench gate hold:**
   ```bash
   bash bench/run.sh --gate
   ```
   Expected: exit 0; `GATE PASS (7/7)`.

2. **Architectural invariants:**
   ```bash
   git diff milestone-v2.3 HEAD -- src/                    # empty
   git diff milestone-v2.3 HEAD -- bench/baseline.json    # empty
   git diff milestone-v2.3 HEAD -- bench/run.sh           # empty
   ! grep -E "import mlx_lm" bench/eval-qwen35-122b.sh    # HTTP-only invariant
   git diff bench/fixtures/fs_idiomatic/                   # empty
   ```

3. **Strict-format regex final check:**
   ```bash
   grep -cE '^\*\*Total: [0-9]+/100, Recommendation: KEEP\*\*$' documentation/qwen35-122b-coding-eval.md
   # Must output 1
   ```

4. **Commit:**
   ```bash
   git add documentation/qwen35-122b-coding-eval.md
   git commit -m "docs(28-05): update eval doc with F# fixture evidence

§5.1 Idiomatic F# rescored from 1/5 → <M>/5 based on v2.4 Phase 28
F# fixture evidence (3 fixtures under bench/fixtures/fs_idiomatic/
run through agent-loop mode against 122B with kickstart pre-flight).

§7 Idiomatic F# row updated to cite v2.4 Phase 28 F# fixtures.
§7 Coding-quality subtotal: 6/10 → <new>/10.
§7 Grand total: 92/100 → <new>/100.
Final scorecard line: '**Total: <new>/100, Recommendation: KEEP**'.

Per-fixture rubric and aggregate scoring detail in
.planning/phases/28-f-coding-quality-measurement-harness-audit/28-04-SUMMARY.md.
Transcripts archived under .planning/phases/.../transcripts/.

FS-EVAL-03 satisfied. Verdict KEEP preserved per architectural invariant."
   ```

   (For Path B with score unchanged, adjust the message to "1/5 confirmed by v2.4 Phase 28 evidence; subtotal/grand total unchanged"; same atomic commit.)

5. **Explicit-path staging verified:**
   ```bash
   git log -1 --stat | head -5
   # Should show only documentation/qwen35-122b-coding-eval.md
   ```
  </action>
  <verify>
```bash
# Bench gate
bash bench/run.sh --gate    # exit 0; "GATE PASS (7/7)"

# Final scorecard line strict-format matches
[ "$(grep -cE '^\*\*Total: [0-9]+/100, Recommendation: KEEP\*\*$' documentation/qwen35-122b-coding-eval.md)" = "1" ]

# Commit landed
git log --oneline -1 | grep -F 'docs(28-05)'

# Commit only touched eval doc
git diff HEAD~1 --name-only | tr '\n' ' '   # only "documentation/qwen35-122b-coding-eval.md"

# All architectural invariants
git diff milestone-v2.3 HEAD -- src/                    # empty
git diff milestone-v2.3 HEAD -- bench/baseline.json    # empty
git diff milestone-v2.3 HEAD -- bench/run.sh           # empty
git diff bench/fixtures/fs_idiomatic/                   # empty
! grep -E "import mlx_lm" bench/eval-qwen35-122b.sh
```
  </verify>
  <done>
Bench gate exits 0 with `GATE PASS (7/7)`; final scorecard line matches strict-format regex; atomic commit `docs(28-05): update eval doc with F# fixture evidence` lands via explicit-path staging (only `documentation/qwen35-122b-coding-eval.md` modified); all architectural invariant diffs empty.
  </done>
</task>

</tasks>

<verification>
Plan-level verification:

```bash
# §5 + §7 updates landed
grep -F 'F# fixture evidence (v2.4 Phase 28)' documentation/qwen35-122b-coding-eval.md
grep -F 'bench/fixtures/fs_idiomatic' documentation/qwen35-122b-coding-eval.md
grep -F 'transcripts/' documentation/qwen35-122b-coding-eval.md

# §7 row updated (matches one of the three forms)
grep -E '^\| Coding quality \| Idiomatic F# \(v2\.4 Phase 28' documentation/qwen35-122b-coding-eval.md

# Final scorecard line strict-format
grep -cE '^\*\*Total: [0-9]+/100, Recommendation: KEEP\*\*$' documentation/qwen35-122b-coding-eval.md   # = 1

# Architectural invariants
git diff milestone-v2.3 HEAD -- src/                    # empty
git diff milestone-v2.3 HEAD -- bench/baseline.json    # empty
git diff milestone-v2.3 HEAD -- bench/run.sh           # empty
git diff bench/fixtures/fs_idiomatic/                   # empty
! grep -E "import mlx_lm" bench/eval-qwen35-122b.sh

# Bench gate
bash bench/run.sh --gate                                # exit 0; "GATE PASS (7/7)"

# Atomic commit (only eval doc changed)
git diff HEAD~1 --name-only | grep -vF 'documentation/qwen35-122b-coding-eval.md' | wc -l   # = 0
```
</verification>

<success_criteria>
1. §5 contains "F# fixture evidence (v2.4 Phase 28)" subsection with per-fixture scores, aggregate, mapped score, and citation to `.planning/phases/28-.../transcripts/`.
2. §5 historical 1-of-3 Python-transcript citation preserved.
3. §7 Idiomatic F# row updated (cites v2.4 Phase 28 F# fixtures with current/new score).
4. §7 Coding-quality subtotal arithmetic correct (sum of category rows).
5. §7 Grand total arithmetic correct (sum of all category subtotals).
6. Final scorecard line strict-format-regex matches `^\*\*Total: \d+/100, Recommendation: KEEP\*\*$` exactly once.
7. Verdict remains `KEEP` (architectural invariant).
8. `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)`.
9. `git diff milestone-v2.3 HEAD -- src/ bench/baseline.json bench/run.sh` empty.
10. Atomic commit `docs(28-05): update eval doc with F# fixture evidence` lands via explicit-path staging (only `documentation/qwen35-122b-coding-eval.md`).
11. FS-EVAL-03 requirement satisfied per REQUIREMENTS.md validation criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/28-f-coding-quality-measurement-harness-audit/28-05-SUMMARY.md` with:
- Diff summary of `documentation/qwen35-122b-coding-eval.md` (lines added/changed)
- §7 arithmetic reconciliation (verbatim table extract showing each category subtotal + grand total)
- Final scorecard line verbatim (strict-format match confirmed)
- Score delta: old → new (e.g., "92/100 → 95/100")
- Bench gate verification (`GATE PASS (7/7)`)
- Atomic commit hash
- FS-EVAL-03 requirement: marked satisfied
</output>
