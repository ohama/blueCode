---
phase: 42-qwen-122b-openai-compat-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/eval-qwen35-122b.sh
  - bench/eval-openai-compat.py
  - bench/requirements-eval.txt
autonomous: true

must_haves:
  truths:
    - "User can run `bash bench/eval-qwen35-122b.sh --openai-compat` and get a non-empty `bench/runs/openai-compat-<ts>/probes.jsonl` with one record per probe (HTTP code + response excerpt + label + category)"
    - "Pre-flight `bash bench/run.sh --gate` returns 7/7 PASS BEFORE any 8001 probing happens (fail-fast contamination guard)"
    - "Plan 42-01 probe set covers Surfaces 1+2+3 from RESEARCH.md: endpoint surface (3 POST + 2 GET), response_format field (json_object + json_schema), role handling (mid-conv system rejected per Phase 17-02 invariant)"
    - "Bench gate `bash bench/run.sh --gate` STILL returns 7/7 PASS after Plan 42-01's probes complete (no KV contamination)"
    - "`bench/.venv-eval` has `jsonschema>=4.21` installed (one new pip dep) and `python -c 'import jsonschema'` succeeds"
  artifacts:
    - path: bench/eval-openai-compat.py
      provides: "Python probe driver: argparse + probe() helper + PROBES list (~10 probes for Surfaces 1+2+3) + main() that writes probes.jsonl"
      min_lines: 120
    - path: bench/eval-qwen35-122b.sh
      provides: "New `--openai-compat` mode handler `run_openai_compat` + dispatcher arm + usage line"
      contains: "run_openai_compat"
    - path: bench/requirements-eval.txt
      provides: "Adds `jsonschema>=4.21` line"
      contains: "jsonschema"
  key_links:
    - from: bench/eval-qwen35-122b.sh
      to: bench/eval-openai-compat.py
      via: '`"$VENV_PY" bench/eval-openai-compat.py --output-dir "$LOG_DIR"` shell-out inside `run_openai_compat`'
      pattern: 'eval-openai-compat\.py'
    - from: bench/eval-openai-compat.py
      to: http://127.0.0.1:8001/v1/chat/completions
      via: "requests.post per-probe; one JSONL record appended per call (flush per record for crash-resumability)"
      pattern: 'requests\.post.*chat/completions'
---

<objective>
Scaffold the OpenAI-compat test harness: extend `bench/eval-qwen35-122b.sh` with a new
`--openai-compat` mode, create `bench/eval-openai-compat.py` skeleton, install one new
pip dep (`jsonschema`), and implement the FIRST batch of ~10 probes covering RESEARCH.md
Surfaces 1+2+3 (endpoint surface, `response_format` field, role handling).

Purpose: Lay the harness groundwork that Plan 42-02 (streaming + concurrency + errors)
and Plan 42-03 (rendering + mitigation docs) will build on. Confirm the bench gate
sandwich (pre + post) holds — i.e., probing 8001 with these ~10 calls does NOT
contaminate KV cache enough to regress the 7/7 gate.

Output: `bench/eval-openai-compat.py` (~150 LOC, one probe-fn list + main); `bench/eval-qwen35-122b.sh` +1 mode handler; `bench/runs/openai-compat-<ts>/probes.jsonl` with ≥10 records; bench gate 7/7 PASS pre AND post.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-qwen-122b-openai-compat-test/42-RESEARCH.md

# Existing harness style references — match these conventions:
@bench/eval-qwen35-122b.sh
@bench/eval-humaneval-http.py
@bench/eval-needle.py
@bench/requirements-eval.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight bench gate + add jsonschema pip dep + create eval-openai-compat.py skeleton</name>
  <files>
    bench/requirements-eval.txt
    bench/eval-openai-compat.py
  </files>
  <action>
    STEP 1 (pre-flight gate — MANDATORY FIRST):
    Run `bash bench/run.sh --gate` and confirm 7/7 PASS. If anything other than 7/7 PASS, STOP — do
    not proceed. Surface the failing fixture(s) and run `launchctl kickstart -k gui/$(id -u)/com.ohama.qwen122b`
    per CLAUDE.md "Connection refused or 300s timeout" then re-run; if still not 7/7, abort plan
    and report — Plan 42-01 cannot start with a contaminated baseline.

    STEP 2 (deps):
    Append the line `jsonschema>=4.21` to `bench/requirements-eval.txt` (idempotent — check first).
    Then run `bench/.venv-eval/bin/pip install -r bench/requirements-eval.txt`. Confirm with
    `bench/.venv-eval/bin/python -c 'import jsonschema; print(jsonschema.__version__)'` (expect
    4.21+).

    STEP 3 (skeleton):
    Create `bench/eval-openai-compat.py` (executable, shebang `#!/usr/bin/env python3`) following
    the skeleton in RESEARCH.md §"Sample eval-openai-compat.py skeleton" but EXTENDED to:
    - Module docstring referencing Phase 42 + RESEARCH.md
    - Constants: ENDPOINT="http://127.0.0.1:8001", MODEL_PATH="/Users/ohama/llm-system/models/qwen122b"
    - `def probe(label, category, body, expected, severity_hint, max_tokens=8, timeout=120)` — POSTs
      `body` merged with `{"model": MODEL_PATH, "max_tokens": max_tokens}` to `/v1/chat/completions`,
      captures `r.status_code`, `r.text[:300]` (RAW excerpt — NOT `r.json()`, per RESEARCH.md
      anti-pattern), elapsed seconds, returns dict with keys
      {label, category, http_code, response_excerpt, elapsed_s, expected, severity_hint, request_excerpt}.
      Wraps `requests.post` in try/except so a probe crash emits an `error` record but does NOT
      abort the suite (resumability).
    - `def probe_get(label, path)` — GET helper for `/v1/models` and `/health`. Same record shape minus
      request_excerpt.
    - PROBES list with 10 entries (one per Surfaces 1+2+3 probe — see Task 2 for content).
    - `def main()` with argparse: `--output-dir` (required), `--render <path-to-jsonl>` (optional;
      Plan 42-03 will populate; for 42-01 just print "rendering deferred to Plan 42-03" if invoked
      with `--render`).
    - Main loop: for each probe in PROBES, call `probe(...)` or `probe_get(...)`, write JSON record +
      `\n` to `probes.jsonl`, `fp.flush()` per record (crash-resumability), print one-line status
      `[<http>] <label> (<elapsed>s)` to stdout.
    - `chmod +x bench/eval-openai-compat.py` after creation.

    Do NOT yet populate the rendering branch beyond the deferral message — that's Plan 42-03.
  </action>
  <verify>
    `grep -c '^jsonschema' bench/requirements-eval.txt` returns 1.
    `bench/.venv-eval/bin/python -c 'import jsonschema, requests; print("ok")'` prints `ok`.
    `bench/eval-openai-compat.py --help` exits 0 and shows `--output-dir` + `--render` flags.
    `wc -l bench/eval-openai-compat.py` ≥ 120 (skeleton + 10 PROBES entries).
    `bench/eval-openai-compat.py --output-dir /tmp/_p42_01_smoke && wc -l /tmp/_p42_01_smoke/probes.jsonl` ≥ 10.
    `python -c 'import json; [json.loads(l) for l in open("/tmp/_p42_01_smoke/probes.jsonl")]'` exits 0.
  </verify>
  <done>
    Pre-flight bench gate 7/7 PASS confirmed. `bench/.venv-eval` has jsonschema installed.
    `bench/eval-openai-compat.py` exists, is executable, has skeleton + 10 PROBES, runs end-to-end
    against live 8001, emits valid JSONL with at least 10 records.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `--openai-compat` mode in eval-qwen35-122b.sh + populate Surfaces 1+2+3 probe content</name>
  <files>
    bench/eval-qwen35-122b.sh
    bench/eval-openai-compat.py
  </files>
  <action>
    STEP 1 (mode handler in shell):
    Edit `bench/eval-qwen35-122b.sh`. After the existing `run_coldstart` function (around line 545)
    and BEFORE `run_full` (line 579), add `run_openai_compat`. Format MUST match RESEARCH.md
    §"Sample bench/eval-qwen35-122b.sh mode handler addition":

    ```
    # ---------------------------------------------------------------------------
    # run_openai_compat — Phase 42: empirical OpenAI conformance probes.
    # Drives bench/eval-openai-compat.py; emits probes.jsonl to LOG_DIR.
    # Pre-requisite: bench/run.sh --gate must pass (run BEFORE this).
    # Post-requisite: bench/run.sh --gate must pass again (run AFTER this).
    # Plan 42-01: ~10 probes (Surfaces 1+2+3); Plan 42-02 extends to ~30; Plan 42-03 renders report.
    # ---------------------------------------------------------------------------
    run_openai_compat() {
      require_port_8001
      if [ ! -x "$VENV_PY" ]; then
        echo "ERROR: $VENV_PY not found. Run: bash $0 --setup" >&2
        exit 5
      fi
      mkdir -p "$LOG_DIR"
      echo "===== OpenAI compat probes (LOG_DIR=$LOG_DIR) ====="
      "$VENV_PY" bench/eval-openai-compat.py --output-dir "$LOG_DIR"
      local probes="$LOG_DIR/probes.jsonl"
      local count
      count=$(wc -l < "$probes" | tr -d ' ')
      echo "openai-compat: $count probes recorded in $probes"
    }
    ```

    Then in the dispatcher case statement (around line 636), add `--openai-compat) run_openai_compat ;;`
    BEFORE the `--full)` line. Update the usage()' EOF line list (~line 617) to include
    `--openai-compat`. Do NOT extend `run_full` to call `run_openai_compat` — Phase 42 is
    intentionally separate from `--full` daily-driver work (RESEARCH.md anti-pattern: parallelizing
    probes for speed contaminates KV state).

    STEP 2 (probe content for Plan 42-01):
    In `bench/eval-openai-compat.py`, populate the PROBES list with these 10 probes (each as a tuple
    or dict — pick one shape and stick with it; recommend dict for clarity):

    SURFACE 1 — endpoint coverage (5 probes):
      - "01-baseline-chat" — POST /v1/chat/completions baseline (model + messages user "Say hi", max_tokens=8) — expected HTTP 200, content non-empty, severity_hint="PASS"
      - "02-completions-legacy" — POST /v1/completions {prompt:"def fib(n):", max_tokens=20, temperature:0.2} — expected HTTP 200 + object="text_completion" (RESEARCH.md preliminary 11). Use a SEPARATE probe variant (call requests.post with /v1/completions URL inside the same probe() helper — extend probe() with optional `path="/v1/chat/completions"` param). severity_hint="PASS".
      - "03-models-list" — GET /v1/models via `probe_get` — expected 200 + data[0].id contains "qwen122b". severity_hint="PASS".
      - "04-health-endpoint" — GET /health via `probe_get` — expected 200. severity_hint="PASS".
      - "05-responses-endpoint" — POST /v1/responses (OpenAI 2024 stateful API) {model, messages} — expected 404 (per RESEARCH.md Open Question 4). severity_hint="LOW".

    SURFACE 2 — response_format (3 probes):
      - "06-response-format-json-object" — POST /v1/chat/completions with messages user "Say hi" + `response_format:{type:"json_object"}` max_tokens=8 — RESEARCH.md preliminary 1 verdict NON-CONFORMANT. severity_hint="HIGH".
      - "07-response-format-json-schema-strict" — POST with `response_format:{type:"json_schema",json_schema:{name:"user",strict:true,schema:{type:"object",properties:{name:{type:"string"},age:{type:"integer"}},required:["name","age"],additionalProperties:false}}}` max_tokens=80 — RESEARCH.md preliminary 2 verdict NON-CONFORMANT. severity_hint="HIGH".
      - "08-response-format-no-rerun-N1" — exact same body as 06 but rerun N=1 (Plan 42-02 may bump to N=10 for content-shape statistics; Plan 42-01 captures one-shot). severity_hint="HIGH".

    SURFACE 3 — role handling (2 probes):
      - "09-mid-conv-system-rejected" — POST with messages= [{system,"You are helpful"},{user,"hi"},{assistant,"Hello!"},{system,"Now be terse"},{user,"explain"}] max_tokens=8 — RESEARCH.md preliminary 7 verdict expects HTTP 404 + error string "System message must be at the beginning." severity_hint="HIGH" (already mitigated in blueCode by Phase 17-02+20-03 invariants — this probe CONFIRMS invariant still holds against current server).
      - "10-system-only-at-start" — POST with messages = [{system,"You are concise."},{user,"Say hi"}] max_tokens=8 — expected HTTP 200 (control case for probe 09). severity_hint="PASS".

    For each probe entry, include keys: label, category, body (dict — partial, merged with model+max_tokens by probe()), path (default "/v1/chat/completions" but "02" uses "/v1/completions", "03" uses "/v1/models" with method=GET, "04" uses "/health" with method=GET, "05" uses "/v1/responses"), expected (one-line human description), severity_hint.

    STEP 3 (run end-to-end):
    Execute `bash bench/eval-qwen35-122b.sh --openai-compat`. Confirm `LOG_DIR=bench/runs/qwen35-eval-<ts>/probes.jsonl` has exactly 10 lines. Spot-check 3 records (probe 01, probe 06, probe 09) by `jq` to confirm `http_code` matches expected behavior from RESEARCH.md preliminary probes.

    STEP 4 (post-flight gate — MANDATORY LAST):
    Run `bash bench/run.sh --gate`. Confirm 7/7 PASS. If any regression, the probes contaminated KV
    state — STOP and surface the failure (likely a malformed body left the server in a bad state).
    Do not commit a contaminated state.
  </action>
  <verify>
    `grep -n "run_openai_compat" bench/eval-qwen35-122b.sh` shows the function def + dispatcher arm
    + usage line entry (3 hits minimum).
    `bash -n bench/eval-qwen35-122b.sh` (syntax check) exits 0.
    `bash bench/eval-qwen35-122b.sh --openai-compat` exits 0.
    Latest `bench/runs/qwen35-eval-*/probes.jsonl` has exactly 10 lines.
    `jq -r '.label' bench/runs/qwen35-eval-*/probes.jsonl | sort` lists 01-baseline-chat through
    10-system-only-at-start (no duplicates, no missing labels).
    `jq 'select(.label=="06-response-format-json-object") | .http_code' bench/runs/qwen35-eval-*/probes.jsonl`
    returns 200 (server silently ignores response_format per RESEARCH.md preliminary 1).
    `jq 'select(.label=="09-mid-conv-system-rejected") | .http_code' ...` returns 404.
    `bash bench/run.sh --gate` post-flight returns 7/7 PASS.
  </verify>
  <done>
    `--openai-compat` mode wired and dispatchable. 10 probes (Surfaces 1+2+3) execute end-to-end
    against live 8001. probes.jsonl is well-formed JSONL with all expected labels. All HTTP
    codes from preliminary probes 1-7 + 11 reproduce. Pre AND post bench-gate sandwich PASS 7/7.
  </done>
</task>

</tasks>

<verification>
- Bench gate sandwich: pre-flight 7/7 (Task 1 step 1) AND post-flight 7/7 (Task 2 step 4) — both required for plan completion.
- `bash -n bench/eval-qwen35-122b.sh` shell syntax clean.
- `python -m py_compile bench/eval-openai-compat.py` clean.
- `wc -l bench/runs/qwen35-eval-*/probes.jsonl` = 10 (most recent run).
- All 10 probes' HTTP codes match preliminary findings in RESEARCH.md §Preliminary probes (1-7 + 11; probes 8 + 9 + 10 land in Plan 42-02 via tools/streaming/concurrency).
- No new src/ diff: `git diff master -- src/` is empty (Phase 42 is measurement work).
</verification>

<success_criteria>
1. `bash bench/eval-qwen35-122b.sh --openai-compat` is dispatchable.
2. probes.jsonl has 10 records covering Surfaces 1+2+3 (endpoint, response_format, role).
3. Bench gate 7/7 PASS pre AND post.
4. jsonschema dep installed in bench/.venv-eval.
5. Source code untouched (`git diff master -- src/` empty).
</success_criteria>

<output>
After completion, create `.planning/phases/42-qwen-122b-openai-compat-test/42-01-SUMMARY.md` with:
- Pre-flight gate result (timestamp + 7/7 PASS confirmation)
- Post-flight gate result (timestamp + 7/7 PASS confirmation)
- LOG_DIR path of the 10-probe run
- Per-probe HTTP code table (label → http_code → matches/diverges from RESEARCH.md preliminary)
- Any deviations from preliminary findings (server build may have changed since 2026-05-06 research)
- Hand-off to Plan 42-02: probe.jsonl path + extension points (15 more probes for streaming/schema/error/concurrency)
</output>
