---
phase: 42-qwen-122b-openai-compat-test
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - bench/eval-openai-compat.py
  - documentation/qwen35-122b-openai-compat.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "User can read `documentation/qwen35-122b-openai-compat.md` and find a per-surface table (8 surfaces) with columns Surface / Probe Label / Expected / Observed / Verdict / Severity, derived directly from probes.jsonl"
    - "User can re-run `bash bench/eval-qwen35-122b.sh --openai-compat` and `bench/.venv-eval/bin/python bench/eval-openai-compat.py --render bench/runs/openai-compat-<ts>/probes.jsonl > documentation/qwen35-122b-openai-compat.md` to regenerate the report from any prior probe transcript (reproducibility)"
    - "Severity-classified findings table at top of report lists every HIGH and MEDIUM finding with a one-line mitigation pointer (rubric per RESEARCH.md §Pattern 4)"
    - "CLAUDE.md `Key Seams` section gains a one-block update referencing the report + the v2.7+ tools/tool_choice candidate (PASS finding from probe 15) IF and only if a HIGH-severity finding exists that affects an existing seam"
    - "Final post-flight `bash bench/run.sh --gate` 7/7 PASS — milestone-wide invariant preserved through Phase 42 close"
  artifacts:
    - path: documentation/qwen35-122b-openai-compat.md
      provides: "Empirical conformance report. Sections: Summary verdict + scorecard / Findings by severity / Per-surface tables / Reproduction instructions / Out-of-scope / Sources (RESEARCH.md + probes.jsonl path)"
      min_lines: 200
    - path: bench/eval-openai-compat.py
      provides: "Render mode: `--render <jsonl_path>` reads jsonl and emits markdown to stdout. Uses jsonschema (the dep installed in Plan 42-01) to validate per-probe expected shape if probe was a 'PASS' candidate; uses category-based grouping for tables."
      contains: "def render_report"
    - path: CLAUDE.md
      provides: "Key Seams update referencing the new doc + v2.7+ tools-as-action-DU candidate IF probe 15 reproduced PASS"
      contains: "qwen35-122b-openai-compat.md"
  key_links:
    - from: documentation/qwen35-122b-openai-compat.md
      to: bench/runs/openai-compat-<ts>/probes.jsonl
      via: "Reproduction Instructions section names the exact LOG_DIR and the --render command"
      pattern: 'probes\.jsonl'
    - from: bench/eval-openai-compat.py
      to: documentation/qwen35-122b-openai-compat.md
      via: "render_report() in --render mode emits this file's structure verbatim from probes.jsonl"
      pattern: 'def render_report'
    - from: CLAUDE.md
      to: documentation/qwen35-122b-openai-compat.md
      via: "one-line reference in Key Seams section pointing to the new report (only if HIGH findings affect existing seams)"
      pattern: 'qwen35-122b-openai-compat'
---

<objective>
Render the empirical transcript (`probes.jsonl` from Plans 42-01 + 42-02 = 25 records) into
`documentation/qwen35-122b-openai-compat.md` per the structure laid out in RESEARCH.md
§Severity classification rubric. Apply the verdict + severity rules to each probe (HIGH /
MEDIUM / LOW / PASS) based on RESEARCH.md preliminary findings + this run's actual data.
Surface multi-call coherence findings (probes 23/24/25 from Plan 42-02) by joining the three
records and asserting answer correctness + non-leakage. Update CLAUDE.md Key Seams ONLY if a
HIGH-severity finding affects an existing seam. Run final post-flight bench gate.

Purpose: Close Phase 42 with a human-readable verdict that downstream consumers (v2.6
remaining phases, v2.7+ planning, future eval runs) can read in 5 minutes and know "is
Qwen 122B OpenAI-compatible?" + "what mitigations does blueCode already apply?".

Output: `documentation/qwen35-122b-openai-compat.md` ~250-400 LOC; `bench/eval-openai-compat.py`
grows from ~280 LOC (Plan 42-02) to ~350-450 LOC; CLAUDE.md may gain ~5-15 lines (conditional);
post-flight bench gate 7/7 PASS.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-qwen-122b-openai-compat-test/42-RESEARCH.md
@.planning/phases/42-qwen-122b-openai-compat-test/42-01-SUMMARY.md
@.planning/phases/42-qwen-122b-openai-compat-test/42-02-SUMMARY.md
@bench/eval-openai-compat.py

# Style reference for the new doc — match section ordering & header conventions:
@documentation/qwen35-122b-coding-eval.md

# CLAUDE.md is editable IFF a HIGH-severity finding affects an existing seam:
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `--render` mode in eval-openai-compat.py + apply verdict/severity rules to probes.jsonl</name>
  <files>
    bench/eval-openai-compat.py
  </files>
  <action>
    STEP 1 (verdict classifier):
    Add `def classify_verdict(record)` to `bench/eval-openai-compat.py`. Rules per probe label
    (matches RESEARCH.md preliminary findings — bake the expected outcomes from RESEARCH.md as the
    reference; the harness diverges from these only if mlx_lm.server changed since 2026-05-06):

    - 01-baseline-chat: PASS if http_code==200 and response_excerpt contains assistant content; else NON-CONFORMANT.
    - 02-completions-legacy: PASS if http_code==200 and response_excerpt contains '"object": "text_completion"'.
    - 03-models-list: PASS if http_code==200 and response_excerpt contains '"data"'.
    - 04-health-endpoint: PASS if http_code==200.
    - 05-responses-endpoint: LOW if http_code==404 (expected absence). PASS if 200 (mlx_lm 0.31.3+ added it; surprising).
    - 06-response-format-json-object: HIGH NON-CONFORMANT if http_code==200 and response is prose (RESEARCH.md preliminary 1).
      PASS if response is valid JSON object (would mean server now honors the field — major upgrade).
    - 07-response-format-json-schema-strict: HIGH NON-CONFORMANT if response is markdown-fenced or hallucinates fields.
      PASS if matches schema exactly.
    - 08-response-format-no-rerun-N1: same rules as 06.
    - 09-mid-conv-system-rejected: PASS if http_code==404 + error contains "System message must be at the beginning"
      (CONFIRMS Phase 17-02+20-03 invariant). HIGH NON-CONFORMANT (regression) if 200.
    - 10-system-only-at-start: PASS if http_code==200.
    - 11-stream-baseline: LOW NON-CONFORMANT if role_chunks_count > 1 (role on every chunk per RESEARCH.md preliminary 9).
      PASS if role_chunks_count == 1.
    - 12-stream-with-usage: PASS if saw_done == True (per RESEARCH.md Pitfall §5).
    - 13-stream-finish-stop: PASS if finish_reason_seen == "stop".
    - 14-stream-finish-length: PASS if finish_reason_seen == "length".
    - 15-tools-tool-choice-auto: PASS if response_excerpt contains '"finish_reason": "tool_calls"' AND 'get_weather'
      in arguments (RESEARCH.md preliminary 8 verdict CONFORMANT).
    - 16-malformed-json-body: LOW (expected divergence) if http_code==400 and response shape is `{"error": "<string>"}`
      (RESEARCH.md preliminary 5).
    - 17-bogus-model-id: MEDIUM (expected divergence) if http_code==404 and response is HF-error-string.
    - 18-missing-model-field: MEDIUM (surprising) if http_code==200 and response.model contains "default_model".
    - 19-n-greater-than-1: LOW (silent ignore) if http_code==200 and choices.length==1.
    - 20-response-format-rate-temp0-N5: HIGH if valid_json_count < 5 (5 of 5 must validate to be CONFORMANT;
      typically 0 of 5 will validate). The probe IS its own statistical observation.
    - 21-no-response-format-rate-temp0-N5: informational only (LOW). Compare against probe 20 to detect
      whether response_format has subtle prompting effect.
    - 22-concurrent-pair: PASS if wall_clock_s < 0.7 * (elapsed_s_each[0] + elapsed_s_each[1])
      (proves parallel batching per RESEARCH.md preliminary 10).
    - 23/24/25-coherence-call-N: aggregate post-hoc — the report joins these 3 records and reports a single
      "multi-call coherence" verdict: PASS if each response contains the correct answer AND no leakage from
      prior call (e.g., 24's response should NOT contain "4" answering 23's "2+2"). HIGH if leakage detected.

    Each classified record gets a {verdict: PASS|NON-CONFORMANT|...; severity: PASS|HIGH|MEDIUM|LOW;
    mitigation: <one-line text from RESEARCH.md severity rubric>} added.

    STEP 2 (render_report):
    Add `def render_report(jsonl_path)` to `bench/eval-openai-compat.py`. Reads jsonl_path, classifies
    each record via classify_verdict(), then emits markdown to stdout with this structure:

    ```
    # Qwen 3.5 122B OpenAI Compatibility — Empirical Conformance Report

    **Date:** <derived from jsonl mtime>
    **Server:** mlx_lm.server <version-from-probe-03-or-fingerprint> @ localhost:8001
    **Model:** /Users/ohama/llm-system/models/qwen122b
    **Source transcript:** `<jsonl_path>`
    **Reproduction:** `bash bench/eval-qwen35-122b.sh --openai-compat`

    ## Verdict Summary

    | Severity | Count | Notes |
    | HIGH | N | (lists labels) |
    | MEDIUM | N | (lists labels) |
    | LOW | N | (lists labels) |
    | PASS | N | (lists labels) |

    ## Findings by Severity

    ### HIGH (action required for v2.6)
    - **<label>** (<surface>): <one-line observation>. Mitigation: <one-line text>.

    ### MEDIUM (informational for v2.6; mitigation only)
    ...

    ### LOW (cosmetic / no action)
    ...

    ### PASS (positive findings)
    ...

    ## Per-Surface Tables

    ### Surface 1: Endpoint coverage
    | Probe | Endpoint | HTTP | Verdict | Severity | Notes |
    | 01-baseline-chat | POST /v1/chat/completions | 200 | PASS | PASS | baseline |
    ...

    ### Surface 2: response_format
    ...

    (One table per RESEARCH.md surface — 8 tables total; probes 23/24/25 collapse into one Surface 6 row)

    ## Multi-call coherence detail
    Three sequential probes (23/24/25): expected answer 4 / 6 / 10. Observed: <table of 3 rows with
    correctness boolean + leakage check>. Verdict: <PASS|HIGH>.

    ## Out of scope
    - Thinking-mode behavior (`enable_thinking`) — set at server-launch via launchd plist; NOT a
      per-request field. Defer to separate investigation per RESEARCH.md Pitfall §4.
    - Concurrency knee at N>2 — RESEARCH.md Pitfall §1: don't disrupt daily driver. v2.7+ candidate.
    - `/v1/responses` endpoint shape if implemented — single-probe coverage in 05; deeper testing v2.7+.

    ## Implications for blueCode

    ### v2.6 mitigations already in place
    - Phase 17-02 + 20-03: mid-conv Role=System forbidden — CONFIRMED still required by probe 09.
    - Path-preference heuristic in `tryParseModelId` (CLAUDE.md "Key Seams") — CONFIRMED necessary by
      probe 17 (bogus model id triggers HF fetch).
    - `--chat-template-args '{"enable_thinking": false}'` in launchd plist — out of test scope; documented in
      Out-of-scope.

    ### v2.7+ candidates (file via `/gsd:add-todo` after this report lands)
    - Replace custom JSON-schema action DU with OpenAI tool calls (probe 15 PASS finding).
    - Wave-parallel exec (Phase F): server supports BatchGenerator decode-concurrency=32 (probe 22 PASS).

    ## Sources
    - 42-RESEARCH.md (research date, sources)
    - probes.jsonl (this run; <jsonl_path>)
    - mlx_lm 0.31.3 source code (server.py)
    - Phase 17-02 + 20-03 + 19 invariants (CLAUDE.md)
    ```

    Use `jsonschema` (installed in Plan 42-01) to validate response shapes ONLY for probes that
    aimed for PASS — e.g., probe 15 tool_calls envelope can be validated against an inline OpenAI
    schema snippet. Optional but recommended (graceful degrade if schema validation throws — log
    "schema validation skipped" and proceed).

    STEP 3 (wire `--render` flag in main()):
    Update `main()`'s argparse so `--render <path>` calls `render_report(path)` and prints to stdout
    (no --output-dir required in render mode). When `--render` is set, do NOT execute probes.

    STEP 4 (run render):
    Execute:
    ```
    LATEST=$(ls -td bench/runs/qwen35-eval-* | head -1)
    bench/.venv-eval/bin/python bench/eval-openai-compat.py --render "$LATEST/probes.jsonl" > documentation/qwen35-122b-openai-compat.md
    ```
    Confirm `wc -l documentation/qwen35-122b-openai-compat.md` ≥ 200.
    Spot-check the report by `grep -c '^### Surface' documentation/qwen35-122b-openai-compat.md` == 8.
    Spot-check that the HIGH severity section lists at least probes 06 + 07 + 20 (response_format
    HIGH findings per RESEARCH.md preliminary 1+2).
  </action>
  <verify>
    `python -m py_compile bench/eval-openai-compat.py` clean.
    `bench/eval-openai-compat.py --render <latest-jsonl>` exits 0 and emits markdown to stdout.
    `documentation/qwen35-122b-openai-compat.md` exists, ≥ 200 lines.
    `grep -c '^### Surface' documentation/qwen35-122b-openai-compat.md` == 8.
    `grep -c '^### HIGH' documentation/qwen35-122b-openai-compat.md` == 1.
    `grep -c '^### PASS' documentation/qwen35-122b-openai-compat.md` == 1.
    `grep -c '06-response-format-json-object' documentation/qwen35-122b-openai-compat.md` ≥ 2 (cited in HIGH section + Surface 2 table).
    `grep -c '15-tools-tool-choice-auto' documentation/qwen35-122b-openai-compat.md` ≥ 2 (cited in PASS section + Surface 8 table or similar).
    `grep -c 'v2.7' documentation/qwen35-122b-openai-compat.md` ≥ 1 (tools-as-action-DU candidate surfaced).
  </verify>
  <done>
    `--render` mode works. `documentation/qwen35-122b-openai-compat.md` exists with 8 surface
    tables + verdict summary + per-severity findings + reproduction instructions + v2.7+ candidate
    callouts. Reproducible from probes.jsonl alone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Conditionally update CLAUDE.md Key Seams + run final post-flight bench gate</name>
  <files>
    CLAUDE.md
  </files>
  <action>
    STEP 1 (CLAUDE.md update — CONDITIONAL):
    Read the rendered `documentation/qwen35-122b-openai-compat.md`. If the HIGH-severity findings
    list contains ANY finding that affects an EXISTING CLAUDE.md "Key Seams" entry, update that
    entry. Likely cases (but only update if probe data confirms):

    Case A: Mid-conv System rejection (probe 09) — already documented in CLAUDE.md as Phase
    17-02+20-03 invariant. NO update needed unless probe 09 unexpectedly PASSED with 200 (regression
    indicator).

    Case B: HuggingFace fallback trap (probe 17) — already documented in CLAUDE.md "Connection
    refused or 300s timeout" + `tryParseModelId` heuristic. NO update needed unless probe 17 found
    a divergence.

    Case C: response_format HIGH findings (probes 06/07/20) — NEW seam-adjacent observation.
    Add a one-block note in CLAUDE.md "Key Seams" or "Common Gotchas" section pointing to the new
    report and the rule "blueCode v2.6+ MUST NOT rely on `response_format`; use prompt-instructed
    schema with retry policy". Place the note near the existing "tryParseModelId" / "buildRequestBody"
    seam discussion (CLAUDE.md "Key Seams (v1.1)" → "Model id flow" block).

    Case D: tools/tool_choice PASS finding (probe 15) — NEW positive observation. Add a one-line
    "v2.7+ candidate" note in CLAUDE.md "Common Gotchas" or as a footnote near "Don't Do" section
    pointing readers to `documentation/qwen35-122b-openai-compat.md` for the full v2.7+ design
    sketch.

    For each update applied, keep it concise (5-15 lines max). The full empirical detail belongs
    in `documentation/qwen35-122b-openai-compat.md`; CLAUDE.md just needs a one-line pointer + the
    actionable rule.

    DO NOT update CLAUDE.md if NO HIGH-severity finding requires a change AND no surprising new
    PASS finding (case D) deserves a callout. Prefer minimal edits — surface the report path once
    in the "Bench" section as a new bullet:
    ```
    - `documentation/qwen35-122b-openai-compat.md` — Phase 42 empirical OpenAI-compat conformance report (8 surfaces, 25 probes). Reproduce via `bash bench/eval-qwen35-122b.sh --openai-compat`.
    ```

    STEP 2 (FINAL post-flight bench gate — MANDATORY):
    Run `bash bench/run.sh --gate` one final time. Confirm 7/7 PASS. This is the milestone-wide
    invariant gate — Phase 42 closes only if 7/7 PASS holds. If anything regresses:
    1. Run `launchctl kickstart -k gui/$(id -u)/com.ohama.qwen122b`
    2. Wait until `curl -fsS http://127.0.0.1:8001/v1/models` returns 200
    3. Re-run `bash bench/run.sh --gate`
    4. If still failing, surface in 42-03-SUMMARY.md as a Phase 42 regression and DO NOT close the phase.

    STEP 3 (final write-out):
    Confirm artifacts:
    - `bench/eval-openai-compat.py` is the final ~350-450 LOC script with probe + render modes
    - `documentation/qwen35-122b-openai-compat.md` is the rendered report
    - `bench/runs/qwen35-eval-<final-ts>/probes.jsonl` is the canonical 25-probe transcript
    - `CLAUDE.md` either updated (one-block pointer) or untouched (justify in summary)

    Note: if Plan 42-02 used a different LOG_DIR convention (e.g., `bench/runs/openai-compat-<ts>/`),
    adjust the report's "Reproduction" + "Source transcript" sections to match. Default per Plan 42-01
    is `bench/runs/qwen35-eval-<ts>/` (matches existing harness LOG_DIR pattern, line 34 of
    eval-qwen35-122b.sh). RESEARCH.md §Architecture Patterns suggested `openai-compat-<ts>/` but the
    harness LOG_DIR formula is `bench/runs/qwen35-eval-$(date +%Y%m%d-%H%M%S)`. Plan 42-01 inherits
    that. Document the actual path in the report (don't paper over the deviation from RESEARCH.md).
  </action>
  <verify>
    `bash bench/run.sh --gate` exits 0 with 7/7 PASS.
    `git status documentation/qwen35-122b-openai-compat.md` shows untracked or new (file exists).
    `git diff CLAUDE.md` is either empty (no update justified — document why in summary) OR shows a
    minimal additive edit (≤ 20 lines added).
    `git diff master -- src/` is empty (Phase 42 measurement-only invariant).
    `ls bench/runs/qwen35-eval-*/probes.jsonl | wc -l` ≥ 1 (canonical transcript present).
  </verify>
  <done>
    Final post-flight bench gate 7/7 PASS confirmed. `documentation/qwen35-122b-openai-compat.md` is
    on disk and reflects probes.jsonl. CLAUDE.md is either minimally updated (one-block pointer +
    optional case-C/D note) or intentionally untouched (justified). Phase 42 ready to close.
  </done>
</task>

</tasks>

<verification>
- `documentation/qwen35-122b-openai-compat.md` exists, ≥ 200 lines, has all 8 Surface tables.
- `bench/eval-openai-compat.py --render <jsonl>` regenerates the report deterministically.
- `bash bench/run.sh --gate` returns 7/7 PASS (final post-flight; phase-close invariant).
- `git diff master -- src/` empty.
- `git diff master -- bench/baseline.json` empty (byte-identical milestone-wide).
- jsonschema dep used (or graceful skip with log message) in render path.
- v2.7+ candidates (probe 15 tool-calls; probe 22 concurrency) explicitly named in the report's "Implications" section so user can `/gsd:add-todo` them.
</verification>

<success_criteria>
1. Empirical report `documentation/qwen35-122b-openai-compat.md` written from probes.jsonl.
2. Reproduction instructions documented in the report (LOG_DIR + render command).
3. CLAUDE.md updated minimally (one-block pointer) OR untouched with justification.
4. Final bench gate 7/7 PASS — milestone-wide invariant preserved.
5. v2.7+ candidates surfaced with /gsd:add-todo guidance.
6. Source code (src/) untouched throughout Phase 42.
</success_criteria>

<output>
After completion, create `.planning/phases/42-qwen-122b-openai-compat-test/42-03-SUMMARY.md` with:
- Final probes.jsonl path + record count
- Severity counts (HIGH / MEDIUM / LOW / PASS)
- HIGH findings list (one-line each) with mitigation pointer
- v2.7+ candidate todos to add via `/gsd:add-todo` (one per surface where PASS finding suggests blueCode could leverage native OpenAI behavior)
- CLAUDE.md edit summary (lines added / "untouched + reason")
- Final post-flight bench gate timestamp + 7/7 PASS confirmation
- Phase 42 close-out: ready for `/gsd:verify-work 42` UAT review
</output>
