---
phase: 42-qwen-122b-openai-compat-test
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - bench/eval-openai-compat.py
autonomous: true

must_haves:
  truths:
    - "User can re-run `bash bench/eval-qwen35-122b.sh --openai-compat` and get a probes.jsonl with ≥25 records (Plan 42-01's 10 + Plan 42-02's ≥15) covering all 8 RESEARCH.md surfaces"
    - "Streaming probe correctly classifies SSE chunk shape using `requests.iter_lines()` (chunk count, role-on-every-chunk observation, [DONE] presence/absence with vs without stream_options.include_usage)"
    - "Schema/error probes capture mlx_lm.server's flat `{error: <string>}` envelope shape (not OpenAI's `{error:{message,type,code}}`) for HTTP 400/404/422 cases"
    - "Concurrency probe N=2 (ONLY — per RESEARCH.md Pitfall §1: don't disrupt daily driver) confirms BatchGenerator parallel batching, no 429"
    - "Tools/tool_choice probe confirms PASS finding from RESEARCH.md preliminary 8 (`tool_calls` envelope returned correctly) — surfaced as v2.7+ candidate, not consumed in v2.6"
  artifacts:
    - path: bench/eval-openai-compat.py
      provides: "Extended PROBES list to ~25 entries; new helper `probe_stream(label, body, max_tokens)` using `requests.iter_lines()`; `probe_concurrent(labels, bodies)` using threading or asyncio for N=2; reused probe() for error+schema cases"
      min_lines: 280
  key_links:
    - from: bench/eval-openai-compat.py
      to: http://127.0.0.1:8001/v1/chat/completions
      via: "stream=True for streaming probes; ThreadPoolExecutor or asyncio.gather for N=2 concurrency probe"
      pattern: 'iter_lines|ThreadPoolExecutor|asyncio'
    - from: bench/eval-openai-compat.py
      to: bench/runs/qwen35-eval-<ts>/probes.jsonl
      via: "append-mode write per probe (not overwrite); --output-dir is reused so re-running 42-02 against an existing dir merges; OR plan executor passes a fresh dir and re-runs Plan 42-01 probes too"
      pattern: 'open.*"a"|append'
---

<objective>
Extend `bench/eval-openai-compat.py` PROBES list with ~15 more probes covering RESEARCH.md
Surfaces 4+5+7+8 (streaming SSE, schema enforcement, error surface, concurrency). Add the
specialized helpers needed for SSE stream parsing (`requests.iter_lines()` state machine)
and concurrency (N=2 simultaneous via ThreadPoolExecutor). Plus the tools/tool_choice probe
that RESEARCH.md preliminary 8 already proved CONFORMANT — confirms the finding under
the harness for transcript completeness.

Purpose: Complete the empirical test surface so Plan 42-03 has a full ~25-probe transcript
to render into the markdown report. After Plan 42-02, the JSONL is the AUTHORITATIVE source
of fact for "is Qwen 122B OpenAI-compatible?" — Plan 42-03's job is purely shape/render/
mitigation-extraction.

Output: `bench/eval-openai-compat.py` grown from ~150 LOC (Plan 42-01) to ~280-350 LOC; one
re-run of `--openai-compat` mode emits a ≥25-record probes.jsonl.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-qwen-122b-openai-compat-test/42-RESEARCH.md
@.planning/phases/42-qwen-122b-openai-compat-test/42-01-SUMMARY.md
@bench/eval-openai-compat.py
@bench/eval-qwen35-122b.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add streaming + tools probes (Surfaces 4 + tool-calls extension)</name>
  <files>
    bench/eval-openai-compat.py
  </files>
  <action>
    STEP 1 (streaming helper):
    Add `def probe_stream(label, category, body, expected, severity_hint, max_tokens=64, timeout=120)`
    to `bench/eval-openai-compat.py`. Behavior per RESEARCH.md §"Pattern 2: Streaming probe via
    Python requests.iter_lines()":
    - Merge body with `{"model": MODEL_PATH, "max_tokens": max_tokens, "stream": True}`
    - `requests.post(..., stream=True, timeout=timeout)`
    - Iterate `r.iter_lines(decode_unicode=True)` and classify each line:
      * empty / whitespace-only → skip
      * starts with `:` → keepalive (count separately)
      * starts with `data: [DONE]` → done sentinel (set `saw_done=True`)
      * starts with `data: {` → parse JSON; classify by `delta` keys (role / content / tool_calls / finish_reason / usage)
      * else → "other" (count + first 80 chars sampled into record)
    - Emit one summary record:
      {label, category, http_code, chunk_count, keepalive_count, role_chunks_count,
       content_chunks_count, finish_reason_seen, saw_done, first_chunk_keys, last_chunk_keys,
       elapsed_s, expected, severity_hint}
    - Also store `first_chunk_excerpt` and `last_chunk_excerpt` (first 200 chars each) for the report.
    - Wrap in try/except so probe stalls/timeouts emit an `error` record but don't abort the suite.

    STEP 2 (streaming probes — append to PROBES, 4 entries):
      - "11-stream-baseline" — `probe_stream`, body={messages:[{user,"count to 3"}]}, max_tokens=12
        — RESEARCH.md preliminary 9 confirms role on every chunk. expected="role on every content
        chunk; multiple data chunks; no [DONE] without include_usage". severity_hint="LOW".
      - "12-stream-with-usage" — `probe_stream`, body={messages:[{user,"count to 3"}], stream_options:{include_usage:true}}, max_tokens=12
        — RESEARCH.md Pitfall §5: with include_usage, [DONE] IS emitted. expected="saw_done=True; final usage chunk".
        severity_hint="LOW".
      - "13-stream-finish-stop" — `probe_stream`, body={messages:[{user,"Say only the word OK and nothing more."}]},
        max_tokens=8 — expected="finish_reason='stop' observed before stream end". severity_hint="LOW".
      - "14-stream-finish-length" — `probe_stream`, body={messages:[{user,"Tell me a long story about a dragon"}]},
        max_tokens=8 — expected="finish_reason='length' observed (max_tokens hit)". severity_hint="LOW".

    STEP 3 (tools probe — append to PROBES, 1 entry):
      - "15-tools-tool-choice-auto" — uses regular `probe()`, body={
        messages:[{user,"What is the weather in Paris?"}],
        tools:[{type:"function",function:{name:"get_weather",description:"Get weather",
        parameters:{type:"object",properties:{city:{type:"string"}},required:["city"]}}}],
        tool_choice:"auto"}, max_tokens=120 — RESEARCH.md preliminary 8 verdict CONFORMANT.
        expected="finish_reason=tool_calls; message.tool_calls[0].function.name=get_weather".
        severity_hint="PASS". (Recommendation per RESEARCH.md: surface as v2.7+ todo via /gsd:add-todo
        in Plan 42-03 — Plan 42-02 just confirms the empirical finding.)

    STEP 4 (run end-to-end + spot-check):
    Run `bash bench/eval-qwen35-122b.sh --openai-compat`. Confirm probes.jsonl has 15 records
    (10 from Plan 42-01's PROBES still in the list + 5 added in Task 1). Spot-check probe 11 by
    `jq 'select(.label=="11-stream-baseline") | {chunk_count, role_chunks_count, saw_done}'` —
    expect chunk_count > 1, role_chunks_count == content_chunks_count (RESEARCH.md preliminary 9
    finding), saw_done == False (no include_usage). Spot-check probe 12: `saw_done == True`.
    Spot-check probe 15: `jq '.response_excerpt' | grep -c tool_calls` >= 1.
  </action>
  <verify>
    `python -m py_compile bench/eval-openai-compat.py` clean.
    `wc -l bench/runs/qwen35-eval-*/probes.jsonl` (most recent) == 15.
    `jq -r '.label' bench/runs/qwen35-eval-*/probes.jsonl | tail -5 | sort` lists 11/12/13/14/15.
    `jq 'select(.label=="11-stream-baseline") | .role_chunks_count' bench/runs/qwen35-eval-*/probes.jsonl`
    > 1 (role on every chunk per RESEARCH.md preliminary 9).
    `jq 'select(.label=="12-stream-with-usage") | .saw_done' bench/runs/qwen35-eval-*/probes.jsonl`
    is `true`.
    `jq 'select(.label=="15-tools-tool-choice-auto") | .response_excerpt' bench/runs/qwen35-eval-*/probes.jsonl`
    contains "tool_calls".
  </verify>
  <done>
    Streaming + tools probes added. probes.jsonl now has 15 records (10 from Plan 42-01 + 5 new).
    SSE shape (role-on-every-chunk, [DONE] gating on include_usage) empirically captured. Tools
    PASS finding reproduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add error-surface + schema-enforcement + multi-call coherence + concurrency probes (Surfaces 5+6+7+8)</name>
  <files>
    bench/eval-openai-compat.py
  </files>
  <action>
    STEP 1 (concurrency helper):
    Add `def probe_concurrent_pair(label_a, body_a, label_b, body_b, max_tokens=8, timeout=60)`
    using `concurrent.futures.ThreadPoolExecutor(max_workers=2)` (simpler than asyncio for one-shot
    pair). Both calls go to /v1/chat/completions. Capture per-request elapsed_s + http_code, plus
    pair-level `wall_clock_s` (start before submit, end after both join). Emit ONE summary record
    with shape:
      {label, category="concurrency", http_codes:[a,b], elapsed_s_each:[a,b],
       wall_clock_s, expected, severity_hint}
    The point: if wall_clock_s ≈ max(elapsed_s_each) → batched in parallel (PASS). If
    wall_clock_s ≈ sum(elapsed_s_each) → serialized (NON-CONFORMANT to RESEARCH.md preliminary 10).
    DO NOT do N>2 (RESEARCH.md Pitfall §1: don't disrupt daily driver; v2.7+ knee-finding is out of scope).

    STEP 2 (probes — append to PROBES, 10 more):

    SURFACE 7 — error surface (4 probes):
      - "16-malformed-json-body" — `probe()` but with raw `data='NOT JSON'` (not a JSON dict) — extend
        probe() with optional `raw_body=None` param so it can pass `data=raw_body` instead of `json=full`.
        expected HTTP 400 + `{"error":"Invalid JSON in request body..."}` per RESEARCH.md preliminary 5.
        severity_hint="LOW".
      - "17-bogus-model-id" — body={model:"BOGUS_MODEL", messages:[{user,"hi"}]} — but DO NOT use the default
        model merge: extend probe() to allow body.model override. expected HTTP 404 + HF-error string
        per RESEARCH.md preliminary 4. severity_hint="MEDIUM".
        SAFETY NOTE: must NOT submit a real-but-incorrect HF id (e.g., "Qwen/Qwen2.5-Coder-32B") —
        RESEARCH.md Pitfall §3 says that WOULD swap the tokenizer. "BOGUS_MODEL" fails earlier (HF
        metadata-fetch step).
      - "18-missing-model-field" — body={messages:[{user,"hi"}]}, but suppress the model merge — extend
        probe() with `omit_model_field=False` flag. expected HTTP 200 + response.model=="default_model"
        per RESEARCH.md preliminary 6. severity_hint="MEDIUM".
      - "19-n-greater-than-1" — body={messages:[{user,"hi"}], n:3}, max_tokens=4 — expected HTTP 200,
        choices.length==1 (silently ignored) per RESEARCH.md preliminary 3. severity_hint="LOW".

    SURFACE 5 — schema-rate at temp=0 with response_format (1 statistical probe + 1 control):
      - "20-response-format-rate-temp0-N5" — Run probe() N=5 with body={messages:[{user,"Return one
        user with name and age as JSON"}], response_format:{type:"json_object"}, temperature:0.0},
        max_tokens=80. Aggregate into ONE record: {label, category="response_format_stat", http_codes:[5x],
        valid_json_count (parsing each .choices[0].message.content as JSON; success counter),
        prose_wrap_count (content starts with "```json"), expected, severity_hint="HIGH"}. Reuses probe()
        in a loop (don't extract separate helper — keep it inline in PROBES handler in main()).
      - "21-no-response-format-rate-temp0-N5" — control: same body MINUS response_format. If valid_json_count
        differs significantly between 20 and 21, response_format has subtle prompting effect (RESEARCH.md
        Open Question 2). severity_hint="LOW" (informational).

    SURFACE 8 — concurrency (1 probe):
      - "22-concurrent-pair" — `probe_concurrent_pair`,
        body_a={messages:[{user,"say A"}]}, body_b={messages:[{user,"say B"}]}.
        expected="wall_clock ≈ max(elapsed_each), not sum — confirms BatchGenerator parallel decode per
        RESEARCH.md preliminary 10". severity_hint="PASS" if conformant.

    SURFACE 6 — multi-call coherence (3 probes — issued sequentially, examined as a triple):
      - "23-coherence-call-1" — `probe()`, body={messages:[{user,"What is 2+2?"}]}, max_tokens=8.
      - "24-coherence-call-2" — IMMEDIATELY after 23 (same suite run, no sleep), body={messages:[{user,"What is 3+3?"}]}, max_tokens=8.
      - "25-coherence-call-3" — IMMEDIATELY after 24, body={messages:[{user,"What is 5+5?"}]}, max_tokens=8.
      Plan 42-03 will join these three records and check that response_excerpt of each contains the
      correct answer (4 / 6 / 10) AND no leakage from prior call (e.g., 24's response should not contain
      "4" answering call 23). severity_hint="PASS" if isolated; "HIGH" if leakage.

    NOTE on probe() extensions needed (do them once in Task 2 step 1):
    - `path` parameter (already added in Task 2 of Plan 42-01)
    - `raw_body=None` (for malformed-JSON probe) — when set, use `data=raw_body` not `json=full`
    - `omit_model_field=False` (for probe 18) — when True, don't merge MODEL_PATH into body
    - `body_model_override=None` (for probe 17) — overrides MODEL_PATH with bogus value
    - `temperature` and `n` already supportable via passing through `body`

    STEP 3 (run end-to-end + post-flight gate):
    Run `bash bench/eval-qwen35-122b.sh --openai-compat`. Confirm probes.jsonl has 24 records
    (15 from Task 1 cumulative + 9 new from Task 2 — note probe 20 and 21 are SINGLE records with
    aggregated N=5 stats; concurrency probe 22 is single record; coherence 23/24/25 are 3 separate
    records). Total: 10 (Plan 42-01) + 5 (Task 1) + 4 (16-19) + 2 (20-21 stat) + 1 (22) + 3 (23-25) = 25 records.

    POST-FLIGHT BENCH GATE: Run `bash bench/run.sh --gate`. Must be 7/7 PASS. Plan 42-02 has
    introduced the riskiest probes (concurrency, malformed JSON, bogus model) — if KV state is
    contaminated, this is where it surfaces. If gate fails, run `launchctl kickstart -k
    gui/$(id -u)/com.ohama.qwen122b` and re-run gate; if still fails, abort and report to Plan 42-03.
  </action>
  <verify>
    `python -m py_compile bench/eval-openai-compat.py` clean.
    `wc -l bench/runs/qwen35-eval-*/probes.jsonl` (most recent) == 25.
    `jq -r '.label' bench/runs/qwen35-eval-*/probes.jsonl | grep -c '^"' ` returns 25 (or
    `jq -r '.label' ... | wc -l` returns 25).
    `jq 'select(.label=="17-bogus-model-id") | .http_code' bench/runs/qwen35-eval-*/probes.jsonl` == 404.
    `jq 'select(.label=="18-missing-model-field") | .response_excerpt' bench/runs/qwen35-eval-*/probes.jsonl`
    contains "default_model".
    `jq 'select(.label=="22-concurrent-pair") | .wall_clock_s' bench/runs/qwen35-eval-*/probes.jsonl`
    is less than `(elapsed_s_each | add)` (proves parallelism).
    `bash bench/run.sh --gate` post-flight returns 7/7 PASS.
  </verify>
  <done>
    Plan 42-02 complete. probes.jsonl has 25 records covering all 8 RESEARCH.md surfaces. Streaming,
    tools, error envelope, concurrency, multi-call coherence all empirically captured. Bench gate
    7/7 PASS post-flight. Hand-off to Plan 42-03 for rendering.
  </done>
</task>

</tasks>

<verification>
- `python -m py_compile bench/eval-openai-compat.py` clean.
- `wc -l bench/eval-openai-compat.py` ≥ 280.
- `wc -l bench/runs/qwen35-eval-*/probes.jsonl` (post-Task 2 latest) == 25.
- All 8 RESEARCH.md surfaces represented (use `jq -r '.category' ... | sort -u` and confirm
  endpoint, response_format, response_format_stat, role, streaming, tools, error, concurrency,
  coherence categories present).
- Bench gate 7/7 PASS post-Task 2.
- No new src/ diff: `git diff master -- src/` empty.
- No NuGet additions; jsonschema NOT yet imported in eval-openai-compat.py (Plan 42-03 will use it for shape validation in rendering).
</verification>

<success_criteria>
1. probes.jsonl ≥ 25 records covering Surfaces 1-8.
2. Streaming chunk-shape state machine works (role/content/done classification).
3. Concurrency N=2 probe confirms BatchGenerator parallel batching.
4. Tools probe confirms PASS finding (positive surprise to surface in Plan 42-03 as v2.7+ candidate).
5. Bench gate 7/7 PASS post-Plan-42-02.
6. Source code (src/) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/42-qwen-122b-openai-compat-test/42-02-SUMMARY.md` with:
- Final probes.jsonl path (LOG_DIR)
- Probe count breakdown by category (8 categories per Surfaces 1-8)
- Per-category one-line empirical verdict (PASS/HIGH/MEDIUM/LOW count)
- Any divergence from RESEARCH.md preliminary findings (esp. if mlx_lm.server has been bumped past 0.31.3 between research and execution — check via probe 03 `system_fingerprint` field if exposed)
- Post-flight bench gate result (7/7 PASS)
- Hand-off to Plan 42-03: jsonl path + count + readiness for rendering
</output>
