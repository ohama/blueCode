---
phase: 17-qwen-3-5-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documentation/qwen35-install.md
autonomous: true

must_haves:
  truths:
    - "A reader who has only `~/llm-system/env/qwen-env` set up can follow `documentation/qwen35-install.md` end-to-end (verify mlx_lm version → download both 4-bit MLX models → drop launchd plists in place → load services → smoke-test thinking-mode disable + Instruct tokenizer present) without consulting any other doc."
    - "The doc explicitly distinguishes Qwen 3.5's NEW failure mode (thinking-mode tokens leaking into the JSON response) from the v1.x Base-vs-Instruct trap. Both diagnostic prompts (chat smoke + JSON smoke) are runnable copy-paste curl commands with EXPECTED-vs-UNEXPECTED output pattern tables."
    - "The unified-memory budget table compares 32B+72B (~58.8 GB pair) vs 35B+122B 4-bit (~89.5 GB pair) on the user's 128 GB Mac and notes the ~30 GB tighter headroom; explicit warnings about memory-hungry apps during cold start."
    - "The doc names the SPECIFIC mitigation path for the thinking-mode trap: Path A = `--chat-template-kwargs '{\"enable_thinking\": false}'` server flag in launchd plist; Path B = `chat_template_kwargs` field in `buildRequestBody` POST body (~5-line F# patch in QwenHttpClient.fs). Path A is preferred; Path B is the fallback IF Path A is empirically unavailable in the installed mlx_lm. The verify procedure for choosing between them lives in this doc (the actual switch happens in 17-02)."
    - "The doc includes the EXACT launchd plist contents for both com.ohama.qwen35b.plist (port 8000) and com.ohama.qwen122b.plist (port 8001), inline as XML blocks the user can copy verbatim, mirroring the §3.1/§3.2 plist sections of `documentation/local-llm-services.md`."
    - "An mlx_lm version check (`>= 0.25.2`) is the FIRST install step; the doc fails closed if the user's installed version is older."
  artifacts:
    - path: "documentation/qwen35-install.md"
      provides: "Operational runbook for installing, loading, swapping, and verifying the Qwen 3.5 35B-A3B-4bit (port 8000) + 122B-A10B-4bit (port 8001) MLX services on a 128 GB Mac, with explicit Base/Instruct + thinking-mode dual-trap diagnostic protocols."
      min_lines: 250
      contains: "enable_thinking"
  key_links:
    - from: "documentation/qwen35-install.md (§3 launchd plist for 35B)"
      to: "documentation/qwen35-install.md (§5 Thinking-Mode Verification — Path A smoke test)"
      via: "the plist's --chat-template-kwargs flag is verified by the §5 smoke test; if §5 fails, §6 (Path B fallback) is invoked"
      pattern: "chat-template-kwargs|chat_template_kwargs"
    - from: "documentation/qwen35-install.md (§7 Memory budget)"
      to: "documentation/qwen35-install.md (§8 Cold-start wait procedure)"
      via: "tight memory means cold start of 122B may exceed blueCode's 180s HttpClient.Timeout — manual `until curl /v1/models` wait is the documented mitigation"
      pattern: "until curl"
    - from: "documentation/qwen35-install.md (§4 Base-vs-Instruct verification)"
      to: "documentation/qwen32b-base-to-instruct.md (sibling doc — analogous v1.x trap)"
      via: "explicit cross-reference: 'For the v1 Coder/Instruct confusion that Qwen 2.5 had, see qwen32b-base-to-instruct.md. The Qwen 3.5 trap is structurally similar (verbose non-JSON output) but the cause is different: thinking mode, not Base model.'"
      pattern: "qwen32b-base-to-instruct"
---

<objective>
Author `documentation/qwen35-install.md` — a single self-contained runbook that takes the user from "Qwen 3.5 not installed" to "both services up, both verified Instruct + non-thinking, ready for Phase 17-02 service swap." The doc mirrors `documentation/local-llm-services.md` (operational shape: Python venv → model download → launchd plist → load → verify → recover) and `documentation/qwen32b-base-to-instruct.md` (verification protocol shape: symptom table → diagnostic curl → expected/unexpected output table) but is a SIBLING, NOT a replacement — the 32B/72B doc stays valid as long as those services exist (they do, until 17-02 swaps them out).

Purpose: 17-01 ships docs only. No service touches the system. The doc is the prerequisite that the user reads BEFORE 17-02's checkpoints fire. After 17-01, the user has a written procedure they can follow during 17-02's manual model-download + launchctl-swap step. The doc also encodes the empirical "is Path A available?" decision so 17-02 can branch cleanly.

Output:
- New file `documentation/qwen35-install.md` (~300-450 lines) with sections §1-§9 enumerated in Task 1's action.
- No service load/unload. No model download. No code changes. PURE documentation.
- Korean+English bilingual prose is OK if the existing local-llm-services.md style is followed (mostly Korean section headers + English code blocks). Mirroring is the goal; deviation is fine where Qwen 3.5 specifics demand new content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-qwen-3-5-evaluation/17-RESEARCH.md
@CLAUDE.md
@documentation/local-llm-services.md
@documentation/qwen32b-base-to-instruct.md
@src/BlueCode.Cli/Adapters/QwenHttpClient.fs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author documentation/qwen35-install.md (sections §1-§9)</name>
  <files>documentation/qwen35-install.md</files>
  <action>
Create `documentation/qwen35-install.md` from scratch. The doc has nine numbered sections. Each section's purpose, required content, and the source-of-truth from research are listed below — write the section by synthesizing the source content into runnable prose + commands. DO NOT paraphrase the research vaguely; copy the runnable commands verbatim where research provided them.

**Top of file (no number — header block):**

```markdown
# Qwen 3.5 35B + 122B 설치 및 운영 가이드 (8000 / 8001 candidate)

blueCode가 현재 Qwen 2.5 32B/72B 페어를 사용한다. 이 문서는 그 페어를 Qwen 3.5
35B-A3B / 122B-A10B (둘 다 MoE, 둘 다 MLX 4-bit) 페어로 **평가하기 위한** 설치
절차다. v2.0 Phase 17의 산출물이며, **교체를 확정하기 전에** 두 페어 모두 디스크에
공존하는 시점이 존재한다 (롤백 가능).

> **핵심 차이 한 줄**: Qwen 3.5는 dense가 아니라 MoE (Mixture-of-Experts).
> 35B-A3B = 35B total / 3B activated; 122B-A10B = 122B total / 10B activated.
> 즉 추론 속도는 dense 35B보다 훨씬 빠르고, 메모리는 4-bit 기준 ~20GB / ~70GB.

> **새로운 함정**: Qwen 3.5는 기본적으로 `<think>...</think>` 토큰을 응답 앞에
> 붙인다 (thinking mode). blueCode의 strict JSON 스키마 검증과 호환되지 않는다.
> §5 검증을 반드시 통과시켜야 한다.

**관련 문서**:
- `documentation/local-llm-services.md` — 현 32B/72B 페어 운영 (이 페어가 살아있는 동안 유효)
- `documentation/qwen32b-base-to-instruct.md` — v1 Base/Coder/Instruct 함정 (구조적으로 유사하나 원인은 다름)
- `.planning/phases/17-qwen-3-5-evaluation/17-RESEARCH.md` — 본 가이드의 단일 진실 소스
```

---

**§1 — 사전 점검 (mlx_lm 버전, 디스크 여유, 기존 서비스 상태):**

Mirror the shape of `local-llm-services.md` §1 (a 3-row "원인/확인/정상" table). Required content:

- mlx_lm `>= 0.25.2` 검증 명령:
  ```bash
  source ~/llm-system/env/qwen-env/bin/activate
  python3 -c "import mlx_lm; print(mlx_lm.__version__)"
  # < 0.25.2 면: pip install --upgrade mlx-lm 후 재확인
  ```
- 디스크 여유 ≥ 150 GB (다운로드 ~90 GB + 기존 ~55 GB + 헤드룸; 35B/122B는 기존 32B/72B와 공존시킨다)
- 기존 서비스 상태 (살아있어야 한다 — 다운로드 도중 32B/72B는 계속 동작):
  ```bash
  launchctl list | grep ohama   # qwen32b, qwen72b 두 줄 보이면 OK
  ```
- 메모리 여유 권장 (Chrome/Xcode/VM 등 GB 단위 점유 프로세스 종료 권장 — 콜드 스타트 시 OOM 방지)

---

**§2 — 디렉토리 컨벤션:**

```
~/llm-system/
├── models/
│   ├── qwen32b/        # 기존 — 17-02 swap 확정 전까지 유지 (롤백 자산)
│   ├── qwen72b/        # 기존 — 동일
│   ├── qwen35b/        # 신규 — Qwen3.5-35B-A3B-4bit (~20.4 GB on disk)
│   └── qwen122b/       # 신규 — Qwen3.5-122B-A10B-4bit (~69.6 GB on disk)
└── ... (env, services, launchd 폴더는 §2.2 of local-llm-services.md와 동일)
```

명시적으로 적어둔다: 기존 32B/72B 모델 파일을 **삭제하지 않는다**. 17-03이 SWITCH 결정을 내리고 1주 이상 안정 운영된 후에야 정리 후보가 된다 (그조차도 본 Phase의 OOS).

---

**§3 — 모델 다운로드:**

Verbatim from research §Install Procedure §Download commands:

```bash
source ~/llm-system/env/qwen-env/bin/activate

python3 - <<'PY'
from huggingface_hub import snapshot_download

# 35B-A3B 4-bit MLX (~20 GB disk)
snapshot_download(
    repo_id="mlx-community/Qwen3.5-35B-A3B-4bit",
    local_dir="/Users/ohama/llm-system/models/qwen35b",
    local_dir_use_symlinks=False,
)

# 122B-A10B 4-bit MLX (~70 GB disk)
snapshot_download(
    repo_id="mlx-community/Qwen3.5-122B-A10B-4bit",
    local_dir="/Users/ohama/llm-system/models/qwen122b",
    local_dir_use_symlinks=False,
)
print("done")
PY
```

다운로드 검증 (파일 크기로 페이크/잘림 검출):

```bash
du -sh ~/llm-system/models/qwen35b   # 약 20G
du -sh ~/llm-system/models/qwen122b  # 약 70G
ls ~/llm-system/models/qwen35b/config.json ~/llm-system/models/qwen122b/config.json
```

두 `config.json` 모두 존재해야 한다. 다운로드 도중 끊긴 경우 동일 명령으로 재시작 (snapshot_download는 resume 가능).

---

**§4 — launchd plist (Path A: server flag로 thinking 끄기):**

§4.1 `~/Library/LaunchAgents/com.ohama.qwen35b.plist` — full XML, copy-paste from research §Install Procedure §launchd plist templates §35B section. Include the `--chat-template-kwargs` `{"enable_thinking": false}` argument pair as separate `<string>` elements in `ProgramArguments`. Logs path: `/Users/ohama/llm-system/services/logs/35b.log` + `35b.err`.

§4.2 `~/Library/LaunchAgents/com.ohama.qwen122b.plist` — explicit instruction: copy 35B plist and change Label/--model path/--port (8001)/log paths. List the 4 exact diff lines.

§4.3 — Plist 검증:

```bash
plutil -lint ~/Library/LaunchAgents/com.ohama.qwen35b.plist
plutil -lint ~/Library/LaunchAgents/com.ohama.qwen122b.plist
# 둘 다 "OK" 출력
```

§4.4 — `--chat-template-kwargs` 플래그 가용성 사전 검증 (Path A 선택의 결정적 단계):

```bash
source ~/llm-system/env/qwen-env/bin/activate
python3 -m mlx_lm.server --help 2>&1 | grep chat-template-kwargs
```

| 출력 | 의미 | 다음 단계 |
|------|------|-----------|
| `--chat-template-kwargs ...` 한 줄 출력 | Path A 사용 가능 | §4.1/§4.2 plist 그대로 사용 |
| 출력 없음 | Path A 불가 | §6 (Path B 코드 패치) 적용 후 plist에서 `--chat-template-kwargs` 인자 2줄 제거 |

---

**§5 — 서비스 로드 + 검증 프로토콜:**

§5.1 — 로드 + 준비 대기:

```bash
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen122b.plist

# 35B는 ~30-60s, 122B는 ~120-240s. 타임아웃 없이 wait — 강제 인터럽트 금지.
until curl -fsS http://127.0.0.1:8000/v1/models > /dev/null 2>&1; do sleep 3; done && echo "35B ready"
until curl -fsS http://127.0.0.1:8001/v1/models > /dev/null 2>&1; do sleep 3; done && echo "122B ready"
```

§5.2 — Instruct tokenizer 검증 (Base 모델이 잘못 다운된 케이스 차단):

```bash
ls ~/llm-system/models/qwen35b/special_tokens_map.json \
   ~/llm-system/models/qwen35b/added_tokens.json
ls ~/llm-system/models/qwen122b/special_tokens_map.json \
   ~/llm-system/models/qwen122b/added_tokens.json
# 4개 파일 모두 존재해야 한다 (Instruct 표시).

python3 -c "
import json
for p in ['/Users/ohama/llm-system/models/qwen35b/tokenizer_config.json',
         '/Users/ohama/llm-system/models/qwen122b/tokenizer_config.json']:
    c = json.load(open(p))
    print(p, 'chat_template len:', len(c.get('chat_template','')), 'has <think>:', '<think>' in c.get('chat_template',''))
# 두 모델 모두: chat_template len > 2000, has <think>: True (정상 — Instruct + thinking 토큰 정의됨)
```

`<think>`가 `chat_template`에 등장하는 것은 **정상**이다 (Qwen 3.5 Instruct는 thinking 토큰을 정의함). 우리는 §5.3 단계에서 *런타임*에 thinking을 끈다.

§5.3 — Thinking-mode 무력화 검증 (Qwen 3.5 핵심 검증):

```bash
curl -s -X POST http://127.0.0.1:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "/Users/ohama/llm-system/models/qwen35b",
    "messages": [
      {"role": "system", "content": "You are a terse assistant. Respond with exactly one word."},
      {"role": "user", "content": "Say OK"}
    ],
    "max_tokens": 100,
    "temperature": 0.0
  }' | python3 -c "
import sys, json
r = json.load(sys.stdin)
content = r['choices'][0]['message']['content']
print('content:', repr(content))
print('PASS' if content.strip() == 'OK' and '<think>' not in content else 'FAIL')
"
```

응답 분류 표:

| `content` 값 | 진단 | 조치 |
|--------------|------|------|
| `'OK'` (대소문자/공백 무시) | Path A 동작 — thinking 비활성 | §5.4로 진행 |
| `'<think>...</think>OK'` 형태 | thinking 활성 — server flag 미반영 | §6 (Path B fallback) |
| `'<think>...'` 만 (비완료) | thinking이 max_tokens에 잘림 | §6 |
| 시스템 프롬프트 echo, FIM 토큰 | Base 모델 (`<\|fim_prefix\|>` 등) | §3 다운로드 자체가 잘못됨 — 재시작 |
| 빈 문자열 + `reasoning_content` 채워짐 | omlx-style 분리 응답 | §6 + extractContent 패치 (대안) |

§5.3 동일 검증을 122B (port 8001) 에 대해서도 반복한다.

§5.4 — JSON 스키마 출력 검증 (blueCode가 사용할 실제 응답 형태):

```bash
curl -s -X POST http://127.0.0.1:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "/Users/ohama/llm-system/models/qwen35b",
    "messages": [
      {"role": "system", "content": "Respond ONLY with valid JSON: {\"thought\": string, \"action\": \"final\", \"input\": {\"answer\": string}}"},
      {"role": "user", "content": "What is 2+2?"}
    ],
    "max_tokens": 200,
    "temperature": 0.0
  }' | python3 -c "
import sys, json
r = json.load(sys.stdin)
content = r['choices'][0]['message']['content']
try:
    obj = json.loads(content)
    print('JSON parse: OK; action=', obj.get('action'))
except Exception as e:
    print('JSON parse FAILED:', e); print('raw:', content[:300])
"
```

`JSON parse: OK; action= final` 이면 통과. 실패 시 §6.

---

**§6 — Path B fallback: F# QwenHttpClient.fs 패치 (Path A 불가 시에만):**

명시적 트리거: §4.4 (`--chat-template-kwargs` 미지원) 또는 §5.3/§5.4 실패 (서버 플래그가 무시됨).

패치 대상: `src/BlueCode.Cli/Adapters/QwenHttpClient.fs`, `buildRequestBody` 함수 (line ~57).

현재 (확인됨):
```fsharp
let req =
    {| model = modelId
       messages = msgArr
       temperature = modelToTemperature model
       max_tokens = 1024
       presence_penalty = 1.5
       stream = false |}
```

수정 후:
```fsharp
let req =
    {| model = modelId
       messages = msgArr
       temperature = modelToTemperature model
       max_tokens = 1024
       presence_penalty = 1.5
       stream = false
       chat_template_kwargs = {| enable_thinking = false |} |}
```

추가 라인 1개. Core 순수성 영향 없음 (Cli 어댑터 내부). 빌드: `dotnet build BlueCode.sln`. 테스트 통과 확인: `dotnet run --project tests/BlueCode.Tests/BlueCode.Tests.fsproj --summary | grep Passed:`. 패치 후 §5.3 재실행하여 `'OK'` 응답 확인.

**중요**: §6은 17-02에서 Path A 실패가 *경험적으로 확인된 후에만* 실행한다. 17-01은 이 절차를 문서화만 하고 코드를 건드리지 않는다.

---

**§7 — 통합 메모리 예산 (128 GB Mac):**

Mirror research §Memory Budget §Simultaneous load comparison table. 핵심 수치:

| 페어 | 모델 RAM | 잔여 (OS+캐시) | 여유도 |
|------|----------|----------------|--------|
| 현재 32B + 72B | 18.4 + 40.4 = **58.8 GB** | ~60 GB | 여유 있음 |
| 후보 35B + 122B (4-bit) | 19.5 + 70 = **89.5 GB** | ~38 GB | **타이트** (현재보다 ~30 GB 적음) |

운영 권고:
- 콜드 스타트 시 Chrome / Xcode / VM 종료 권장
- macOS 메모리 압축이 활성화되어 있어야 한다 (기본값)
- KV cache (8K context) 35B ~1 GB / 122B ~3 GB 추가 점유

OOM 발생 시 옵션:
1. 122B 만 로드, 35B 언로드 (단일 모델 워크플로우)
2. 35B 4-bit + 72B 유지 (혼합 페어)
3. 122B 3-bit 변종 (~60 GB) — 본 Phase OOS, v2.1 후보

---

**§8 — 콜드 스타트 + blueCode 180s timeout 회피:**

blueCode `QwenHttpClient.fs`의 `HttpClient.Timeout = 180s`는 72B 워스트케이스 기준이다. 122B는 콜드 스타트 시 180-240s 소요 가능하므로, 첫 프로브 호출이 타임아웃될 수 있다. 수동 대기 절차:

```bash
# blueCode 실행 전에:
until curl -fsS http://127.0.0.1:8001/v1/models > /dev/null 2>&1; do
  echo "waiting for 122B..."; sleep 5
done && echo "ready"

# 그 다음에야:
cd ~/projs/blueCode
dotnet run --project src/BlueCode.Cli -- --model 72b "smoke test"
```

`launchctl kickstart -k`로 강제 재기동 후에도 동일 절차 적용. (코드 변경 없이 운영으로 해결 — `Timeout = 300s`로 늘리는 옵션은 가능하나 본 Phase OOS.)

---

**§9 — 서비스 swap 절차 (17-02에서 사용):**

본 절은 17-02 checkpoint의 "사전에 읽고 따라할 절차"다. 본 Phase에서는 **실행하지 않는다** — 17-02가 user 동의를 받아 실행한다.

```bash
# 1) 기존 서비스 unload (KILL 금지 — KeepAlive가 재기동시킴; unload 사용)
launchctl unload ~/Library/LaunchAgents/com.ohama.qwen32b.plist
launchctl unload ~/Library/LaunchAgents/com.ohama.qwen72b.plist

# 2) 포트 해제 확인
lsof -iTCP:8000 -sTCP:LISTEN || echo "8000 released"
lsof -iTCP:8001 -sTCP:LISTEN || echo "8001 released"

# 3) 신규 서비스 load
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen122b.plist

# 4) Ready 대기
until curl -fsS http://127.0.0.1:8000/v1/models > /dev/null 2>&1; do sleep 3; done && echo "35B ready"
until curl -fsS http://127.0.0.1:8001/v1/models > /dev/null 2>&1; do sleep 3; done && echo "122B ready"

# 5) §5.2-§5.4 검증 전체 재실행
```

롤백 (35B/122B에 문제가 있을 때):

```bash
launchctl unload ~/Library/LaunchAgents/com.ohama.qwen35b.plist
launchctl unload ~/Library/LaunchAgents/com.ohama.qwen122b.plist
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen32b.plist
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen72b.plist
```

기존 plist + 모델 파일은 §2의 정책에 따라 디스크에 보존된다.

---

**문서 작성 시 주의:**

1. 헤더 스타일은 `local-llm-services.md`와 일치 (`## 1. 제목`, `### 1.1 부제`).
2. 명령 블록은 모두 ` ```bash` fence (또는 적절한 언어).
3. 파일 절대경로는 모두 `/Users/ohama/llm-system/...` (`~`은 prose에서만).
4. 인용/주의 박스는 `> ` blockquote (research에 있는 것을 그대로 옮긴다).
5. 섹션 사이 구분은 `---`.
6. 모든 launchd plist 내용은 (compact가 아닌) full XML로 인라인 (사용자가 그대로 복붙할 수 있어야 함).
7. 표(table)는 GFM `| col | col |` 문법.

빌드 검증 명령은 없다 (순수 docs). Lint 가능한 것은:
- `wc -l documentation/qwen35-install.md` — 250줄 이상이면 충분한 깊이.
- 모든 fenced code block이 닫혀 있는지: `awk '/^```/{c++} END{print c}' documentation/qwen35-install.md` — 짝수.
- 모든 섹션이 존재: `grep -c "^## " documentation/qwen35-install.md` ≥ 9.

Commit:
```bash
git add documentation/qwen35-install.md
git commit -m "docs(17-01): qwen35-install runbook (35B/122B MoE + thinking-mode mitigation)"
```
  </action>
  <verify>
1. `test -f documentation/qwen35-install.md && echo OK` returns OK.
2. `wc -l documentation/qwen35-install.md` ≥ 250 (target depth).
3. `grep -c "^## " documentation/qwen35-install.md` ≥ 9 (all numbered sections present).
4. `awk '/^\`\`\`/{c++} END{print c}' documentation/qwen35-install.md` is an even number (all fenced blocks closed).
5. `grep -c "enable_thinking" documentation/qwen35-install.md` ≥ 4 (Path A flag mentioned in §4 plist + §4.4 verify + §5.3 expected behavior + §6 Path B fallback).
6. `grep -c "chat_template_kwargs" documentation/qwen35-install.md` ≥ 2 (Path A `--chat-template-kwargs` flag and Path B request-body field).
7. `grep -c "mlx-community/Qwen3.5" documentation/qwen35-install.md` ≥ 2 (35B + 122B HF repo IDs).
8. `grep -c "0.25.2" documentation/qwen35-install.md` ≥ 1 (mlx_lm version requirement explicit).
9. `grep -c "qwen35b\|qwen122b" documentation/qwen35-install.md` ≥ 6 (path conventions used consistently).
10. `grep -c "launchctl unload\|launchctl load" documentation/qwen35-install.md` ≥ 4 (swap procedure documented in §9).
11. `grep -c "qwen32b-base-to-instruct" documentation/qwen35-install.md` ≥ 1 (sibling-doc cross-reference present).
12. `grep -c "180" documentation/qwen35-install.md` ≥ 1 (the 180s HttpClient.Timeout discussion in §8).
13. `grep -c "buildRequestBody" documentation/qwen35-install.md` ≥ 1 (Path B target function named).
14. `git log -1 --pretty=%s` shows `docs(17-01): qwen35-install runbook (35B/122B MoE + thinking-mode mitigation)`.
15. No code files modified: `git diff HEAD~1 HEAD --name-only` lists ONLY `documentation/qwen35-install.md`.
  </verify>
  <done>
`documentation/qwen35-install.md` exists with ≥9 numbered sections covering: prerequisites + mlx_lm version check (§1), directory layout (§2), model download (§3), launchd plist for both services with `--chat-template-kwargs` flag (§4), service load + Instruct + thinking-mode dual verification protocol (§5), Path B F# fallback procedure (§6), unified-memory budget table 32B+72B vs 35B+122B (§7), 180s cold-start timeout mitigation (§8), swap + rollback procedure for 17-02 (§9). The doc is structurally a sibling of `local-llm-services.md` and shares the §1.x verification-table style of `qwen32b-base-to-instruct.md`. No service or code touched. Atomic commit lands on master.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

1. Single new file: `documentation/qwen35-install.md` (no other tracked file changes).
2. Doc passes the 15 mechanical verify checks (line count, section count, key-term presence, sibling reference).
3. `bash scripts/check-no-async.sh` exits 0 (no Core change so trivially passes — sanity check).
4. `dotnet build BlueCode.sln 2>&1 | tail -3` exits 0 (no source change touched, build remains green).
5. Bench gate is NOT run (operations-only docs phase has nothing to gate).
6. Test suite is NOT run (no source change).
7. Single atomic commit: `docs(17-01): qwen35-install runbook (35B/122B MoE + thinking-mode mitigation)`.

Out-of-scope guardrails (these MUST NOT change in 17-01):
- `src/BlueCode.Core/**` — UNTOUCHED. Operations + docs phase. Path B coding (if needed) happens in 17-02 ONLY if empirically required.
- `src/BlueCode.Cli/Adapters/QwenHttpClient.fs` — UNTOUCHED. The patch is documented in §6 as a procedure but not applied.
- `tests/**` — UNTOUCHED. No tests added/modified (no code change to test).
- `bench/**` — UNTOUCHED. 17-03 territory.
- `documentation/local-llm-services.md` — UNTOUCHED. The 32B/72B services are still operational; that doc stays valid.
- `documentation/qwen32b-base-to-instruct.md` — UNTOUCHED. Sibling reference only.
- `~/Library/LaunchAgents/*` — UNTOUCHED. Phase 17-02 territory (with checkpoint).
- `~/llm-system/models/*` — UNTOUCHED. Nothing downloaded in 17-01.
- `CLAUDE.md` — UNTOUCHED. Updated only in 17-03 IF SWITCH decision.
- `bench/baseline.json` — UNTOUCHED. Updated only in 17-03 IF SWITCH decision.
- `.planning/phases/16-*/` — UNTOUCHED (Phase 16 plans on disk are preserved regardless of Phase 17 outcome).
- No new external dependencies (`pip install` commands appear in the doc as INSTRUCTIONS for the user but are NOT run by the executor).
</verification>

<success_criteria>
This plan satisfies Phase 17 Success Criteria #1 in full:

> SC1: `documentation/qwen35-install.md` exists with: model download instructions (HF ids + local path conventions), `mlx_lm.server` launchd plist templates for both 35B and 122B (mirrors `documentation/local-llm-services.md` shape), Base-vs-Instruct verification protocol (mirrors `documentation/qwen32b-base-to-instruct.md`), unified-memory budget calculation (35B + 122B vs 32B + 72B on 128GB Mac).

Mapping:
- "model download instructions (HF ids + local path conventions)" → §2 (paths) + §3 (download script with `mlx-community/Qwen3.5-35B-A3B-4bit` + `mlx-community/Qwen3.5-122B-A10B-4bit`).
- "`mlx_lm.server` launchd plist templates for both 35B and 122B" → §4.1 (full 35B plist XML) + §4.2 (122B copy-and-modify procedure).
- "mirrors `documentation/local-llm-services.md` shape" → header style, section numbering, language, table conventions all match.
- "Base-vs-Instruct verification protocol (mirrors `documentation/qwen32b-base-to-instruct.md`)" → §5.2 (file presence checks) + §5.3 (response classification table) + §5.4 (JSON schema smoke test) all use the symptom→diagnosis→action shape.
- "unified-memory budget calculation (35B + 122B vs 32B + 72B on 128GB Mac)" → §7 (full comparison table with running totals + headroom + OOM mitigation options).
- BONUS (not in SC1 but load-bearing): §4.4 (Path A availability check) + §6 (Path B fallback procedure) + §8 (180s timeout mitigation) + §9 (swap procedure for 17-02).

Phase 17 SC2-5 are NOT touched by this plan (they belong to 17-02 and 17-03). 17-01 is purely the doc-prerequisite that 17-02's user-facing checkpoints rely on.
</success_criteria>

<output>
After completion, create `.planning/phases/17-qwen-3-5-evaluation/17-01-SUMMARY.md` documenting:
- File created: `documentation/qwen35-install.md` with line count and section list.
- Key decisions baked into the doc:
  - Path A vs Path B mitigation choice (which the doc enumerates without picking; 17-02 picks empirically).
  - Co-existence policy: old + new models share disk during evaluation; 32B/72B not deleted.
  - Korean+English bilingual convention preserved (matches local-llm-services.md).
- Cross-references made: sibling links to `local-llm-services.md` and `qwen32b-base-to-instruct.md`; transitive reference to research doc.
- Anything that surprised you (e.g., research's plist XML diverged from local-llm-services.md plist shape — note any normalization choices).
- Confirmation that no source/test/bench files were touched (operations-pure phase).
</output>
</content>
