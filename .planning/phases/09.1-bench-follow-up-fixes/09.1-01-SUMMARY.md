---
phase: 09.1-bench-follow-up-fixes
plan: "01"
subsystem: llm-prompting
tags: [system-prompt, read_file, edit_file, truncation, benchmark-regression]

# Dependency graph
requires:
  - phase: 09-read-file-metadata
    provides: "read_file metadata header with truncated/out-of-range keywords already in system prompt"
provides:
  - "Fix 2: 'If truncated' strategy hint inside read_file block of defaultSystemPrompt"
  - "Fix 3: 'edit_file persists' terminal hint inside edit_file block of defaultSystemPrompt"
affects:
  - "09.1-02 (dispatcher fix) — independent but part of same audit remediation"
  - "09.1-03 (production-trace test) — may reference system prompt wording"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Continuation-line style: 15-space indentation inside triple-quoted defaultSystemPrompt for multi-line schema annotations"

key-files:
  created: []
  modified:
    - src/BlueCode.Cli/CompositionRoot.fs

key-decisions:
  - "Two verbatim hint lines added; no code logic, no test changes — lowest-risk wave-1 work per bench-followup §5"
  - "Indentation locked to 15 spaces matching surrounding Tool output begins:/If out-of-range: style"

patterns-established:
  - "Schema annotation continuation lines: 15-space indent, same column as value field of sibling rows"

# Metrics
duration: 5min
completed: 2026-04-25
---

# Phase 9.1 Plan 01: System Prompt Clarifications Summary

**Added 'If truncated' window-narrowing hint to read_file block and 'edit_file persists' terminal hint to edit_file block in defaultSystemPrompt — zero code changes, zero test changes.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-25T15:58:00Z
- **Completed:** 2026-04-25T16:02:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Inserted verbatim Fix 2 hint: `If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.` — positioned between `Tool output begins:` and `If out-of-range:` lines at line 60.
- Inserted verbatim Fix 3 hint: `edit_file persists the change. After a successful edit_file, do NOT call write_file with the same path.` — continuation line after `edit_file` schema row at line 66.
- Build green (0 errors), test count unchanged at 240 passed, 1 ignored, 0 failed.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Fix 2 — truncated strategy hint to read_file block** - `6eb0892` (feat)
2. **Task 2: Add Fix 3 — edit_file terminal-action hint** - `d251489` (feat)

**Plan metadata:** (docs: complete system prompt clarifications plan) — staged below

## Files Created/Modified

- `src/BlueCode.Cli/CompositionRoot.fs` — +2 lines inside `defaultSystemPrompt` triple-quoted literal; no other changes. No Core files touched.

## Exact Diff Applied

```diff
@@ -57,6 +57,7 @@ Action input schemas:
 - read_file:   {"path": "<rel-path>", "start_line": <int?>, "end_line": <int?>}
                Tool output begins: [file: <path>, lines X-Y of Z, not-truncated|truncated|out-of-range]
+               If truncated: content was clipped to 2000 chars; pick a smaller window (e.g., end_line - start_line < 50) to fetch unclipped content.
                If out-of-range: requested start_line > total_lines (Z); choose a start_line <= Z.

@@ -63,6 +63,7 @@ Action input schemas:
 - edit_file:   {"path": "<rel-path>", "old_string": "<exact-unique-match>", "new_string": "<replacement>"}
+               edit_file persists the change. After a successful edit_file, do NOT call write_file with the same path.
 - glob_search: {"pattern": "<glob like src/**/*.fs>", "path": "<rel-path?>"}
```

**Cumulative diff:** 2 insertions, 0 deletions in `src/BlueCode.Cli/CompositionRoot.fs`.

## Verification Results

| Check | Result |
|-------|--------|
| `grep -c "If truncated: content was clipped"` | 1 |
| `grep -c "edit_file persists the change"` | 1 |
| `dotnet build BlueCode.slnx` (pre-change) | 0 errors |
| `dotnet build BlueCode.slnx` (post-change) | 0 errors |
| Test count (pre) | 240 passed, 1 ignored, 0 failed |
| Test count (post) | 240 passed, 1 ignored, 0 failed |
| `grep -rn "llm-system" src/` | 0 lines (Core purity intact) |
| `bash scripts/check-no-async.sh` | OK: no async {} in Core |
| `git diff --stat HEAD~2 HEAD -- CompositionRoot.fs` | 2 insertions, 0 deletions |
| Core files touched | None — only `src/BlueCode.Cli/CompositionRoot.fs` |

## Decisions Made

None beyond plan specification — hint text was taken verbatim from `documentation/v1.2-bench-followup.md` §2 Fix 2 and Fix 3. Indentation style (15 spaces) confirmed from surrounding `Tool output begins:` / `If out-of-range:` lines.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. Both insertions placed correctly on first attempt.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- System prompt now carries both strategy hints for 72B truncation regression and 32B edit+write redundancy.
- Ready for Wave 2: Plan 09.1-02 (dispatcher fix — `AgentLoop.dispatchTool` partial-bounds collapse + production-trace test).

---
*Phase: 09.1-bench-follow-up-fixes*
*Completed: 2026-04-25*
