---
phase: 21-empirical-qwen-3-5-122b-coding-evaluation
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - documentation/qwen35-122b-coding-eval.md
  - .planning/STATE.md
  - CLAUDE.md
autonomous: true

must_haves:
  truths:
    - "documentation/qwen35-122b-coding-eval.md exists with >= 600 lines and 10 sections (§1..§10)"
    - "Each section ends with a verdict line (e.g., '§3.1 Throughput verdict: PASS — median 42 tok/s ≥ 30 threshold')"
    - "§7 Verdict scorecard renders the 100-point rubric (40+25+25+10) with empirical numbers from 21-01..21-04 artifacts"
    - "Final line of doc matches the format: 'Total: NN/100, Recommendation: <KEEP|KEEP-WITH-CAVEATS|ESCALATE>'"
    - "§3.3 Cold-start states 'deferred to separate --coldstart invocation per scope; reproduction instructions in §10'"
    - "§6.3 Cloud comparison documents the deliberate non-goal (single paragraph)"
    - "§10 Reproduction instructions list how to re-run --full and --coldstart"
    - ".planning/STATE.md observation note added pointing to the eval doc"
    - "CLAUDE.md gets 2-line cross-reference under existing 'Bench' section pointing to the eval doc"
    - "MANDATORY final regression: bash bench/run.sh --gate exits 0 with GATE PASS (7/7) post-plan"
  artifacts:
    - path: "documentation/qwen35-122b-coding-eval.md"
      provides: "100-point empirical scorecard verdict for Qwen 3.5 122B coding evaluation"
      min_lines: 600
      contains: "§1 Methodology\\|§7 Verdict\\|Total: \\|Recommendation:"
    - path: ".planning/STATE.md"
      provides: "Observation note referencing the eval doc"
      contains: "qwen35-122b-coding-eval"
    - path: "CLAUDE.md"
      provides: "Cross-reference to eval doc under 'Bench' section"
      contains: "qwen35-122b-coding-eval"
  key_links:
    - from: "documentation/qwen35-122b-coding-eval.md §2 (Correctness)"
      to: "bench/runs/qwen35-eval-<ts>/humaneval_results.json + humaneval_*_score.txt + refactor_multifile_diff.txt + refactor_orphan_count.txt + bug_*_diagnose.log"
      via: "raw artifacts from 21-02 and 21-03 cited and summarized; §2.4 specifically reads refactor_orphan_count.txt for CORR-EVAL-02 5/0-pts scoring"
      pattern: "humaneval_results\\.json\\|refactor_multifile_diff\\.txt\\|refactor_orphan_count\\.txt\\|bug_.*_diagnose\\.log"
    - from: "documentation/qwen35-122b-coding-eval.md §3 (Performance)"
      to: "bench/runs/qwen35-eval-<ts>/throughput.json + ttft.json"
      via: "raw artifacts from 21-01 cited and summarized"
      pattern: "throughput\\.json\\|ttft\\.json"
    - from: "documentation/qwen35-122b-coding-eval.md §4 (Reliability)"
      to: "bench/runs/qwen35-eval-<ts>/multiturn_N{1,3,5,7,10}/ + schema_rate.txt + needle.json"
      via: "raw artifacts from 21-04 cited and summarized"
      pattern: "multiturn_N\\|schema_rate\\.txt\\|needle\\.json"
    - from: "documentation/qwen35-122b-coding-eval.md §6.1 (vs published)"
      to: "Qwen 3.5 published HumanEval+ numbers"
      via: "WebFetch lookup at writing time, cite verbatim"
      pattern: "Qwen 3\\.5\\|HumanEval"
    - from: "CLAUDE.md Bench section"
      to: "documentation/qwen35-122b-coding-eval.md"
      via: "2-line cross-reference"
      pattern: "qwen35-122b-coding-eval"
---

<objective>
Aggregate all measurement artifacts produced by 21-01..21-04 into a single empirical verdict document. Apply the 100-point scorecard rubric (40 Correctness + 25 Performance + 25 Reliability + 10 Coding-quality) to produce the headline recommendation: KEEP, KEEP-WITH-CAVEATS, or ESCALATE.

Purpose: Closes the v2.0 measurement gap noted in source-of-truth plan §Context. The doc is the deliverable; its existence with the verdict line is the milestone success criterion (DOC-EVAL-01).

Output:
- `documentation/qwen35-122b-coding-eval.md` (~600 lines, 10 sections, ends with verdict line)
- `.planning/STATE.md` updated with observation note
- `CLAUDE.md` updated with 2-line cross-reference under "Bench"
- MANDATORY: `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)` post-plan (regression authority preserved)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/21-empirical-qwen-3-5-122b-coding-evaluation/21-01-SUMMARY.md
@.planning/phases/21-empirical-qwen-3-5-122b-coding-evaluation/21-02-SUMMARY.md
@.planning/phases/21-empirical-qwen-3-5-122b-coding-evaluation/21-03-SUMMARY.md
@.planning/phases/21-empirical-qwen-3-5-122b-coding-evaluation/21-04-SUMMARY.md
@documentation/benchmark-qwen35-eval.md
</context>

<architectural_invariants>
1. **mlx-runner constraint:** Not directly relevant (this plan only writes documentation); but the deviation note in §1 must mention that this eval used HTTP-only adaptation (no in-process mlx_lm.load).
2. **Bench gate stability (load-bearing for THIS plan — final regression authority):** `bash bench/run.sh --gate` MUST exit 0 with `GATE PASS (7/7)` after this plan completes. This is the milestone-level guarantee. Run it as the LAST step of the LAST task in this plan. If it fails, the milestone is NOT complete; diagnose before declaring victory.
3. **Role=User invariant:** Not exercised by this plan (no LLM calls); document in §1 that the eval honored Phase 20-03's invariant.
4. **No src/ changes:** `git diff src/` empty post-plan.
5. **No bench/baseline.json changes:** preserved byte-for-byte. The eval did NOT modify the gate baseline; only added observational artifacts.
6. **No new tests in tests/BlueCode.Tests/:** test count stays 282/1/0.
7. **Atomic commits per CLAUDE.md:** Three task commits (`docs(21-05): write coding eval verdict doc` + `docs(21-05): update STATE.md observation note` + `docs(21-05): cross-reference eval doc in CLAUDE.md`) plus plan-meta. Stage files individually (per CLAUDE.md commit protocol — `git add` specific files, NEVER `-A` or `.`).
8. **Cold-start gated and explicitly addressed in doc §3.3 + §10.**
9. **Cloud comparison: explicit non-goal in §6.3.**
10. **Single 122B service contention:** not relevant (no live runs in this plan).
</architectural_invariants>

<out_of_scope>
- DO NOT modify `src/`, `bench/baseline.json`, or `bench/run.sh`
- DO NOT add new tests in `tests/BlueCode.Tests/`
- DO NOT re-run any 21-01..21-04 measurements (this plan is aggregation only; if artifacts are missing, that is a 21-04 gap to address before starting 21-05)
- DO NOT load second 122B instance via mlx_lm.load()
- DO NOT add cloud comparison numbers (§6.3 documents the non-goal; do not weaken the boundary)
- DO NOT exercise --coldstart (deferred per scope; document in §3.3 + §10 only)
- DO NOT modify the verdict scorecard thresholds — they are fixed in source-of-truth plan §Verdict criteria
- DO NOT touch Phase 20 deferred items
</out_of_scope>

<tasks>

<task type="auto">
  <name>Task 1: Write documentation/qwen35-122b-coding-eval.md (10 sections, >=600 lines, ends with verdict line)</name>
  <files>documentation/qwen35-122b-coding-eval.md</files>
  <action>
**Step 1 — Locate all artifacts.** Find the canonical LOG_DIRs from 21-01..21-04:
```bash
# If --full was run by 21-04 Option A: ONE LOG_DIR has everything
# If sub-modes ran separately: multiple LOG_DIRs, walk all
ls -dt bench/runs/qwen35-eval-*/
```
Identify the LOG_DIR(s) containing each artifact:
- `throughput.json`, `ttft.json` — from 21-01 or 21-04 --full
- `humaneval_results.json`, `humaneval_*_score.txt` — from 21-02
- `refactor_multifile_diff.txt`, `bug_*_diagnose.log` — from 21-03
- `multiturn_N{1,3,5,7,10}/`, `schema_rate.txt`, `needle.json` — from 21-04

**Step 2 — Extract empirical numbers.** For each artifact, extract the headline number for the verdict scorecard. Examples:
```bash
# Throughput median
jq -s 'map(.tokens_per_sec) | sort | .[length/2 | floor]' "$DIR/throughput.json"

# TTFT median (excluding -1 sentinel for failed trials)
jq -s 'map(select(.ttft_ms > 0) | .ttft_ms) | sort | .[length/2 | floor]' "$DIR/ttft.json"

# HumanEval+ pass@1 chat
grep -E "pass@1" "$DIR/humaneval_chat_score.txt"
grep -E "pass@1" "$DIR/humaneval_completion_score.txt"

# Schema rate
cat "$DIR/schema_rate.txt"

# Needle: per-size retrieved
jq '.results' "$DIR/needle.json"

# Multi-turn: highest N where >=2 of 3 trials passed (qualitative; review transcripts)
for n in 1 3 5 7 10; do
  ls "$DIR/multiturn_N${n}"/trial*/transcript.log 2>/dev/null | wc -l
done
```

**Step 3 — Look up published Qwen 3.5 numbers** (for §6.1 comparison). Use WebFetch if blueCode has internet access:
```
WebFetch https://huggingface.co/Qwen/Qwen3.5-122B-A10B-4bit-MoE  → "What are the published HumanEval pass@1 numbers for this model?"
```
If model card not accessible, fall back to Qwen 3.5 release blog or model report. Cite the source verbatim. If no published number found, state "Published HumanEval+ for Qwen 3.5 122B-A10B-4bit not located at writing time; mlx-community quantization may not have reported numbers — comparison §6.1 limited to qualitative discussion."

**Step 4 — Write the doc.** Write `documentation/qwen35-122b-coding-eval.md` with all 10 sections. Mirror structure of `documentation/benchmark-qwen35-eval.md`. Each section ends with a verdict line.

**Required structure (target ~600 lines total; do not pad — depth comes from concrete numbers + transcripts cited inline):**

```markdown
# Qwen 3.5 122B-A10B-4bit MoE — Empirical Coding Evaluation

**Date:** 2026-04-NN
**Phase:** v2.1 Phase 21
**Model:** Qwen 3.5 122B-A10B-4bit MoE (mlx-community quantization)
**Service:** mlx_lm.server on localhost:8001 (launchd: com.ohama.qwen122b)
**blueCode commit:** <git rev-parse HEAD at time of writing>
**Eval run dir(s):** bench/runs/qwen35-eval-<ts>/ (cite specific path[s])

## §1 Methodology

- Sampling: temperature=0.2, top_p=0.8, top_k=20 (eval-standard per mlx-runner/mlx_llm_eval_guide.md §8)
- DEVIATION from blueCode runtime defaults: blueCode at runtime uses temperature=0.7 (Phase 20-01, Router.modelToSamplingParams). Rationale: eval-standard 0.2 is for stable measurement; runtime 0.7 is for creative coding latitude. Both are correct for their context.
- Environment: macOS Darwin 25.3.0, M-series, mlx_lm.server with --chat-template-args '{"enable_thinking": false}' (Phase 19 thinking-mode mitigation)
- Adaptation: HTTP-only against localhost:8001. mlx-runner's in-process mlx_lm.load() would OOM; all measurements use requests.post / curl.
- Role=User invariant honored (Phase 20-03): all multi-turn injections via dotnet run --resume; no Role=System mid-conversation.
- Resolved Python: <3.14 or 3.12 from 21-01 SUMMARY>
- evalplus version: <from 21-01 SUMMARY>
- Code extraction (HumanEval+ chat mode): first ```python ... ``` block; fallback to def-signature substring match

§1 verdict: documented; methodology reproducible.

## §2 Correctness (40 pts)

### §2.1 HumanEval+ pass@1
- Mode A (chat): X/164 = X.X% pass@1, Y/164 = Y.Y% pass@1+
- Mode B (completion): X/164 = X.X% pass@1, Y/164 = Y.Y% pass@1+
- Cite raw: bench/runs/qwen35-eval-<ts>/humaneval_chat_score.txt, humaneval_completion_score.txt

§2.1 verdict: PASS/MARGINAL/FAIL — chat-mode pass@1 of X% lands in <threshold band>. Score: NN/15.

### §2.2 F# bug-fix on 4 fixtures (B1, B2, bug_binsearch, refactor)
- B1 (bug_lastchar): from bench/baseline.json gate ⇒ PASS
- B2 (bug_divide_zero): from bench/baseline.json gate ⇒ PASS (diagnose-only)
- bug_binsearch: <PASS/FAIL based on transcript review>; cite bench/runs/qwen35-eval-<ts>/bug_binsearch_diagnose.log
- refactor_multifile: read `bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt` (single integer; written by 21-03 run_refactor BEFORE bench gate's EXIT trap restores fixtures, so it preserves the post-refactor state). PASS iff the integer is 0; otherwise FAIL. Also cite `CORR-EVAL-02 PASS:` or `CORR-EVAL-02 FAIL:` line in `refactor_multifile_diff.txt` for human-readable confirmation.

§2.2 verdict: 3.75 × <count_correct> = NN/15 pts.

### §2.3 Language coverage (Python typeerror, TypeScript async)
- Python parse_age: <PASS/FAIL>; cite bug_python_typeerror_diagnose.log
- TS fetchAllUsers: <PASS/FAIL>; cite bug_typescript_async_diagnose.log

§2.3 verdict: 2.5 × <count_correct> = NN/5 pts.

### §2.4 Multi-file refactor preserves test behavior (all-or-nothing)
- Read `bench/runs/qwen35-eval-<ts>/refactor_orphan_count.txt` (written by 21-03 run_refactor BEFORE the bench gate's EXIT trap restores fixtures). The file contains a single integer N: count of orphan `add` references remaining post-refactor across Calculator.fs/Main.fs/Tests.fs.
- Inspect the POST-REFACTOR FILE STATES block in `refactor_multifile_diff.txt` for qualitative verification (did Tests.fs use sum / sum3 with same expected values?).
- Apply CORR-EVAL-02 scoring rule: **N=0 → 5 pts; N>0 → 0 pts** (all-or-nothing).
- Cite both the count and the `CORR-EVAL-02 PASS|FAIL:` verdict line from `refactor_multifile_diff.txt`.

§2.4 verdict: NN/5 pts.

§2 total: NN/40

## §3 Performance (25 pts)

### §3.1 Throughput
- Trials: 5 prompts × 3 trials = 15 measurements
- Median: X tok/s; min: Y tok/s; max: Z tok/s
- Cite raw: throughput.json

§3.1 verdict: <PASS/MARGINAL/FAIL> per threshold (≥30/20-30/15-20/<15). Score: NN/10.

### §3.2 TTFT
- Trials: 10 (~200-token prompt)
- Median: X ms; (note any -1 sentinel trials filtered)
- Cite raw: ttft.json

§3.2 verdict: <PASS/MARGINAL/FAIL> per threshold (≤500/500-1500/>1500). Score: NN/5.

### §3.3 Cold-start (REFERENCE; deferred per scope)
This dimension is GATED behind --coldstart and was DEFERRED from this evaluation per user scope decision (disruptive: kills 122B for ~3min). The implementation is present in bench/eval-qwen35-122b.sh:run_coldstart() for explicit invocation. Reproduction instructions in §10.

§3.3 verdict: N/A (deferred). Score: 0/5 (or treat as N/A in aggregate; we apply 0 to keep 100-point total honest).

### §3.4 End-to-end task time within ±20% of baseline
- Compare bench/baseline.json gate entries (T6/W1/W2/T1/T5/B2 + MT_122b) to step counts/elapsed times observed during 21-03 langcoverage and 21-04 multi-turn runs
- Cite specific entries

§3.4 verdict: <PASS/FAIL>. Score: NN/5.

§3 total: NN/25

## §4 Reliability (25 pts)

### §4.1 JSON schema compliance rate
- Result: X/50 InvalidJsonOutput
- Cite raw: schema_rate.txt

§4.1 verdict: <PASS/MARGINAL/FAIL> per threshold (≥49/47-49/45-47/<45). Score: NN/10.

### §4.2 Multi-turn stability
- N=1: 3/3 trials succeeded with valid JSON; mean step count X
- N=3: 3/3; mean step count X
- N=5: 3/3; mean step count X
- N=7: 1/1; step count X
- N=10: 1/1; step count X
- Qualitative: cite specific transcript moments where coherence held / broke
- Cite raw: multiturn_N{1,3,5,7,10}/

§4.2 verdict: <stable through 7+/5+/<5 turns>. Score: NN/10.

### §4.3 Long-context needle accuracy at max tested size
- Server max_model_len: <from needle.json>
- 8k: retrieved=true/false, elapsed=Xs
- 16k: retrieved=true/false, elapsed=Xs
- 32k: retrieved=true/false, elapsed=Xs
- Cite raw: needle.json

§4.3 verdict: <correct/degraded/failed> at 32k. Score: NN/5.

§4 total: NN/25

## §5 Coding quality (10 pts; qualitative transcript review)

### §5.1 Idiomatic F# in 3 transcripts
Review 3 transcripts (refactor_multifile_diff.txt + 2 multiturn N=5 trials). Look for: pipelines (|>), pattern matching (match ... with), discriminated unions, `let` bindings over `var`. Score:
- All 3 idiomatic: 5
- 2 of 3: 3
- 1 of 3: 1
- 0 of 3: 0

§5.1 verdict: NN/5.

### §5.2 Generated tests compile and pass
From multi-turn turn 3 (Tests for parse_csv): copy the generated code into a temp .py file and run pytest. Or from refactor_multifile/Tests.fs (post-refactor): does it match the rename intent and would compile if added to a project?

§5.2 verdict: <PASS/FAIL>. Score: NN/3.

### §5.3 Code review identifies ≥80% of known issues in buggy file
For bug_binsearch.fs (only known issue: hi=mid off-by-one), bug_python_typeerror.py (2 issues: silent None on TypeError, silent None on negative), bug_typescript_async.ts (1 issue: missing await). Total: 4 known issues.
- Did the agent identify ≥80% (3.2 → ≥4) of these? Cite each diagnose log.

§5.3 verdict: NN/2.

§5 total: NN/10

## §6 Comparison anchors

### §6.1 vs published Qwen 3.5 122B
<cite WebFetch result or "not located"; report Mode B (completion) pass@1 alongside published number; quantify delta>

§6.1 verdict: within X% of published / no public number / outperforms / underperforms.

### §6.2 vs Qwen 2.5 archive
Qwen 2.5 32B (retired Phase 19) gate baseline pre-retirement: cite step counts / elapsed from .planning/milestones/v1.x-phases/<phase>/SUMMARY.md if available.

§6.2 verdict: 122B is faster/slower/equivalent on canonical fixtures.

### §6.3 Cloud comparison (DELIBERATE NON-GOAL)
A direct comparison against Claude Opus / GPT-4 / Gemini was considered and explicitly rejected for this evaluation, for the following reasons:
1. Requires API keys with ongoing config / billing dependency.
2. Introduces network variance into measurement, polluting tok/s and TTFT numbers.
3. Scope drift: the question "is Qwen 3.5 122B useful for daily coding?" is independent of "is it the best available cloud model?"
4. The user has muscle memory from daily Claude Opus 4.7 use; cloud is the implicit baseline of "good enough" without needing a benchmark.
This boundary is preserved deliberately. If a future re-evaluation needs cloud anchors, that is a separate eval, not this one.

§6.3 verdict: documented as deliberate boundary.

## §7 Verdict scorecard

| Dimension | Sub-criterion | Score | Max |
|-----------|---------------|-------|-----|
| Correctness | HumanEval+ pass@1 (chat) | NN | 15 |
| Correctness | F# bug-fix (4 fixtures) | NN | 15 |
| Correctness | Language coverage (Py + TS) | NN | 5 |
| Correctness | Multi-file refactor | NN | 5 |
| **Correctness subtotal** |  | **NN** | **40** |
| Performance | Throughput tok/s | NN | 10 |
| Performance | TTFT ms | NN | 5 |
| Performance | Cold-start (deferred) | 0 | 5 |
| Performance | End-to-end ±20% baseline | NN | 5 |
| **Performance subtotal** |  | **NN** | **25** |
| Reliability | JSON schema rate | NN | 10 |
| Reliability | Multi-turn stable | NN | 10 |
| Reliability | Needle 32k | NN | 5 |
| **Reliability subtotal** |  | **NN** | **25** |
| Coding quality | Idiomatic F# | NN | 5 |
| Coding quality | Tests compile + pass | NN | 3 |
| Coding quality | Bug identification | NN | 2 |
| **Coding quality subtotal** |  | **NN** | **10** |

**Aggregate rule (per source-of-truth plan):**
- ≥80/100: KEEP — empirically useful for daily F# coding via blueCode
- 60-79 OR any single dimension <60% of its max: KEEP-WITH-CAVEATS — document specific weaknesses
- <60 OR multi-turn degrades before turn 5 OR HumanEval+ <30%: ESCALATE — recommend cloud for non-trivial work

## §8 Caveats and known limitations

- Cold-start NOT measured (gated; reproduce via §10)
- Cloud comparison NOT measured (deliberate non-goal §6.3)
- Sampling temp=0.2 in eval ≠ blueCode runtime 0.7 (§1)
- Single quantization (mlx-community 4-bit MoE) — other quant levels not evaluated
- Mac-only (M-series); CUDA / other platforms outside scope
- 4-bit weights may quietly underperform on edge cases not covered by 164-problem HumanEval+ — list any specific failures observed

## §9 Recommended thresholds for re-evaluation

Re-run this eval if any of:
- mlx_lm.server major version change
- Qwen 3.5 model card update or YaRN config change
- blueCode runtime sampling change
- macOS major version upgrade with metal/ANE driver delta
- Memory profile drift (RSS exceeds 50 GB sustained)

## §10 Reproduction instructions

```
# One-time setup (5 min)
bash bench/eval-qwen35-122b.sh --setup

# Full eval (~2 hr; excludes cold-start)
bash bench/eval-qwen35-122b.sh --full

# Cold-start measurement (DISRUPTIVE; kills 122B for ~3 min; run separately)
bash bench/eval-qwen35-122b.sh --coldstart

# Individual modes
bash bench/eval-qwen35-122b.sh --throughput
bash bench/eval-qwen35-122b.sh --ttft
bash bench/eval-qwen35-122b.sh --humaneval
bash bench/eval-qwen35-122b.sh --refactor
bash bench/eval-qwen35-122b.sh --langcoverage
bash bench/eval-qwen35-122b.sh --multiturn
bash bench/eval-qwen35-122b.sh --schema-rate
bash bench/eval-qwen35-122b.sh --needle

# Mandatory regression after any eval run
bash bench/run.sh --gate   # must exit 0 with GATE PASS (7/7)
```

§10 verdict: reproducible; commands self-contained.

---

**Total: NN/100, Recommendation: <KEEP|KEEP-WITH-CAVEATS|ESCALATE>**
```

Replace all `NN`, `X`, `Y`, `Z`, `<...>` placeholders with actual values from the artifacts. Each section's verdict line MUST be present (the checker / verifier looks for them).

**Line count target: >= 600 lines.** This is enforced by the milestone success criterion. If your draft is shorter, expand by:
- Including raw artifact excerpts inline (e.g., paste 5-10 lines of `humaneval_chat_score.txt`)
- Including transcript excerpts in §5 (paste 10-20 lines of multi-turn transcript showing idiomatic F# or its absence)
- Including the full needle.json in §4.3
Do NOT pad with corporate filler ("comprehensive analysis indicates..."); inline the data.

Atomic commit at end of task: `docs(21-05): write coding eval verdict doc` staging exactly `documentation/qwen35-122b-coding-eval.md`.
  </action>
  <verify>
- `test -f documentation/qwen35-122b-coding-eval.md`
- `wc -l documentation/qwen35-122b-coding-eval.md` shows >= 600
- `grep -c "^## §" documentation/qwen35-122b-coding-eval.md` shows >= 10 (one per section header — adjust pattern if you used different markdown style; verify all 10 sections present)
- `grep -c "verdict:" documentation/qwen35-122b-coding-eval.md` shows >= 10 (one per major section, plus sub-sections)
- Final-line scorecard format check (strict + loose-fallback for diagnostic clarity):
  ```bash
  strict=$(grep -cE '^\*\*Total: [0-9]+/100, Recommendation: (KEEP|KEEP-WITH-CAVEATS|ESCALATE)\*\*$' documentation/qwen35-122b-coding-eval.md)
  if [ "$strict" -ne 1 ]; then
      # Loose fallback to surface near-misses (trailing whitespace, missing comma, missing bold, etc.):
      loose=$(grep -F "Total:" documentation/qwen35-122b-coding-eval.md | grep -E "[0-9]+/100" | grep -cE "(KEEP|KEEP-WITH-CAVEATS|ESCALATE)")
      if [ "$loose" -ge 1 ]; then
          echo "WARNING: Total line found in loose match but NOT strict format. Verify markdown bold + comma-separated values + no trailing whitespace." >&2
      fi
      echo "FAIL: scorecard final line does not match required strict format" >&2
      exit 1
  fi
  ```
  Strict pattern (preferred): `^\*\*Total: NN/100, Recommendation: (KEEP|KEEP-WITH-CAVEATS|ESCALATE)\*\*$`. Loose fallback runs only if strict fails — its purpose is diagnostic (tells the executor "you have the data, just format is wrong"), NOT to weaken the gate. Strict must pass for verify to succeed.
- `grep -F "deferred to separate" documentation/qwen35-122b-coding-eval.md` matches §3.3 cold-start deferral
- `grep -F "DELIBERATE NON-GOAL" documentation/qwen35-122b-coding-eval.md` matches §6.3 cloud non-goal
- No `NN` or unfilled placeholders remain: `grep -E "\\bNN\\b" documentation/qwen35-122b-coding-eval.md | head` should NOT show any (placeholders should all be replaced with real numbers)
- `git status` shows only the eval doc staged
  </verify>
  <done>
Eval doc exists, ≥600 lines, all 10 sections, all verdict lines present, final line matches required format, all placeholders replaced with empirical numbers, single atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/STATE.md and CLAUDE.md with cross-references</name>
  <files>.planning/STATE.md, CLAUDE.md</files>
  <action>
**.planning/STATE.md update:**

Read `.planning/STATE.md` to find the "Accumulated Decisions" or "Current Position" section. Append a new observation entry (do NOT remove existing entries) under the most recent date heading:

```markdown
- 2026-04-NN — v2.1 Phase 21 complete; empirical Qwen 3.5 122B coding evaluation produced `documentation/qwen35-122b-coding-eval.md` with 100-point scorecard verdict (Total: NN/100, Recommendation: <verdict>). Bench gate 7/7 PASS post-eval. No `src/` or `bench/baseline.json` modifications.
```

Replace `NN` and `<verdict>` with the actual numbers from Task 1.

Atomic commit: `docs(21-05): update STATE.md observation note` staging exactly `.planning/STATE.md`.

**CLAUDE.md update:**

Read `CLAUDE.md` to find the existing `## Bench` section. Append exactly 2 lines under that section (do NOT modify existing content; append at the end of the section, before the next `##` heading):

```markdown
- `documentation/qwen35-122b-coding-eval.md` — empirical 100-point scorecard verdict for Qwen 3.5 122B coding capability (v2.1 milestone). Reproduce via `bash bench/eval-qwen35-122b.sh --full`.
- Bench gate (`bench/run.sh --gate`) is the regression authority; eval doc is observational. Run gate after any eval invocation to confirm fixtures restored cleanly by EXIT trap.
```

Atomic commit: `docs(21-05): cross-reference eval doc in CLAUDE.md` staging exactly `CLAUDE.md`.
  </action>
  <verify>
- `grep -F "qwen35-122b-coding-eval" .planning/STATE.md` matches (observation note added)
- `grep -F "Total:" .planning/STATE.md | grep -F "Recommendation:"` matches the verdict line in STATE
- `grep -F "qwen35-122b-coding-eval.md" CLAUDE.md` matches (cross-reference added)
- `grep -A 2 "^## Bench" CLAUDE.md | tail -5` shows the new lines under the Bench section
- `git diff .planning/STATE.md | head -20` shows ONLY the appended observation (no other lines modified)
- `git diff CLAUDE.md | head -20` shows ONLY the 2 new lines under Bench (no other lines modified)
- Two atomic commits landed: `docs(21-05): update STATE.md observation note`, `docs(21-05): cross-reference eval doc in CLAUDE.md`
  </verify>
  <done>
STATE.md observation note added with verdict number; CLAUDE.md cross-reference added under Bench section; minimal diffs (only appends, no modifications to existing content); two atomic commits landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: MANDATORY final regression — bench/run.sh --gate exit 0 with 7/7 PASS</name>
  <files>(no file modifications; final regression check)</files>
  <action>
This is the milestone-level guarantee. After all documentation is in place and committed, run the bench gate. If it fails, the milestone is NOT complete; diagnose before declaring victory.

```bash
# Pre-flight: 122B service must be running
curl -fsS http://127.0.0.1:8001/v1/models > /dev/null || \
  launchctl kickstart -k gui/$(id -u)/com.ohama.qwen122b
until curl -fsS http://127.0.0.1:8001/v1/models > /dev/null 2>&1; do sleep 5; done

# Run gate
bash bench/run.sh --gate
GATE_EXIT=$?
echo "GATE EXIT: $GATE_EXIT"
```

Expected output:
- Gate runs 7 invocations (T6_122b, W1_122b, W2_122b, T1_122b, T5_122b, B2_122b, MT_122b) sequentially
- Final line: `GATE PASS (7/7)` on stdout
- `$GATE_EXIT` = 0

If gate FAILs:
1. Inspect `bench/runs/<timestamp>/timeline.txt` to see which entry failed
2. Run that specific fixture invocation directly to reproduce
3. Most likely cause: a fixture file was modified (refactor or write task) and EXIT trap did not restore. Check `git diff bench/fixtures/`. If non-empty, the trap edit in 21-03 has a bug.
4. Second most likely: 122B service crashed (check `~/llm-system/services/logs/122b.err` for `[METAL] Insufficient Memory` or similar)
5. Do NOT modify `bench/baseline.json` to "fix" a failure — that is the gate authority

**Source diff sanity (final mandatory check):**
```bash
git diff src/
git diff bench/baseline.json
git diff bench/run.sh   # only line-18 EXIT trap edit from 21-03 should appear here, AND it must already be committed
```
- `src/` diff empty
- `bench/baseline.json` diff empty
- `bench/run.sh` diff empty (the line-18 edit is committed; no uncommitted modifications)

**Final state confirmation:**
```bash
# All eval-only artifacts present
ls bench/eval-qwen35-122b.sh bench/eval-humaneval-http.py bench/eval-needle.py bench/requirements-eval.txt
ls bench/fixtures/refactor_multifile/Calculator.fs bench/fixtures/refactor_multifile/Main.fs bench/fixtures/refactor_multifile/Tests.fs bench/fixtures/refactor_multifile/README.md
ls bench/fixtures/bug_binsearch.fs bench/fixtures/bug_python_typeerror.py bench/fixtures/bug_typescript_async.ts
ls bench/fixtures/multiturn_prompts.txt
ls documentation/qwen35-122b-coding-eval.md

# Test count unchanged
dotnet test tests/BlueCode.Tests/BlueCode.Tests.fsproj 2>&1 | tail -3
# Expect: "Failed: 0", "Passed: 282", "Skipped: 1" (or whatever is current head's count)
```

No commit needed for this task; it is the final regression check. The plan-meta commit (`docs(21-05): complete eval verdict plan`) is created in the SUMMARY step.
  </action>
  <verify>
- `bash bench/run.sh --gate` exits 0
- stdout contains `GATE PASS (7/7)`
- `git diff src/` empty
- `git diff bench/baseline.json` empty
- `git diff bench/run.sh` empty (line-18 trap edit already committed by 21-03)
- All 12 new files exist (bench/eval-qwen35-122b.sh, bench/eval-humaneval-http.py, bench/eval-needle.py, bench/requirements-eval.txt, 4 refactor_multifile files, 3 bug fixtures, multiturn_prompts.txt, eval doc)
- 3 modified files have diffs only on cross-reference lines (bench/run.sh trap, .planning/STATE.md note, CLAUDE.md cross-ref) and all are committed
- Test count: 282/1/0 (Passed/Skipped/Failed)
- Working tree clean: `git status` shows nothing pending or only gitignored eval-run artifacts
  </verify>
  <done>
`bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)`. No source/baseline/run.sh diffs uncommitted. Test count unchanged. All 12 new files + 3 modified files on disk per file map. v2.1 milestone success criteria 1-5 all satisfied.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (also serves as milestone-level verification):**

1. `documentation/qwen35-122b-coding-eval.md` exists, >= 600 lines, all 10 sections, ends with `**Total: NN/100, Recommendation: <verdict>**` line.
2. `.planning/STATE.md` contains observation note referencing the eval doc.
3. `CLAUDE.md` Bench section contains 2-line cross-reference to eval doc.
4. `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)` (MANDATORY final regression).
5. `git diff src/` empty.
6. `git diff bench/baseline.json` empty.
7. `git diff bench/run.sh` empty (line-18 trap edit was committed by 21-03).
8. All 12 new files + 3 modified files match source-of-truth plan §File map.
9. Test count unchanged: 282/1/0.
10. Three atomic commits: `docs(21-05): write coding eval verdict doc`, `docs(21-05): update STATE.md observation note`, `docs(21-05): cross-reference eval doc in CLAUDE.md`.

Maps to phase Success Criteria 3 (eval doc deliverable, all 10 sections, verdict line), SC4 (bench gate post-eval, MANDATORY), SC5 (file map: documentation/qwen35-122b-coding-eval.md + STATE.md observation + CLAUDE.md cross-reference).
</verification>

<success_criteria>
- [ ] `documentation/qwen35-122b-coding-eval.md` exists with >= 600 lines, 10 sections, every section has verdict line
- [ ] Final line of doc matches `**Total: NN/100, Recommendation: <KEEP|KEEP-WITH-CAVEATS|ESCALATE>**` format
- [ ] §3.3 documents cold-start deferral; §6.3 documents cloud non-goal
- [ ] All `NN`/placeholder values replaced with actual empirical numbers from 21-01..21-04 artifacts
- [ ] `.planning/STATE.md` has observation note (only append, no modifications)
- [ ] `CLAUDE.md` Bench section has 2-line cross-reference (only append, no modifications)
- [ ] `bash bench/run.sh --gate` exits 0 with `GATE PASS (7/7)` (MANDATORY)
- [ ] `git diff src/` empty; `git diff bench/baseline.json` empty
- [ ] Three atomic commits with `docs(21-05):` prefix
- [ ] Test count unchanged (282/1/0)
- [ ] All 12 new files + 3 modified files (per source-of-truth plan §File map) exist
</success_criteria>

<output>
After completion, create `.planning/phases/21-empirical-qwen-3-5-122b-coding-evaluation/21-05-SUMMARY.md` documenting:
- Final scorecard total: NN/100, Recommendation: <verdict>
- Per-dimension subtotals (Correctness/Performance/Reliability/Coding-quality)
- Headline empirical numbers (HumanEval+ pass@1 chat, throughput median, TTFT median, schema rate, multi-turn N where stable, needle@32k retrieved)
- Confirmation that bench gate exits 0 with 7/7 PASS
- Confirmation that no `src/` or `bench/baseline.json` diffs exist
- Path to eval doc: `documentation/qwen35-122b-coding-eval.md`
- Plan-meta commit: `docs(21-05): complete eval verdict plan` staging only PLAN.md + SUMMARY.md
- Phase-meta commit (for /gsd:complete-phase or /gsd:complete-milestone): `docs(21): complete v2.1 empirical Qwen 3.5 122B coding evaluation phase` staging ROADMAP.md + STATE.md + REQUIREMENTS.md if those need final updates
</output>
