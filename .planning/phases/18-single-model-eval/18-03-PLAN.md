---
phase: 18-single-model-eval
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - documentation/single-model-eval.md
  - .planning/STATE.md
  # NOTE: This plan does NOT modify CLAUDE.md, bench/baseline.json, or any src/** file.
  # Per ROADMAP §SC5, architectural changes (Router collapse, baseline halve, CLAUDE.md
  # Runtime Environment update) are DEFERRED to a follow-up plan regardless of verdict.
  # 18-03 writes the eval doc, decides the verdict, optionally reloads 35B, and updates STATE.
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "`documentation/single-model-eval.md` exists, ≥ 150 lines, contains the literal string 'Decision'."
    - "The doc has structured sections: Overview / Methodology / 18-01 Memory profile (lifted from 18-01-MEMORY-PROFILE.md §5.1) / 18-02 Bench results (lifted from 18-02-BENCH-RESULTS.md §3) / Per-test comparison table (vs Phase 17 dual-loaded baseline) / Decision matrix / Verdict / Reversibility (35B reload) / Conditional follow-ups."
    - "The decision matrix applies all 5 ROADMAP §SC4 criteria mechanically (no judgment): T1/T2 median ≤ 6s; T6/W1/W2/B2 step counts within Phase 17 baseline_max; B2 actual_diagnosis preserved; PhysMem unused ≥ 5 GB; Compressor < 1 GB. Each criterion has explicit PASS/FAIL flag derived from 18-01 §5.1 + 18-02 §3."
    - "The verdict is named: **DROP-35B** (all 5 PASS) / **KEEP-DUAL** (≥ 2 FAIL) / **CONDITIONAL** (1 FAIL with mitigation path documented). The verdict appears verbatim in §Decision under a heading 'VERDICT: DROP-35B' or 'VERDICT: KEEP-DUAL' or 'VERDICT: CONDITIONAL'."
    - "The 35B reload step is documented in the eval doc with verbatim commands. If verdict = KEEP-DUAL: reload IS performed in this plan (mandatory reversibility). If DROP-35B / CONDITIONAL: reload is RECOMMENDED but optional; the doc records whether the user opted to reload or not. Either way, the reload command + verification (`launchctl list | grep ohama` shows both, `curl localhost:8000/v1/models` returns 200) is enumerated."
    - "Architectural follow-ups (Router collapse, bench/baseline.json halve, CLAUDE.md update, scripts/bench-122b-only.sh promotion) are ENUMERATED in the eval doc under a 'Conditional follow-ups' section, but explicitly NOT executed in this plan. ROADMAP §SC5 says they are deferred to a follow-up phase."
    - "`.planning/STATE.md` is updated: Current Position reflects Phase 18 ✓, the new disposition (DROP-35B / KEEP-DUAL / CONDITIONAL) is added to the New Decisions section, and any Phase 16 implications (re-key required if DROP-35B) are noted."
    - "Phase 18 is shippable on plan completion: SC1-SC5 from ROADMAP all satisfied (SC1 by this doc, SC2 by 18-01, SC3 by 18-02, SC4 mechanically derived in this plan, SC5 by deferral language in this plan)."
  artifacts:
    - path: "documentation/single-model-eval.md"
      provides: "Phase 18 evaluation deliverable: full per-test comparison vs Phase 17 dual-loaded numbers; decision matrix with explicit PASS/FAIL per criterion; named verdict (DROP-35B/KEEP-DUAL/CONDITIONAL); reversibility section with 35B reload commands; enumeration of conditional follow-up architectural changes (deferred, not executed)."
      min_lines: 150
      contains: "Decision"
    - path: ".planning/STATE.md"
      provides: "Updated to mark Phase 18 complete with verdict; New Decisions section captures the disposition; Phase 16 follow-up implications recorded if relevant."
      contains: "Phase 18"
  key_links:
    - from: "18-01-MEMORY-PROFILE.md §5.1 (PhysMem unused, Compressor)"
      to: "documentation/single-model-eval.md decision matrix (criteria 4 + 5)"
      via: "Lift the PASS/FAIL values directly from 18-01 §5.1 — no re-measurement"
      pattern: "PhysMem unused|Compressor"
    - from: "18-02-BENCH-RESULTS.md §3 (T1/T2 median, step counts) + §4 (B2 diagnosis)"
      to: "documentation/single-model-eval.md decision matrix (criteria 1-3)"
      via: "Lift the PASS/FAIL values directly from 18-02 §3 + §4 — no re-running of bench"
      pattern: "T1/T2 median|step counts|B2 diagnosis"
    - from: "documentation/single-model-eval.md §Verdict"
      to: "documentation/single-model-eval.md §Reversibility (35B reload command sequence)"
      via: "Verdict drives whether reload is mandatory (KEEP-DUAL) or recommended (DROP-35B/CONDITIONAL); both branches documented inline"
      pattern: "VERDICT|launchctl load -w"
    - from: "documentation/single-model-eval.md §Conditional follow-ups"
      to: "ROADMAP §SC5 deferral language ('does NOT execute them in this phase')"
      via: "Doc enumerates Router collapse / baseline halve / CLAUDE.md update as a list, marked as 'Deferred — follow-up phase required'; this plan executes ZERO of them"
      pattern: "Deferred|follow-up|Router collapse"
    - from: ".planning/STATE.md Current Position"
      to: "Phase 18 ✓ marker + New Decisions section entry"
      via: "Standard STATE.md update pattern from prior phases (17-03, 15-03 precedent)"
      pattern: "Phase 18 ✓|New Decision"
---

<objective>
Synthesize the empirical evidence from 18-01 (memory profile) and 18-02 (bench results) into a decision document. Apply ROADMAP §SC4's 5 mechanical criteria, name a verdict (DROP-35B / KEEP-DUAL / CONDITIONAL), document the 35B reload procedure (mandatory on KEEP-DUAL; recommended on DROP-35B/CONDITIONAL for reversibility), enumerate (but do NOT execute) the architectural follow-ups, and update STATE.md.

Purpose: 18-01 produced the memory data; 18-02 produced the bench data; 18-03 turns that data into a documented decision. The decision is mechanical — each criterion has an explicit threshold and the verdict is the conjunction. Phase 18 is data-gathering + decision; the architectural changes (Router collapse, baseline halve, CLAUDE.md update) are explicitly deferred per ROADMAP §SC5.

Output:
- `documentation/single-model-eval.md` (≥ 150 lines) — sibling to `documentation/benchmark-qwen35-eval.md`. Per-test comparison + decision matrix + named verdict + reversibility + deferred-follow-ups list.
- 35B service reloaded (mandatory if verdict = KEEP-DUAL; recommended if DROP-35B/CONDITIONAL — user direction may override).
- `.planning/STATE.md` updated: Phase 18 marked complete with verdict; New Decisions section adds the disposition; Phase 16 implications noted if relevant.
- Atomic commits per CLAUDE.md commit-protocol (1 doc commit, 1 STATE commit, optional 1 plan-metadata commit at end).

Operating constraint: Fully autonomous. The decision criteria are pre-specced in ROADMAP §SC4 — no judgment is invoked. The verdict falls out mechanically from the criteria-conjunction table.

Reversibility: This plan makes ZERO permanent code changes. The eval doc is additive (new file). STATE.md is updated. The 35B reload (when performed) restores the system to Phase 17 canonical state. If the user later wants to revisit the verdict, the data is on disk (18-01-MEMORY-PROFILE.md, 18-02-BENCH-RESULTS.md, single-model-eval.md) and the bench is repeatable via `scripts/bench-122b-only.sh`.

Pre-condition: 18-02 §7 disposition MUST be READY. If BLOCKED, do not start this plan — investigate the bench failures first.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-single-model-eval/18-RESEARCH.md
@.planning/phases/18-single-model-eval/18-01-MEMORY-PROFILE.md
@.planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md
@.planning/phases/17-qwen-3-5-evaluation/17-03-SUMMARY.md
@CLAUDE.md
@documentation/qwen35-install.md
@documentation/benchmark-qwen35-eval.md
@bench/baseline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 5 decision criteria + write decision matrix + name verdict</name>
  <files>documentation/single-model-eval.md</files>
  <action>
Synthesize 18-01 + 18-02 data into the decision matrix. The verdict is mechanically derivable; no judgment.

**Step 1.** Confirm 18-02 §7 disposition is READY:

```bash
grep -E "18-03 readiness.*READY" .planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md
echo "exit: $?"
```

If non-zero exit, abort: 18-02 said BLOCKED — surface the issue to user.

**Step 2.** Lift the 5 ROADMAP §SC4 criteria values from 18-01 + 18-02:

```bash
# Latency (criterion 1): T1/T2 median ≤ 6s — from 18-02 §3
grep -A 5 "ROADMAP §SC4 latency criterion\|T1/T2 median" .planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md | head -15

# Step counts (criterion 2): T6/W1/W2/B2 within baseline_max — from 18-02 §3
grep -A 8 "Step-count criteria\|Step-count" .planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md | head -15

# B2 diagnosis (criterion 3) — from 18-02 §4
grep -A 3 "Single-model 122B preserved diagnosis" .planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md

# PhysMem unused (criterion 4) — from 18-01 §5.1
grep -A 3 "PhysMem unused increase\|PhysMem unused.*GB" .planning/phases/18-single-model-eval/18-01-MEMORY-PROFILE.md

# Compressor (criterion 5) — from 18-01 §5.1
grep -A 3 "Compressor (post-unload)\|Compressor.*MB" .planning/phases/18-single-model-eval/18-01-MEMORY-PROFILE.md
```

Capture each criterion's PASS/FAIL value. Compute verdict per the rule:
- ALL 5 PASS → DROP-35B
- 2+ FAIL → KEEP-DUAL
- exactly 1 FAIL with documented mitigation path → CONDITIONAL
- exactly 1 FAIL without mitigation → KEEP-DUAL (conservative default)

**Step 3.** Capture per-test comparison data. Pull Phase 17 dual-loaded numbers from `documentation/benchmark-qwen35-eval.md` (the Phase 17 baseline doc) and `bench/baseline.json` (8 keyed entries). Pull Phase 18 single-model numbers from `bench/runs/122b-only-*/` and 18-02-BENCH-RESULTS.md §2.

```bash
LATEST_RUN=$(ls -1d bench/runs/122b-only-*/ 2>/dev/null | tail -1)
echo "$LATEST_RUN"
# 18-02 already extracted per-test data; lift it from 18-02-BENCH-RESULTS.md §2.
```

**Step 4.** Write `documentation/single-model-eval.md`. Use the structure below; populate with actual values from Steps 2-3.

```markdown
# Single-Model 122B Evaluation (Phase 18)

**Phase:** 18 (Single-Model 122B Evaluation)
**Date:** <ISO>
**Decision:** **<DROP-35B | KEEP-DUAL | CONDITIONAL>**
**Status:** Phase 18 shipped — evaluation complete; architectural follow-ups deferred per ROADMAP §SC5.

---

## Overview

Phase 17 (Qwen 3.5 Evaluation, complete 2026-04-27) made Qwen 3.5 35B/122B the canonical model
pair, replacing 32B/72B. Phase 18 asks the next question: can 35B be dropped, leaving 122B alone
to serve all blueCode invocations? If yes, the dual-model `Router` collapses to dead code,
`bench/baseline.json` halves, ~17 GB of RSS frees up, and operational surface reduces to one
launchd plist.

Phase 18 is data-gathering + decision: **NO permanent code changes are made in this phase.** The
architectural changes (Router collapse, baseline halve, CLAUDE.md update, bench script promotion)
are deferred to a follow-up phase, gated on the verdict here.

The decision is mechanical: 5 criteria from ROADMAP §SC4, each with an explicit threshold. The
verdict is the conjunction.

## Methodology

Three evaluation steps:

1. **18-01 (memory profile, checkpoint):** User unloaded 35B via `launchctl unload`. Memory snapshots
   captured before and after (PhysMem used/unused, Compressor, 35B + 122B RSS). 122B health verified
   via two smoke tests post-unload (thinking-mode disabled smoke; real blueCode `--model 72b` invocation).
   Output: `.planning/phases/18-single-model-eval/18-01-MEMORY-PROFILE.md`.

2. **18-02 (122B-only bench):** Created `scripts/bench-122b-only.sh` (mirrors `bench/run.sh` but uses
   `--model 72b` everywhere, routing to port 8001 = 122B). Ran `--all` mode (~30 invocations across
   regression/variance/diagnose/write/canary/b2). Per-test elapsed + step counts + B2 diagnosis quote
   captured. Output: `.planning/phases/18-single-model-eval/18-02-BENCH-RESULTS.md`.

3. **18-03 (this doc):** Decision matrix + verdict + reversibility procedure + deferred follow-ups.

The Phase 17 dual-loaded baseline (35B + 122B both running, ports 8000 + 8001 active) is the
comparison anchor. Phase 17 numbers are sourced from `documentation/benchmark-qwen35-eval.md`
(the Phase 17 eval doc) and `bench/baseline.json` (the regression gate's 8-entry baseline).

## 18-01 Memory profile (summary)

Lifted from `18-01-MEMORY-PROFILE.md` §5.1 + §5.2:

| ROADMAP §SC4 criterion          | Threshold | Observed                | Verdict |
|--------------------------------|-----------|-------------------------|---------|
| PhysMem unused increase        | ≥ 5 GB    | +<X> GB                 | <PASS / FAIL> |
| Compressor (post-unload)       | < 1 GB    | <Y> MB                  | <PASS / FAIL> |

122B RSS hypothesis (RESEARCH §Pitfall 5): <CONFIRMED — RSS stable / REJECTED — RSS expanded>.
Phase 17 dual-loaded steady-state was 45.4 GB; post-unload single-model = <Z> GB.

122B health post-unload: thinking-mode smoke <PASS>, JSON-schema smoke <PASS>.

## 18-02 Bench results (summary)

Lifted from `18-02-BENCH-RESULTS.md` §3 + §4 + §6:

Bench wall-clock: <T> s (~<min> min). Total invocations: <N>. Non-zero exits: <K>.

| ROADMAP §SC4 criterion              | Threshold       | Observed             | Verdict |
|------------------------------------|------------------|----------------------|---------|
| T1 median elapsed (variance, 3 runs) | ≤ 6s          | <m1>s                | <PASS / FAIL> |
| T2 median elapsed (variance, 3 runs) | ≤ 6s          | <m2>s                | <PASS / FAIL> |
| T6_122b step count                 | ≤ 5 (baseline_max)| <s6>                | <PASS / FAIL> |
| W1_122b step count                 | ≤ 3 (baseline_max)| <sw1>               | <PASS / FAIL> |
| W2_122b step count                 | ≤ 3 (baseline_max)| <sw2>               | <PASS / FAIL> |
| B2_122b step count                 | ≤ 3 (baseline_max)| <sb2>               | <PASS / FAIL> |
| B2 actual_diagnosis preserved      | DivByZero on empty list | <observed quote>| <PASS / FAIL> |

B2 diagnosis quote (verbatim from `<LATEST_RUN>/diagnose_B2_122b.log`):
> <quote>

Phase 17 baseline `actual_diagnosis` (B2_122b from `bench/baseline.json`):
> empty list causes DivideByZeroException — 'The bug is a division by zero when the input list is empty, because List.length [] returns 0, and dividing by zero raises a DivideByZeroException.'

Semantic equivalence: <YES — preserved / NO — diverged / PARTIAL — explain>.

Post-bench 122B RSS: <Z2> GB (Δ vs pre-bench <Z>: <Z2 - Z> GB). Phase 17 finding was that RSS held
flat at 45.4 GB through bench-all; <CONFIRMED / REJECTED — RSS expanded by <Δ> GB during bench>.

## Per-test comparison (Phase 18 single-model 122B vs Phase 17 dual-loaded 122B)

| Test          | Phase 17 (dual-loaded) elapsed (s) | Phase 18 (single-model) elapsed (s) | Δ (s) | Δ % | Phase 17 steps | Phase 18 steps | Δ steps |
|---------------|------------------------------------|-------------------------------------|-------|-----|----------------|----------------|---------|
| T1_122b       | 4                                  | <e>                                 | <Δ>   | <%> | 1              | <s>            | <Δ>     |
| T2_122b       | 3                                  | <e>                                 | <Δ>   | <%> | 1              | <s>            | <Δ>     |
| T5_122b       | 6                                  | <e>                                 | <Δ>   | <%> | 3              | <s>            | <Δ>     |
| T6_122b       | 11                                 | <e>                                 | <Δ>   | <%> | 4              | <s>            | <Δ>     |
| T7_122b       | 15                                 | <e>                                 | <Δ>   | <%> | -              | <s>            | -       |
| W1_35b→W1_122b | (35B in Phase 17 dual: 5s)        | <e>                                 | <Δ>   | -   | 3              | <s>            | <Δ>     |
| W2_35b→W2_122b | (35B in Phase 17 dual: 6s)        | <e>                                 | <Δ>   | -   | 3              | <s>            | <Δ>     |
| B2_122b       | (n/a — diagnose; ~5-7s)            | <e>                                 | -     | -   | 2              | <s>            | <Δ>     |

Interpretation:
- Tests previously routed to 35B (W1/W2) now go to 122B; expect SLOWER but still ≤ baseline_max (3 steps).
- 122B-only tests (T1/T2/T5/T6/T7/B2) may be marginally faster (no mmap competition with 35B) or identical.
- Step-count drift (Δ steps > 0) on 122B-only tests would indicate single-model degradation; expected: 0 drift.

## Decision matrix

The verdict applies the 5 ROADMAP §SC4 criteria. Each is PASS or FAIL.

| # | Criterion                              | Threshold              | Observed (from §18-01 + §18-02) | Verdict |
|---|----------------------------------------|------------------------|--------------------------------|---------|
| 1 | T1/T2 median elapsed                   | ≤ 6s                   | T1=<m1>s, T2=<m2>s             | <PASS / FAIL> |
| 2 | T6/W1/W2/B2 step counts                | ≤ Phase 17 baseline_max | T6=<s6>/5, W1=<sw1>/3, W2=<sw2>/3, B2=<sb2>/3 | <PASS / FAIL> |
| 3 | B2 actual_diagnosis preserved          | "DivideByZeroException" semantic equivalent | <quote>     | <PASS / FAIL> |
| 4 | PhysMem unused increase post-unload    | ≥ 5 GB                 | +<X> GB                        | <PASS / FAIL> |
| 5 | Compressor post-unload                 | < 1 GB                 | <Y> MB                         | <PASS / FAIL> |

### Decision rule

- **5/5 PASS → DROP-35B**: 122B alone is fully viable; architectural follow-ups (Router collapse, baseline halve) are warranted.
- **2+ FAIL → KEEP-DUAL**: Dual model is correct; 35B is reloaded; no architectural changes; eval recorded as "evaluated, deferred".
- **Exactly 1 FAIL with mitigation → CONDITIONAL**: 122B-only viable for some workloads but with caveat; opt-in mechanism (env var or CLI flag) sketched in §Follow-ups; default stays dual until follow-up implements opt-in.
- **Exactly 1 FAIL without mitigation → KEEP-DUAL** (conservative).

## VERDICT: <DROP-35B | KEEP-DUAL | CONDITIONAL>

PASS count: <P>/5. FAIL count: <F>/5.

<Rationale paragraph (2-4 sentences):>
- If DROP-35B: "All 5 criteria PASS. 122B alone meets latency, step-count, diagnosis, memory-headroom, and compressor thresholds. Single-model is a viable canonical configuration. Follow-up phase to execute the architectural changes is recommended."
- If KEEP-DUAL: "<N> criteria FAIL: <list>. Dual-model 35B+122B remains canonical. 35B was reloaded post-bench. The eval is recorded as 'evaluated, deferred'."
- If CONDITIONAL: "1 criterion FAIL (<which>); mitigation: <e.g., env var BLUECODE_FORCE_PORT=8001 to opt into single-model on demand>. Default stays dual; follow-up phase implements the opt-in."

(Replace placeholders with mechanically-derived values.)
```

**Step 5.** Atomic commit:

```bash
git add documentation/single-model-eval.md
git commit -m "docs(18-03): single-model 122B evaluation — verdict <DROP-35B|KEEP-DUAL|CONDITIONAL>

Decision matrix applied (ROADMAP §SC4): <P>/5 PASS, <F>/5 FAIL.
Per-test comparison vs Phase 17 dual-loaded baseline tabulated.
Architectural follow-ups (Router collapse, baseline halve, CLAUDE.md update)
DEFERRED to follow-up phase per ROADMAP §SC5 — not executed in 18-03."
```

(Replace `<DROP-35B|KEEP-DUAL|CONDITIONAL>` with the actual verdict, and `<P>`/`<F>` with the actual counts.)
  </action>
  <verify>
1. `test -f documentation/single-model-eval.md` exits 0.
2. `grep -E "VERDICT: (DROP-35B|KEEP-DUAL|CONDITIONAL)" documentation/single-model-eval.md` returns ≥ 1 match.
3. `grep -c "Decision" documentation/single-model-eval.md` returns ≥ 2 (the literal string appears in section heading + body).
4. `grep -c "PASS\|FAIL" documentation/single-model-eval.md` returns ≥ 10 (decision matrix is densely tagged).
5. `wc -l documentation/single-model-eval.md` returns ≥ 100 (decision matrix + per-test comparison; full 150 lands after Task 2).
6. `grep -c "ROADMAP §SC4" documentation/single-model-eval.md` returns ≥ 2.
7. `grep -c "Phase 17\|dual-loaded" documentation/single-model-eval.md` returns ≥ 3 (per-test comparison anchored to Phase 17).
8. `git log -1 --pretty=%s | grep -E "docs\(18-03\): single-model 122B evaluation"` matches.
9. `git status --short documentation/single-model-eval.md` returns empty (committed).
  </verify>
  <done>
`documentation/single-model-eval.md` first part lands with: Overview, Methodology, 18-01 + 18-02 summaries, per-test comparison vs Phase 17 dual-loaded baseline, decision matrix (5 criteria, mechanical), and named verdict with rationale paragraph. Verdict is one of DROP-35B / KEEP-DUAL / CONDITIONAL. Atomic commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reversibility section (35B reload) + conditional follow-ups + final commit</name>
  <files>documentation/single-model-eval.md, .planning/STATE.md</files>
  <action>
Append the reversibility section and conditional-follow-ups list, then perform the 35B reload (mandatory on KEEP-DUAL; recommended on DROP-35B/CONDITIONAL), then update STATE.md.

**Step 1.** Read the verdict from `documentation/single-model-eval.md`:

```bash
VERDICT=$(grep -E "VERDICT: (DROP-35B|KEEP-DUAL|CONDITIONAL)" documentation/single-model-eval.md | head -1 | sed -E 's/.*VERDICT: //; s/ .*//')
echo "Verdict: $VERDICT"
```

**Step 2.** Append the reversibility section + follow-ups list to `documentation/single-model-eval.md`:

```markdown
## Reversibility — 35B reload procedure

Phase 18 made ZERO permanent code changes. The system can be returned to Phase 17 canonical state
(both 35B and 122B loaded) by reloading the 35B service.

Reload command (per `documentation/qwen35-install.md §5.1.1`):

```bash
launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist
until curl -fsS http://127.0.0.1:8000/v1/models > /dev/null 2>&1; do sleep 3; done
echo "35B reloaded"
launchctl list | grep ohama   # should show BOTH com.ohama.qwen35b and com.ohama.qwen122b
curl -fsS http://127.0.0.1:8000/v1/models > /dev/null && echo "8000 OK"
curl -fsS http://127.0.0.1:8001/v1/models > /dev/null && echo "8001 OK"
```

If reload fails with `Load failed: 5: Input/output error` (RESEARCH §Pitfall 2), use bootout/bootstrap:

```bash
launchctl bootout gui/$(id -u) ~/Library/LaunchAgents/com.ohama.qwen35b.plist
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.ohama.qwen35b.plist
```

### Reload disposition by verdict

- **VERDICT = KEEP-DUAL:** Reload is **MANDATORY** for reversibility. The system must be returned
  to Phase 17 canonical state (both services loaded). Performed in 18-03 Task 2 Step 3 (autonomous).

- **VERDICT = DROP-35B:** Reload is **RECOMMENDED** for the Phase 17 reversibility window (≥ 1 week
  of stable single-model operation before any cleanup of old plists / model files). The actual
  decision (reload now vs stay single-model) is the user's. The follow-up architectural-changes
  phase decides whether to formally unload 35B; until then, single-model is informal.

- **VERDICT = CONDITIONAL:** Reload is **MANDATORY**. Until the follow-up phase implements the
  opt-in mechanism (env var or CLI flag), default behavior must remain dual-model.

### Reload performed in this plan?

<YES (verdict was KEEP-DUAL or CONDITIONAL) | NO (verdict was DROP-35B; user opted to stay single-model) | YES — opted-in even though optional>

Verification (post-reload, if performed):
- `launchctl list | grep ohama` lines: <copy actual output>
- `curl http://127.0.0.1:8000/v1/models`: <200 / Connection refused>
- `curl http://127.0.0.1:8001/v1/models`: <200>

## Conditional follow-ups (DEFERRED — not executed in 18-03)

Per ROADMAP §SC5, the architectural changes are gated on the verdict and DEFERRED to a follow-up
phase. They are NOT executed in 18-03. Enumerated here for the follow-up planner:

1. **Router collapse:** `src/BlueCode.Core/Router.fs` `modelToEndpoint` currently maps both `Qwen32B`
   and `Qwen72B` (semantic small/large labels) to ports 8000 and 8001. If DROP-35B, the small
   route becomes dead code or both routes collapse to port 8001. Decision belongs to the follow-up
   phase: rename DU cases? Delete one? Keep both for symmetry?

2. **bench/baseline.json halve:** Currently 8 entries (`T6_35b`, `T6_122b`, `W1_35b`, `W2_35b`,
   `T1_35b`, `T5_122b`, `B2_35b`, `B2_122b`). If DROP-35B, the `_35b` keys become unreachable; the
   gate would always fail with connection-refused on port 8000. The follow-up phase either re-keys
   all entries to `_122b` (single-model gate) or removes the small-slot entries entirely.

3. **CLAUDE.md `## Runtime Environment`:** Currently names BOTH 35B and 122B with their plist files.
   If DROP-35B, the small-slot description is removed. If CONDITIONAL, an opt-in note is added.

4. **scripts/bench-122b-only.sh disposition:** If DROP-35B, this script either replaces `bench/run.sh`
   or both scripts coexist (one for single-model, one for dual). The follow-up phase decides.

5. **Phase 16 implications:** Phase 16 plans on disk (16-01..16-03) reference `_35b/_122b` bench
   keys. If DROP-35B and follow-up halves baseline.json, Phase 16-03 needs another mechanical
   re-key (similar to the 17-03 SWITCH re-key). Document this in the follow-up phase plan.

The follow-up phase is OUT OF SCOPE for v2.0 unless verdict = DROP-35B. If KEEP-DUAL, no follow-up
is needed (Phase 18 is "evaluated, deferred"). If CONDITIONAL, the follow-up implements the opt-in.

## Phase 18 disposition

Phase 18 SC1: <PASS — eval doc ≥ 150 lines, contains "Decision">
Phase 18 SC2: <PASS — 35B unloaded via launchctl, memory snapshot captured (18-01 §1+§3)>
Phase 18 SC3: <PASS — bench-122b-only.sh ran 30+ invocations, all routed to 122B (18-02 §1)>
Phase 18 SC4: <PASS — 5 criteria evaluated; verdict named (above)>
Phase 18 SC5: <PASS — architectural follow-ups enumerated as deferred (above); no code changes in this plan>

Phase 18 is shippable.
```

**Step 3.** Perform the 35B reload per the disposition rule:

```bash
case "$VERDICT" in
  KEEP-DUAL|CONDITIONAL)
    echo "Verdict $VERDICT: reloading 35B (mandatory)..."
    launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist
    # Poll for readiness
    for i in $(seq 1 60); do
      if curl -fsS http://127.0.0.1:8000/v1/models > /dev/null 2>&1; then
        echo "35B reloaded after ${i}*3 seconds"
        break
      fi
      sleep 3
    done
    # Final verification
    launchctl list | grep ohama | tee /tmp/18-03-reload-verify.txt
    curl -fsS http://127.0.0.1:8000/v1/models > /dev/null && echo "8000 OK" || echo "8000 FAILED — investigate"
    curl -fsS http://127.0.0.1:8001/v1/models > /dev/null && echo "8001 OK"
    ;;
  DROP-35B)
    echo "Verdict DROP-35B: reload is RECOMMENDED but optional."
    echo "Recommendation: reload to maintain Phase 17 reversibility window."
    echo "Reload command (run when ready):"
    echo "  launchctl load -w ~/Library/LaunchAgents/com.ohama.qwen35b.plist"
    echo "Auto-reload disabled in this run; system stays single-model."
    # NOTE: For a fully-autonomous DROP-35B path, this plan does NOT auto-reload.
    # The eval doc records the recommendation; the user/follow-up phase decides.
    # If preferred, change this branch to also reload — but ROADMAP allows either path.
    ;;
  *)
    echo "ERROR: Unknown verdict '$VERDICT' — cannot determine reload disposition."
    exit 1
    ;;
esac
```

After the reload (or skip), update the "Reload performed in this plan?" line in `documentation/single-model-eval.md` with the actual outcome (YES / NO / YES-opt-in).

**Step 4.** Update `.planning/STATE.md`. Use the surgical-edit pattern from prior phases:

Read the current STATE.md, then update:

a. **Current Position section** (around line 11-16): Add Phase 18 ✓ marker.

   Before:
   ```
   Phase: Phase 14 ✓ Phase 15 ✓ Phase 16 plans on disk (revision pending — _32b/_72b keys need re-keying to _35b/_122b) Phase 17 ✓ Phase 18 added (not planned yet)
   ...
   Last activity: 2026-04-27 — Phase 18 added to v2.0 (Single-Model 122B Evaluation: ...)
   Progress: v1.0 ✓ → v1.1 ✓ → v1.2 ✓ → v1.3 ✓ → v1.4 ✓ → v2.0 ◆ [Phase 14 ✓ Phase 15 ✓ Phase 16 ░ Phase 17 ✓ Phase 18 ░]
   ```

   After:
   ```
   Phase: Phase 14 ✓ Phase 15 ✓ Phase 16 plans on disk Phase 17 ✓ Phase 18 ✓ (verdict: <VERDICT>; architectural follow-ups deferred)
   ...
   Last activity: <today's ISO date> — Phase 18 complete (verdict: <VERDICT>; eval doc + 18-01 memory profile + 18-02 bench results all on disk; <reload performed YES/NO>; architectural changes deferred to follow-up phase per ROADMAP §SC5)
   Progress: v1.0 ✓ → v1.1 ✓ → v1.2 ✓ → v1.3 ✓ → v1.4 ✓ → v2.0 ◆ [Phase 14 ✓ Phase 15 ✓ Phase 16 ░ Phase 17 ✓ Phase 18 ✓]
   ```

b. **New Decisions section**: Append a new subsection at the bottom:

   ```markdown
   ### New Decisions (18-03)

   - **v2.0 Phase 18 verdict <VERDICT>** — <one-paragraph rationale lifted from §Verdict rationale paragraph>. Eval doc: `documentation/single-model-eval.md`. Memory profile: `18-01-MEMORY-PROFILE.md`. Bench results: `18-02-BENCH-RESULTS.md`. New harness: `scripts/bench-122b-only.sh` (preserved as evaluation evidence regardless of verdict).
   - **v2.0 Phase 18 architectural follow-ups deferred** — Router collapse, bench/baseline.json halve/re-key, CLAUDE.md Runtime Environment update, scripts/bench-122b-only.sh promotion ALL deferred to a follow-up phase per ROADMAP §SC5. Phase 18 made ZERO permanent code changes (zero src/ diff, zero bench/run.sh diff, zero baseline.json diff, zero CLAUDE.md diff).
   - **v2.0 Phase 18 35B reload** — <reload performed YES/NO with rationale>. <If KEEP-DUAL/CONDITIONAL: "System back to Phase 17 canonical dual-model state." If DROP-35B with reload skipped: "System stays single-model; user can reload at will via `launchctl load -w` per documentation/single-model-eval.md §Reversibility.">
   - **v2.0 Phase 18 122B RSS hypothesis** — RESEARCH §Pitfall 5 hypothesized 122B RSS stays near 45.4 GB after 35B unload. Observed: <CONFIRMED — RSS stable / REJECTED — expanded by N GB>. <One-sentence implication.>
   ```

c. **Session Continuity section** (last ~5 lines): Update.

   Before:
   ```
   Last session: 2026-04-27
   Stopped at: Completed 17-03-PLAN.md ...
   Resume file: None — Phase 17 complete (SWITCH to 35B/122B); run Phase 16 next ...
   ```

   After:
   ```
   Last session: <today's ISO date>
   Stopped at: Completed 18-03-PLAN.md (single-model eval — verdict: <VERDICT>; eval doc + STATE updated; <reload performed YES/NO>; architectural follow-ups enumerated as deferred)
   Resume file: None — Phase 18 complete; run Phase 16 next (note: if verdict was DROP-35B, the follow-up architectural-changes phase should run BEFORE Phase 16 so 16-03 bench fixtures use the final canonical baseline keys)
   ```

**Step 5.** Atomic commits — separate concerns per CLAUDE.md commit-protocol guidance:

```bash
# Commit 1: eval doc complete (Reversibility + Follow-ups appended)
git add documentation/single-model-eval.md
git commit -m "docs(18-03): single-model-eval reversibility + deferred follow-ups

35B reload procedure documented (mandatory on KEEP-DUAL/CONDITIONAL,
recommended on DROP-35B). Architectural changes (Router collapse,
baseline halve, CLAUDE.md update) enumerated as DEFERRED per ROADMAP §SC5;
not executed in 18-03. Phase 18 SC1-SC5 all PASS."

# Commit 2: STATE.md update
git add .planning/STATE.md
git commit -m "docs(18): Phase 18 complete (verdict: <VERDICT>)

Current Position updated: Phase 18 ✓.
New Decisions section: 4 entries (verdict, deferred follow-ups, reload, RSS hypothesis).
Session Continuity updated."
```

(Replace `<VERDICT>` with the actual verdict.)
  </action>
  <verify>
1. `grep -c "^## Reversibility" documentation/single-model-eval.md` returns 1.
2. `grep -c "^## Conditional follow-ups" documentation/single-model-eval.md` returns 1.
3. `grep -c "launchctl load -w" documentation/single-model-eval.md` returns ≥ 1 (reload command present).
4. `grep -c "DEFERRED\|Deferred\|deferred" documentation/single-model-eval.md` returns ≥ 3 (deferral language present).
5. `grep -c "Router collapse\|baseline.json halve\|CLAUDE.md" documentation/single-model-eval.md` returns ≥ 3 (all 3 follow-ups enumerated).
6. `wc -l documentation/single-model-eval.md` returns ≥ 150 (full doc per ROADMAP §SC1).
7. `grep -c "^## Phase 18 disposition" documentation/single-model-eval.md` returns 1.
8. `grep -E "Phase 18 SC[1-5]:" documentation/single-model-eval.md | wc -l` returns 5 (all 5 SCs explicitly marked PASS).
9. `grep -c "Phase 18 ✓" .planning/STATE.md` returns ≥ 1 (Current Position updated).
10. `grep -c "^### New Decisions (18-03)" .planning/STATE.md` returns 1.
11. **Reload check (verdict-conditional):**
    - If verdict = KEEP-DUAL or CONDITIONAL: `launchctl list | grep -c ohama` returns 2; `curl http://127.0.0.1:8000/v1/models` returns 200 (35B reloaded).
    - If verdict = DROP-35B (reload skipped): `launchctl list | grep ohama` returns only `qwen122b`; the doc records "Reload performed: NO".
12. `git log --oneline | head -3 | grep -E "(docs\(18-03\)|docs\(18\))" | wc -l` returns ≥ 2 (two atomic commits).
13. **Out-of-scope guard:** `git diff HEAD~5 HEAD -- src/ tests/ bench/run.sh bench/baseline.json CLAUDE.md` returns empty (zero changes to those files in Phase 18).
14. **Phase 18 SCs all satisfied:**
    - SC1: `documentation/single-model-eval.md` ≥ 150 lines, contains "Decision" → verified by checks 6 + (`grep -c "Decision" documentation/single-model-eval.md` ≥ 2).
    - SC2: 35B unloaded → 18-01 record on disk; `launchctl unload` was run in 18-01.
    - SC3: bench-122b-only.sh ran ≥ 30 invocations → 18-02 record on disk.
    - SC4: 5 criteria evaluated → check 4 + decision matrix in eval doc.
    - SC5: architectural changes deferred → check 4 + 5; verified by check 13.
  </verify>
  <done>
`documentation/single-model-eval.md` is complete (≥ 150 lines, all sections populated): Overview, Methodology, 18-01 + 18-02 summaries, per-test comparison, decision matrix, named verdict, reversibility section with 35B reload commands + verdict-conditional disposition, conditional follow-ups list (5 items, all marked DEFERRED), Phase 18 disposition with all 5 SCs marked PASS.

35B reload performed per verdict disposition (mandatory on KEEP-DUAL/CONDITIONAL; skipped or recommended-only on DROP-35B). Verification commands run; outputs recorded in the eval doc.

`.planning/STATE.md` updated: Phase 18 ✓ marker in Current Position; New Decisions (18-03) subsection added with 4 decision entries; Session Continuity updated to reflect Phase 18 completion.

Two atomic commits land: eval doc complete + STATE update. Zero changes to src/, tests/, bench/run.sh, bench/baseline.json, CLAUDE.md (verified by git diff).

Phase 18 is shippable.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `documentation/single-model-eval.md` exists with ≥ 150 lines covering: Overview, Methodology, 18-01 Memory profile summary, 18-02 Bench results summary, Per-test comparison table (vs Phase 17 dual-loaded), Decision matrix (5 criteria with PASS/FAIL), VERDICT (DROP-35B / KEEP-DUAL / CONDITIONAL), Reversibility section with 35B reload commands, Conditional follow-ups list (DEFERRED), Phase 18 disposition with 5 SCs marked PASS.

2. The verdict is ONE OF: DROP-35B (5/5 PASS) / KEEP-DUAL (2+ FAIL or 1 FAIL without mitigation) / CONDITIONAL (1 FAIL with documented mitigation). The verdict is mechanically derivable from the decision matrix.

3. 35B service disposition matches verdict:
   - KEEP-DUAL or CONDITIONAL: 35B reloaded; `launchctl list | grep -c ohama` = 2; both ports 8000+8001 respond 200.
   - DROP-35B: 35B may stay unloaded (user opt-in via follow-up phase) OR be reloaded for reversibility window — either is valid.

4. `.planning/STATE.md` updated: Phase 18 ✓ in Current Position; New Decisions (18-03) section with 4 entries; Session Continuity reflects Phase 18 completion.

5. Two atomic commits land per CLAUDE.md commit-protocol convention:
   - `docs(18-03): single-model 122B evaluation — verdict <X>` (eval doc Task 1)
   - `docs(18-03): single-model-eval reversibility + deferred follow-ups` (eval doc Task 2 part 1)
   - `docs(18): Phase 18 complete (verdict: <X>)` (STATE.md update)
   (Task 1's commit + Task 2's two commits = 3 total; or Task 2 commits combined = 2; either layout acceptable as long as eval doc + STATE are committed.)

Out-of-scope guardrails (these MUST NOT change in 18-03):
- `src/**` — UNTOUCHED. Phase 18 makes ZERO Core/Cli source changes per ROADMAP §SC5.
- `tests/**` — UNTOUCHED.
- `bench/run.sh` — UNTOUCHED. Phase 17 gate authority preserved.
- `bench/baseline.json` — UNTOUCHED. Architectural changes (including baseline halve/re-key) deferred per ROADMAP §SC5.
- `bench/fixtures/**` — UNTOUCHED.
- `scripts/bench-122b-only.sh` — UNTOUCHED in 18-03 (created in 18-02; preserved here).
- `CLAUDE.md` — UNTOUCHED. The Runtime Environment update is a deferred follow-up per ROADMAP §SC5 (enumerated in §Follow-ups, not executed).
- `~/Library/LaunchAgents/com.ohama.qwen{35b,122b}.plist` — UNTOUCHED (the reload uses these as-is).
- `documentation/qwen35-install.md` — UNTOUCHED (single-model-eval.md is a new sibling doc; install doc is unchanged).
- `documentation/benchmark-qwen35-eval.md` — UNTOUCHED (Phase 17 baseline; lifted from but not modified).
- `.planning/ROADMAP.md` — UNTOUCHED in 18-03 itself (planner updated it during plan creation; execution does not edit it again).
- v2.1+ candidates (Router collapse, baseline halve, CLAUDE.md update, SHIP-BOTH per-task routing) — explicitly DEFERRED, mentioned in §Conditional follow-ups as roadmap candidates for a follow-up phase.

Tests + build + Core purity green throughout (no source changes; this is purely doc + STATE work):
- `dotnet build BlueCode.sln` exits 0.
- `bash scripts/check-no-async.sh` exits 0.
- `grep -rn "llm-system" src/` returns 0.
</verification>

<success_criteria>
This plan satisfies Phase 18 Success Criteria #1, #4, and #5 in full:

> SC1: `documentation/single-model-eval.md` exists (≥ 150 lines), contains "Decision". Per-test comparison table covers latency + step counts + diagnose accuracy for 122B-only vs Phase 17 dual-loaded baseline. Decision matrix has explicit pass/fail criteria, not "use judgment". Verdict named: DROP-35B / KEEP-DUAL / CONDITIONAL.

Mapping:
- "≥ 150 lines, contains 'Decision'" → must_haves and verify checks 6 + (`grep -c "Decision"` ≥ 2).
- "Per-test comparison table covers latency + step counts + diagnose accuracy" → §Per-test comparison table (Task 1 Step 4); columns include elapsed Δ, step Δ, B2 diagnose verdict.
- "Decision matrix has explicit pass/fail criteria, not 'use judgment'" → §Decision matrix table (Task 1 Step 4); each of 5 criteria has explicit threshold + observed value + PASS/FAIL.
- "Verdict named: DROP-35B / KEEP-DUAL / CONDITIONAL" → §Verdict heading (Task 1 Step 4).

> SC4: Latency thresholds applied per Decision criteria: T1/T2 (simple tasks) median ≤ 6s on 122B-only ... T6/W1/W2/B2 step counts match Phase 17 baseline ... B2 actual_diagnosis text preserved.

Mapping (mechanically applied via decision matrix):
- T1/T2 median ≤ 6s → criterion 1 in matrix.
- T6/W1/W2/B2 step counts ≤ baseline_max → criterion 2 in matrix.
- B2 actual_diagnosis preserved → criterion 3 in matrix.
- (Memory portions PhysMem unused ≥ 5 GB and Compressor < 1 GB → criteria 4 + 5; data lifted from 18-01.)

> SC5: If verdict is DROP-35B: doc enumerates the architectural changes needed but does NOT execute them in this phase. A separate follow-up plan handles the code changes. If KEEP-DUAL: 35B is reloaded post-bench; findings recorded as "evaluated, deferred". If CONDITIONAL: documented opt-in mechanism sketched; full implementation OOS.

Mapping:
- "DROP-35B: enumerates architectural changes but does NOT execute" → §Conditional follow-ups (Task 2 Step 2); 5 items listed; all marked DEFERRED; verify check 13 confirms zero changes to src/, tests/, bench/run.sh, bench/baseline.json, CLAUDE.md.
- "KEEP-DUAL: 35B reloaded post-bench; findings recorded as 'evaluated, deferred'" → §Reversibility section (Task 2 Step 2); reload command + verification verbatim; STATE.md New Decisions records the disposition.
- "CONDITIONAL: documented opt-in mechanism sketched" → §Verdict rationale + §Conditional follow-ups (Task 2 Step 2); the eval doc sketches an env var or CLI flag opt-in; follow-up phase implements.
</success_criteria>

<output>
After completion, create `.planning/phases/18-single-model-eval/18-03-SUMMARY.md` documenting:
- Verdict (DROP-35B / KEEP-DUAL / CONDITIONAL) with the 5-criteria pass/fail breakdown
- One-paragraph rationale (lifted from eval doc §Verdict)
- 35B reload disposition (performed YES/NO with reason)
- Architectural follow-ups (5 enumerated) — all DEFERRED
- Phase 18 SC1-SC5 marked PASS
- Out-of-scope guardrails verified (zero src/, bench/run.sh, baseline.json, CLAUDE.md changes)
- Reference to:
  - `documentation/single-model-eval.md` (the primary deliverable)
  - `18-01-MEMORY-PROFILE.md` + `18-02-BENCH-RESULTS.md` (predecessor evidence)
  - `scripts/bench-122b-only.sh` (the new harness, preserved regardless of verdict)
  - Follow-up phase note (only if DROP-35B): "Architectural changes (Router collapse, baseline halve, CLAUDE.md update, scripts/bench-122b-only.sh promotion) require a follow-up phase. Phase 16 may be re-keyed yet again if baseline halves."

The SUMMARY.md is the consumable handoff for Phase 18 completion. Combined with 18-01-SUMMARY.md and 18-02-SUMMARY.md it forms the phase's full delivery record.
</output>
</content>
